# Approach ledger — current and retained pitfalls

Updated 2026-09-05. Full attempts A1–A304 and C1–C2 are preserved verbatim in
[the July archive](archive/2026-07-27/APPROACH_LEDGER.md). IDs are permanent.
Historical ACTIVE labels there refer to their dated checkpoint, not today's plan.

## September proof audits

The repository contains an independently audited finite-matrix reconstruction
of the Lorist–Schwenninger argument and an adversarial audit of the earlier
local frontier. See `RESEARCH_STATE.md` for the results and source links.

## Latest local approaches (historical status retained)

| ID | Track | Idea | Status | Outcome / failure condition |
|---|---|---|---|---|
| A277 | F/G | Assemble the compatible Jensen, joint disk/channel, scalar normal/reflected, and grouped elliptic margins by finite curve selection | **PROVED FIXED REPEATED-BLOCK SCALAR NEIGHBOURHOOD (L329)** | Compactify the scalar extremal by Crouzeix's degree-`pm−1` Blaschke reduction and fix a ramified rank stratum.  L61/L199 enter a zero-Jensen scalar winner.  L324/L327 make every accumulated disk/channel response quadratic in L325's same-direction linear defect; one vector completion pays it.  L317/L318 retain the exact grouped elliptic margin and analytic tail.  L197 makes the disk descent finite, while L326/L328 split exactly one Gau--Wu model and reduce multiplicity, with L192 as base.  A stratumwise Taylor/curve-selection argument closes the scalar local theorem without claiming a complete-similarity or global result. `proof/repeated_crabb_scalar_local_theorem.md`; `experiments/repeated_crabb_scalar_local_merger.py`. |
| A278 | F/G | Before searching for a global sharp-sequence classification, test whether the already known Gau--Wu equality family lies in the L192/L329 Crabb charts | **NAIVE COVERAGE FALSE; EXACT NONCRABB SHARP FAMILY L330** | The degree-two Blaschke product `f_a=z(z−a)/(1−az)` has Gau--Wu matrix `G_a` with support eigenvalues `1,−1,a cos theta`, so `W(G_a)=D`, and direct functional calculus gives `f_a(G_a)=2E_13`.  For `a!=0`, the spectrum `{0,0,a}` rules out affine similarity to a Crabb block.  Thus the first post-L329 wording skipped known exact equality points.  Replace global classification by the ordered task of proving a scalar neighbourhood theorem at arbitrary fixed Gau--Wu disk models, beginning with `G_a`; only then classify sharp sequences relative to that larger equality manifold. `proof/gau_wu_noncrabb_sharp_stratum.md`; `experiments/gau_wu_noncrabb_sharp_stratum.py`. |
| A279 | F/G | Derive the full ambient first variation at `G_a` before opening a second-order local proof | **PROVED EXACT CONFORMAL STATIONARITY (L331)** | Differentiate the rational extremal `f_a` and use L101's support/Riemann tangent.  The support projection is explicit because its eigenvalues are `1,−1,a cos theta`.  After `a=2q/(1+q²)`, every required Poisson-weighted support mean is rational; residues at `0,q,a` reproduce the exact Frechet-gradient matrix.  Thus the raw functional derivative and conformal-domain correction cancel in real part for every complex perturbation.  There is no first-order obstruction or descent to exploit; quotient the exact disk/Gau--Wu tangent and compute the optimized transverse Hessian next. `proof/gau_wu_ambient_stationarity.md`; `experiments/gau_wu_ambient_stationarity.py`. |
| A280 | F/G | Quotient the exact Gau--Wu equality tangent, optimize both moving Blaschke zeros, and sign the full transverse second variation | **PROVED STRICT FIVE-NORMAL HESSIAN + FULL FIXED-MODEL SCALAR NEIGHBOURHOOD (L332--L333)** | The equality tangent has dimension 13, not the preliminary count 12; a closed `18×18` determinant gives a five-dimensional bottom-row complement.  Exact support projections and second normal data give the Riemann jet by residues at `0,q,a`.  The nonnilpotent Frechet term requires degree-five Hermite calculus for the doubled minimal polynomial; reusing the nilpotent Crabb truncation falsely creates positive modes.  The four zero velocities have a negative-definite Hessian, and their exact Schur complement splits into real `3×3` and imaginary `2×2` normal blocks.  Sylvester's criterion reduces the sign to four positive Bernstein lists on `q² in [0,1]`, proving strict negativity for every fixed `0<|a|<1`.  Gau--Wu uniqueness at the irreducible base plus compact separation of all other degree-at-most-two Blaschke branches then yields a full scalar constant-two neighbourhood.  This closes the first non-Crabb local stratum; the next ordered target is the arbitrary finite Gau--Wu model, not a global `3×3` claim. `proof/gau_wu_transverse_hessian.md`; `experiments/gau_wu_transverse_hessian.py`. |
| A281 | F/G | Before deriving an arbitrary-degree Gau--Wu Hessian identity, falsify its sign on the complete normal quotient in dimensions four through eight | **NUMERICALLY SURVIVES TEN FULL-NORMAL MODELS (L334); DERIVE ONE DEFECT-ONE GRAM NEXT** | The general model generator verifies `W(A)=D` and `f(A)=2E_(1n)`.  SVD gives tangent rank `n²+2n−2` and normal dimension `(n−1)²+1`.  A general analytic support/Riemann jet, followed by the Schur complement of all `2(n−1)` Blaschke-zero velocities, reproduces L332's complex `3×3` spectrum to `1.76e−13`.  Every eigenvalue in ten complete normal Hessians for `n=4..8` is negative; the softest is `−1.6048e−8` near a root collision and is stable from 256 to 2048 angular nodes, but remains conservatively labelled unresolved rather than proved.  Independent true-map/nonlinear optimization confirms a soft `n=5` direction.  No isolated-size proof grind is warranted: rewrite the jet as a defect-one/model-space negative Gram and prove quotient injectivity. `proof/gau_wu_finite_hessian_falsification.md`; `experiments/gau_wu_finite_hessian_jet.py`. |
| A282 | F/G | Isolate the general Hessian contribution of the second numerical-range support coefficient before guessing a full defect-one factorization | **PROVED DIMENSION-FREE POSITIVE-WEIGHT NEGATIVE GRAM (L335); HARDY REMAINDER IS THE SOLE LOCAL DEBT** | The model transition `<p,h(A)q>=2<f,h>_(H²)` turns the inverse-Riemann term `−K_t(A)` into `−2 Re<f,f'K_t>`.  Since `zeta f'/f` is the positive sum of the Poisson kernels of the Blaschke zeros and `Re H_t=t`, this is exactly `−2∫(zeta f'/f)t dm`.  The formula is fully polarized and independent of model degree.  It leaves the sharp comparison `max_u H_rest(E,u)<=2∫D_f t_E`; numerical inertia says only `2(n−2)` real directions are dangerous.  A raw partial-isometry-defect residual was tested first and rejected because its kernel misses two Hessian directions. `proof/gau_wu_second_support_gram.md`; `experiments/gau_wu_second_support_gram.py`. |
| A283 | F/G | Replace the indefinite L335 remainder split by the two-sided endpoint defects of the complete Blaschke image | **EXACT ENDPOINT IDENTITY L336; SEPARATE SIGNS FALSE, SYMMETRIC STEIN-FLUX SIGN OPEN** | Conjugate the complete jet back to `S(zf)` and write `Y_e=f_e(T_e)`.  Simple-singular-value algebra at `pq*`, including the endpoint weights of `X`, rewrites the sharp Hessian as `−(r_2+l_2)/2` minus two diagonal squares.  Blaschke--Stein composition makes `r_2,l_2` the right and left endpoint defect fluxes.  Cheap falsification rejects both one-sided signs: every standard model with `n=4..8` has two negative directions at each endpoint, and holdouts confirm the failure; their sum remains positive in 54 complete standard/holdout tests.  The contraction and moving-characteristic residuals jointly have numerical rank `n²`, explaining the whole nonflat quotient.  Prove the symmetric sum by keeping both Stein identities coupled; do not return to pointwise support domination or the incomplete raw partial-isometry defect. `proof/gau_wu_two_sided_endpoint_defect.md`; `experiments/gau_wu_two_sided_endpoint_defect.py`. |
| A284 | F/G | Separate the pure inner-function motion from L336 before completing the mixed endpoint flux | **PURE ZERO SECTOR EXACTLY POSITIVE (L337); MIXED HARDY COMPLETION OPEN** | At fixed `S(zf)`, the canonical model conjugation sends the right output `P_(K_phi)g` to the left output, so both endpoint defects equal the single distance `||P_(phi H²)g||²` for every inner `g`.  Infinitesimally the zero block is `2||P_(phi H²)h||²`, not an opaque numerical Schur matrix.  Express the mixed physical/zero term as a Riesz pairing in this metric, complete the Hardy projection square, and retain the resulting purely physical remainder.  Do not infer the mixed sign from pure zero positivity. `proof/gau_wu_zero_motion_endpoint_projection.md`; `experiments/gau_wu_zero_motion_endpoint_projection.py`. |
| A285 | F/G | Parametrize every inner tangent by the smaller model space and eliminate all zero velocities before signing L336 | **EXACT MIXED HARDY COMPLETION L338; PURELY PHYSICAL RIESZ DOMINATION OPEN** | The phase-free tangent chart is `h_k=(zf)k−J_fk`, with zero velocities represented by the Szegő kernels of `K_f`.  This turns the pure zero block into `2||k||²` and the mixed response into one real Riesz vector `kappa_C`.  Completing the square gives `2||k−kappa_C||²+Q(C,0)−2||kappa_C||²`; hence the optimizing zero tangent and the remaining physical gate are exact in every degree.  Insert L335's support Gram into `Q(C,0)` and prove it dominates `2||kappa_C||²` through the contraction/characteristic residuals.  Do not rebuild a real zero Schur matrix or optimize roots numerically. `proof/gau_wu_mixed_hardy_completion.md`; `experiments/gau_wu_mixed_hardy_completion.py`. |
| A286 | F/G | Absorb L335's Blaschke angular-derivative weight into the canonical support null state before estimating L338's Riesz response | **EXACT UNWEIGHTED SUPPORT-PORT ENERGY L339; LOSSLESS RESPONSE CONTRACTION OPEN** | The defect-one completion `U=S+qp*` gives the singular Ando pencil `F_zeta=UD−zeta E`.  Its null state is the model kernel scaled only on the middle summand, and its squared norm is exactly `2D_f(zeta)`.  Therefore L335's reserve is the ordinary `L²` energy of the minimal response `(F_zeta*)^dagger H_Cy_zeta`.  Prove the remaining physical loss and `kappa_C` are outputs of this same unitary colligation and satisfy the corresponding energy contraction.  Do not estimate the Poisson weight separately or return to pointwise support domination. `proof/gau_wu_support_port_energy.md`; `experiments/gau_wu_support_port_energy.py`. |
| A287 | F/G | Before deriving the final port contraction, determine whether its loss map is genuinely quadratic-size or only boundary-size | **NUMERIC LOW-RANK LAW L340: PSD LOSS, RANK `6n−14`, NULLITY `(n−4)²`; PROOF OPEN** | After L338's zero completion and L339's support Gram, the loss `G_phi=2I+2P_phi−H_phys` is PSD to roundoff in twelve complete models through dimension nine.  Its rank is exactly `6n−14`, leaving an automatic `(n−4)²` bulk.  Seek an explicit boundary response `L_phi` from the paired Stein/Ando colligation and prove its Gram equals `G_phi`, then prove its energy contraction.  Reject any proposed factor that does not reproduce the rank/nullity law before investing in its proof. `proof/gau_wu_physical_loss_rank.md`; `experiments/gau_wu_physical_loss_rank.py`. |
| A288 | F/G | Replace L340's abstract numerical square root by canonical first-support and two-endpoint observations | **EXACT PAIRED TRANSFERRED-FRAME EQUATIONS L341; NUMERIC KERNEL/RANK LAW, RESPONSE METRIC OPEN** | The primal/dual L127 frames have base projections `I−pp*` and `I−qq*`; their differentiated Stein equations are forced by `C`, `Y_1q`, and `Y_1*p`, and their moving kernels supply exact second-order squares.  Numerically the loss kernel is exactly the kernel of `O_phi(E)=(s_E,Y_1^opt q,(Y_1^opt)*p)`, with endpoint rank `4n−8`, support increment `2n−6`, and total rank `6n−14`.  Derive `G_phi=O_phi* M_phi O_phi` directly from the coupled frames and prove the L339 port contraction.  Do not use a data-dependent Cholesky factor or split the endpoints. `proof/gau_wu_boundary_observation.md`; `experiments/gau_wu_boundary_observation.py`. |
| A289 | F/G | Test whether L21's stronger similarity route has a different local obstruction at a general Gau--Wu equality model before continuing the boundary-loss factorization | **EXACT SECOND-ORDER OSCULATION L342; UNIVERSAL FINITE QUADRATIC IS THE NEW COMMON GATE** | The general second-order SDP has the same sharp metric and dual balance at every model.  Its first metric derivative is determined by one boundary row, its free Hessian is the universal diagonal form `8I` with one terminal `8/3` weight, and eliminating the second metric coefficient is exact.  More strongly, the difference between this metric quadratic and four times the joint scalar Hessian is the sum of two explicit endpoint-matching squares.  The residual map is bijective, so the optimized similarity and scalar Hessians agree exactly.  This removes the strategic uncertainty flagged for the cb route: locally it is neither stronger nor weaker at quadratic order.  Prove the common sign as `min_x F_C(x)<=0`, using the universal metric curvature and L339's support port; keep L341 as the equivalent scalar-response interpretation. `proof/gau_wu_similarity_osculation.md`; `experiments/gau_wu_similarity_hessian.py`. |
| A290 | F/G | Recenter L187's full disk chart at an arbitrary Gau--Wu equality point and use the known disk theorem to remove the quadratic-size bulk before attacking L342's sign | **SUCCEEDED AS L343; LIVE FORM REDUCED TO A CANONICAL `2n−2` BOUNDARY-SHAPE SCHUR FORM** | The model resolvent supplies an exact polynomial support frame, whose support-pencil congruence recovers L122's general-H chart and places the anchor on its inverse-Toeplitz equality locus globally.  The general-H directions transverse to that locus form a `(n−2)^2`-dimensional block on which Okubo--Ando plus L183's linearized Hardy residual gives strict negative curvature.  With `omega_phi=|delta(conj(zeta))|²D_f`, the quotient is exactly the non-affine polynomial support motion `[omega_phi s] mod omega_phi span{1,cos,sin}`, of dimension `2n−2`; its kernel is the disk block.  Continue only on this quotient, seeking a Hardy/port factor for its Schur maximum.  Its tested signs remain evidence, not proof. `proof/gau_wu_disk_chart_recenter.md`; `experiments/gau_wu_disk_chart_recenter.py`. |
| A291 | F/G | Use L343's common support-pencil factorization to remove L339's pointwise moving pseudoinverse before signing the shape quotient | **SUCCEEDED AS L344; PORT IS A FIXED INVERSE-TOEPLITZ HARDY NORM** | The Ando factor times the polynomial frame and `Hhat^(1/2)(I−conj(zeta)R)` have the same Gram and rank, hence differ by a range isometry.  The minimal port response transports exactly to `Hhat^(dagger/2)(I−zeta R*)^−1 Ccal*r`, with only a fixed pseudoinverse and a triangular nilpotent resolvent.  After polynomialization its scalar forcing is precisely L343's shape coordinate.  Eliminate the strict general-H disk variable directly in this fixed coefficient system and identify the remaining endpoint output; do not return to the moving support pseudoinverse. `proof/gau_wu_toeplitz_port_transport.md`; `experiments/gau_wu_toeplitz_port_transport.py`. |
| A292 | F/G | Replace L343's weighted polynomial coordinate by the ordinary conformal support coefficients and test whether the real shape form has a hidden phase symmetry | **EXACT COMPLEX COORDINATE CHART; PHASE-COVARIANT HERMITIAN COLLAPSE SURVIVES 15 COMPLETE MODELS, PROOF OPEN** | The modes `2(hat(s)(2),...,hat(s)(n))` give an exact isomorphism from `P_n^R/(omega P_1^R)` to `C^(n−1)`: after killing the low modes of `sigma`, vanishing of these coordinates would give `integral sigma²/omega=integral sigma s=0`.  In these coordinates the Schur form commutes with multiplication by `i` within `3.85e−12` and its complex-symmetric block vanishes at the same scale in three models per dimension `4..8`.  Thus the credible live target is one Hermitian `(n−1)`-square, but neither phase covariance nor its negative sign is promoted.  Derive the phase identity from L344's fixed port before seeking a positive Gram for the negative Hermitian matrix; the nearly paired spectrum in weighted coordinates is not itself an exact symmetry.  Dataset SHA-256 `41e0bec1e7a37a5ac981a59821191a7ebac44969593187203ffcffa4bc1a0ad6`. `proof/gau_wu_conformal_shape_phase.md`; `experiments/gau_wu_conformal_shape_phase.py`. |
| A293 | F/G | Replace A292's unexplained phase-paired spectrum by an exact dual Schur criterion and identify the finite Hardy polarization that should prove it | **EXACT CONDITIONAL EQUIVALENCE L345; COMPLEX DUAL RANGE IS NUMERICALLY UPPER-TRIANGULAR AND ENDPOINT-ISOTROPIC, PROOF OPEN** | Whenever the uneliminated joint scalar Hessian `J` is nondegenerate, phase covariance of the final Schur form for `S=[S_x;S_y]` is equivalent to `(J^−1S_y*+iJ^−1S_x*)^T J (J^−1S_y*+iJ^−1S_x*)=0`.  This dual lift's first Blaschke image has zero lower triangle in every complete model, and the right and left L336 endpoint forms vanish separately as complex bilinear forms on it.  Prove the upper-Schur-flag Euler response in L344's fixed port coordinates and polarize the exact endpoint identities there; do not infer a sign for either one-sided real form.  A tempting universal-diagonal-plus-rank-one inverse formula was falsified after removing its dominant soft component and is not a route.  Dataset SHA-256 `d02f3734280a2a5594fba11cdcc1514ad6b760c6b3fb112c1b76bf5b733ce8db`. `proof/gau_wu_conformal_dual_polarization.md`; `experiments/gau_wu_conformal_dual_polarization.py`. |
| A294 | F/G | Prove the lower-Schur-flag structure seen in A293 before attacking its Euler equation entry by entry | **SUCCEEDED AS L346; FULL LOWER RESPONSE COLLAPSES TO `n` DIAGONALS + ONE CYCLIC CORNER** | Differentiated functional-calculus commutation gives `[S,Y_1]=Y_0C−CY_0`, whose strict lower triangle is zero.  The southwest recurrence for an upper triangular nondegenerate Gau--Wu shift solves every lower entry from the unique repeated-eigenvalue corner `(L,0)`; adding the free diagonal gives exactly `n+1` generators.  Thus A293's flag target is no longer a matrix identity: prove only `diag Y_1(W)=0` and `Y_1(W)_(L0)=0`, then L346 closes the rest.  Do not expand lower entries separately.  Colliding interior zeros require a later confluent chart.  Dataset SHA-256 `537bb48cebeb8c79ff3ba84df566f97a6fe01b5efeb1cd56ce81f70ece7476f3`. `proof/gau_wu_lower_flag_recurrence.md`; `experiments/gau_wu_lower_flag_recurrence.py`. |
| A295 | F/G | Identify L346's cyclic corner directly in L343's polynomial shape coordinate before attacking the remaining Euler equations | **SUCCEEDED AS L347; CORNER IS EXACTLY THE TOP NEGATIVE WEIGHTED SUPPORT MODE** | Expanding `sigma_E=(1/4)f_zeta*(conj(zeta)Ccal*E Ccal+zeta Ccal*E*Ccal)f_zeta` shows `hat(sigma_E)(−n)=(Ccal*E Ccal)_(L0)/4`.  The frame endpoints convert this to `<q,Ep>`, while analytic Riemann and zero-motion corrections cannot enter the southwest corner; consequently `(Y_1)_(L0)=4f'(0)hat(sigma_E)(−n)/gamma`.  Continue by deriving vanishing of this last negative Hardy coefficient from the L345 Euler equation in L344's fixed port.  Do not attempt to cancel it with zero motion: L347 proves that impossible.  Dataset SHA-256 `a4bad7eb40ff2aa41d65c8b7cee515acb4f8ae21b1ea4b0caa3eb353f8646829`. `proof/gau_wu_cyclic_corner_shape.md`; `experiments/gau_wu_cyclic_corner_shape.py`. |
| A296 | F/G | Replace L346's remaining diagonal entries by invariant spectral/root coordinates after isolating the cyclic corner | **SUCCEEDED AS L348; DIAGONALS REDUCE TO ROOT TRACKING + ONE ENDPOINT SPLIT ON THE CYCLIC-FREE FACE** | The southwest commutator gauge separates an arbitrary first operator response into an upper-triangular part, one cyclic matrix unit, and a similarity commutator.  Spectral traces and functional-calculus equivariance then express every interior diagonal as `f'(b_j)(mu_j−v_j)` plus a fixed multiple of the cyclic corner, and express the endpoint sum as `2f'(0)(mu_0−v_0)` plus another fixed cyclic multiple.  Thus, after L347 is closed, prove only simple-root tracking, mean repeated-root tracking, and the endpoint split; do not use the false raw-diagonal formula before removing the cyclic path.  Dataset SHA-256 `d87d5cc883aefe7ad77ab0a7c4ec81c8e8643d9779dfa2c4160b4c2e3875a04c`. `proof/gau_wu_root_tracking_normal_form.md`; `experiments/gau_wu_root_tracking_normal_form.py`. |
| A297 | F/G | Use a degree-preserving inner symmetry to close one of L348's Euler equations without expanding the fixed port | **SUCCEEDED AS L349; POSTCOMPOSITION KILLS THE ENDPOINT TRACE EXACTLY, SPLIT CLOSED BY A298/L350** | Postcomposing `f` with `beta_(tw)` gives a canonical pure zero tangent with first image `−wI`.  Polarizing the sharp singular-value Hessian shows its pairing with any joint direction is exactly `(3/4)Re(conj(w)((Y_1)_(00)+(Y_1)_(LL)))`.  L345's source is zero on this tangent, so the endpoint sum vanishes on `W_phi`; A298/L350 then proves the two entries agree, hence both vanish.  After the cyclic equation is closed, L348's mean repeated-root tracking follows automatically.  Continue only on the cyclic equation and the simple-root equations.  Dataset SHA-256 `e572460e3f284788bcf5a121e93e49aaa0fb7f0439fa7e2d02c232fde483d1d4`. `proof/gau_wu_postcomposition_euler.md`; `experiments/gau_wu_postcomposition_euler.py`. |
| A298 | F/G | Use the canonical model-space conjugation before expanding a disk-fibre Riesz representative for L348's endpoint difference | **SUCCEEDED AS L350; CONJUGATION CLOSES THE ENDPOINT SPLIT AND L349 KILLS BOTH ENDPOINTS** | The canonical antiunitary exchanges the two singular endpoint vectors and fixes the Gau--Wu operator under `T -> C T* C`.  That reflection preserves both the full sharp objective and every boundary-shape coefficient, so it fixes `J_phi^−1S_phi*` exactly.  The corresponding first Blaschke response is complex symmetric under the endpoint exchange, forcing its two endpoint diagonal entries to agree; L349 says their sum is zero.  This supersedes the tentative numerical observation that the endpoint-difference Riesz direction lies in L343's disk fibre and avoids an unnecessary pseudoinverse representative.  Continue only on the cyclic coefficient and the `n−2` simple-root equations.  Dataset SHA-256 `8e317c98ea8216aae05b979cff0fda8b6766b25980e19b9c3e39e34ee96fed8b`. `proof/gau_wu_canonical_conjugation.md`; `experiments/gau_wu_canonical_conjugation.py`. |
| A299 | F/G | After removing the first soft shape pivot, dominate the remainder by a fixed multiple of the inverse model-kernel coefficient Gram | **EVERY COEFFICIENT AT LEAST ONE FALSE NUMERICALLY ON SEPARATED RATIONAL `n=4` WITNESSES** | Moderate roots `3/8+i/4,−1/2−i/4` give eigenvalues `−0.11585777,1.17891488` for `S_phi−3D G_g^−1D`.  More decisively, boundary roots `−41/64−47i/64,−34/64−52i/64`, still separated by `0.1344`, give `−275.39428827,246481.1030077` already for `S_phi−D G_g^−1D`; positivity of the kernel Gram then rejects every coefficient at least one.  Both complete shape matrices remain positive definite, both first pivots match `16|b_1b_2|²`, and 512--2048-node relative drift is below `4.1e−10`.  Thus no complete copy of this isolated kernel Gram can be detached from L342's coupled port/disk elimination.  This does not refute phase covariance, the shape sign, or Crouzeix.  Dataset SHA-256 `41352e20a5af43b1d4d1d3d6bfcb6ad193e055abfbb93c902928be08f2cd9d7a`. `proof/gau_wu_kernel_gram_falsification.md`; `experiments/gau_wu_kernel_gram_falsification.py`. |
| A300 | F/G | Combine the conformal shape and root-tracking sectors before attacking L345's remaining Euler equations one at a time | **EXACT ACTIVE CHART L351; FULL HARDY--LAGRANGIAN IDENTITY SURVIVES 12 MODELS, PROOF OPEN** | Shape plus endpoint/simple-root tracking has exact real rank `4n−4`, with only L343's `(n−2)^2` disk graph in its kernel.  In the Hardy orientation `P=[S_y+iS_x;tau]`, all three pure complex-bilinear inverse-Hessian blocks vanish numerically: `P J_phi^−1P^T=0`.  The shape block is A292 phase covariance, the cross block is exactly L347--L350's cyclic/root-tracking debt, and the tracking block is its reciprocal polarization.  The shape-constrained optimized tracking mismatch is also purely antiholomorphic.  Treat this as one coupled L342/L338 Lagrangian theorem, not three proved identities; no sign or phase claim is promoted.  Dataset SHA-256 `7f00bc8167dbbf27fc96a64b3bf7cddc28c8e4c9aeb66dbb80021b0c4d35d0ac`. `proof/gau_wu_shape_tracking_lagrangian.md`; `experiments/gau_wu_shape_tracking_lagrangian.py`. |
| A301 | F/G | Locate the exact complex structure in L342's two residual squares before trying to prove L351's full active isotropy | **SUCCEEDED AS L352; SIGNED RESIDUAL COVARIANCE EXACT, COMMON PHYSICAL TERM NOT CONTROLLED** | The homogeneous metric/zero residual map obeys `R_0(ix,−iv)=(−ir_-,ir_+)` exactly.  This follows from canonical conjugation of the selfadjoint truncated-Toeplitz metric extension and L338's antilinear model conjugation.  Both endpoint weights preserve the signed phase, while the metric and zero ranges are weighted-orthogonal complementary halves.  This makes A300's residual structure exact, but not its full inverse-Hessian isotropy: the square-gap identity leaves one common pure physical quadratic coefficient in `F` and `4J`.  Prove both the mixed physical-forcing intertwiner and a type-`(1,1)` formula for that common L344 port term; neither may be silently inferred from L352.  Dataset SHA-256 `4b300f489b23ad51830014139640c1d403fc598f4a191bdb16d0751d8dc0e471`. `proof/gau_wu_residual_complex_covariance.md`; `experiments/gau_wu_residual_complex_covariance.py`. |
| A302 | F/G | Before factoring L340's boundary loss, determine whether the second inverse-Riemann support curvature is genuinely part of the output | **SUCCEEDED AS L353; SECOND SUPPORT CANCELS EXACTLY, PURE FIRST-JET FACTORIZATION OPEN** | The inverse-Riemann second operator collapses to `−DK_s(A)[G]+K_(a²/2)(A)−K_t(A)`.  L341's paired frame expansion and L335--L336 show that the `−K_t` contribution to the endpoint form is exactly `2P_phi`, so it cancels identically from `G_phi=2I+2P_phi−H_phys`.  The loss is therefore the first-order form `2I−B_1−Xi`, where `B_1` is the first conformal-curvature trace and `Xi` keeps both first frame responses coupled.  Factor this form before proving its upper domination by `2I+2P_phi`; do not carry the second support into the factor, omit the load-bearing `B_1`, or replace the full loss by the indefinite frame-only term `−Xi`.  Dataset SHA-256 `15507d65397ecf1e0845c5d7831eb5ebabfd802b16b45e8234e592802546a78e`. `proof/gau_wu_first_jet_loss.md`; `experiments/gau_wu_first_jet_loss.py`. |
| A303 | F/G | Quotient L352's mixed endpoint residual relation by canonical conjugation before expanding the fixed port | **SUCCEEDED AS EXACT L354 HOMOGENEOUS NORMAL FORM; PHYSICAL HARDY FACTOR OPEN** | The canonical endpoint restriction `Gamma` puts pure metric residuals on the graph `r_+=−Gamma r_-` and pure zero residuals on the opposite graph.  L351's tracking isomorphism supplies the explicit zero corrector `T_phi bar(tau)`, leaving `Delta_phi=r_++Gamma r_-−T_phi bar(tau)` independent of all metric and zero variables.  Its endpoint-trace coordinate vanishes, so only `n−2` complex physical outputs survive.  The full mixed signed intertwiner is now exactly equivalent to the complex-linear Hardy factorization `Delta_phi=B_phi a`.  Twelve complete models fit this factor and its disk-fibre annihilation at machine precision with rank `2n−4`, but those physical identities remain numerical and must be proved from L344 rather than inferred from row-space data.  Dataset SHA-256 `9d3cadeb5deeb464b707c5fc8171f80b1bbe5296360d265a0b96cacd7fe46f02`. `proof/gau_wu_canonical_residual_normal_form.md`; `experiments/gau_wu_canonical_residual_normal_form.py`. |
| A304 | F/G | Eliminate L342's metric row directly in L354's canonical endpoint graph before doing any further fixed-port expansion | **SUCCEEDED AS L355 EXACT PARALLEL-SUM SQUARE; PHYSICAL INTEGRATION CONSTANT OPEN** | Canonical conjugation intertwines the endpoint weights, so the metric graph and its translate complete by the elementary identity `||y||²+||w−y||²=2||y−w/2||²+||w||²/2`.  This gives the full joint formula `e_phi(C)−4J_C(tau)=(1/2)||T_phi bar(tau)+Delta_phi(C)||_(D_+)²` and the unique optimizer `bar(tau_opt)=−T_phi^−1Delta_phi(C)`.  Thus the metric and zero Schur eliminations are finished in fixed complex coordinates, and the fixed-physical zero-only antiholomorphic optimizer is exactly equivalent to A303's remaining complex-linear physical factor.  The common physical form `e_phi`, its type-`(1,1)` property, and its sign are not supplied.  Dataset SHA-256 `2c99ea653b2b5ddf671db159f115d4e07ac42ca12b1209df429154e5a2797e1c`. `proof/gau_wu_residual_parallel_sum.md`; `experiments/gau_wu_residual_parallel_sum.py`. |

## Known dead ends / pitfalls (from literature — do not repeat)
1. **Improving the abstract C–P lemma alone.** Ransford–Schwenninger: sharp at 1+√2 abstractly. Any improvement MUST inject concrete structure of (A, W(A), Cauchy transform).
2. **Invalid degree reduction via Cayley–Hamilton.** Replacing p by its remainder mod char poly changes max over W(A) (can increase it), so "wlog deg p < n" is FALSE for the sup side. Valid only as: ‖p(A)‖ computable from remainder.
3. **Pointwise-to-operator-norm leaps.** Scalar estimates on W(A) do not transfer to ‖·‖ for nonnormal A.
4. **Proving the completely-bounded version as a shortcut.** Possibly strictly harder; a cb-proof failure says nothing about scalar version.
5. **Von Neumann on a disk containing W(A).** Gives constant depending on disk vs W(A) geometry; radius arguments alone max out well above 2 for eccentric shapes.


## Methodological pitfalls discovered (Track G)
- P1. Offset-curve construction silently breaks on matrices with nonsmooth W(M) (near-normal, corners/flats): FFT ringing → garbage integrals that still satisfy holomorphic-identity checks. FIX: certify positivity of P(σ) pointwise + convex tangent winding + mass 2I + Cauchy reconstruction; reject otherwise. COVERAGE GAP: such matrices currently unsearchable — needs smoothed support-function domains.
- P2. Background process hygiene: stale processes with old code contaminated logs → phantom "violations". FIX: harness-tracked jobs only; verify claims by re-evaluating saved records with current code.
- P3. Catastrophic cancellation: optimizer drives poly coefficients to ~1e50 with boundary cancellation; consistency certificates fooled (both sides share garbage). FIX: theorem guards — computed ‖p(M)‖ > 1+√2 or ‖γ_Φ(p)x₀‖ > 2 under certified hypotheses PROVES numerics lie (they contradict established theorems) → reject. Plus coefficient-magnitude cap.
- P4. Near-singular layer-potential quadrature (outer→inner contour, gap ε/2): N=1024 insufficient; passed all other certificates while Φ values were wrong (fake viol +0.358 at n=3, killed at N=4096). FIX: unitality certificate Φ(1)=1 through the same kernel + N-vs-2N stability check.
- P5. **Family-restricted optimizers miss the global phase** (found 2026-07-21): scalar α-searches
  inside one Blaschke family (e.g. odd {0,±α}) can converge to non-global stationary points —
  the true extremal may be in another phase (even {±α}, or Möbius = odd family's α→1 endpoint).
  FIX: always cross-check with the unrestricted best_extremal solver (deg 1..n−1, multistart);
  boundary-hugging zeros (|z| ≈ 1) in its output are removable (unimodular factors), not real.
  Related DATA BUG: sym4_sweep_s61.jsonl OTHER-branch records have broken taus (τ₂ ≡ 0) and
  inconsistent f0e — recompute; the ρ/K/qs columns are fine.
- P6. **Inadmissible probe families in map-class falsification** (found 2026-07-21): test maps
  built from tanh/step smoothings can have poles INSIDE the disk (tanh(κ(z²−t₀²)) poles at
  z² = t₀²+iπ/(2κ)) — huge "violations" from non-analytic non-univalent junk. FIX: before
  trusting any V > 1, verify analyticity (pole locations) and univalence (Re G' > 0
  Noshiro–Warschawski, or boundary-curve simplicity). Entire (polynomial) smoothings +
  explicit Re G' bounds give certified counterexamples (D2_landscape.md).
- P7. **Certification capability gap at n ≥ 6 (dense)** (found 2026-07-21): the true-extremal
  criterion diag = |⟨f₀(A)x₀,x₀⟩| < 1e-5 is currently UNREACHABLE for arbitrary dense 6×6
  matrices — both find_extremal_blaschke and best_extremal stall at diag ~1e-3 on the deg-5
  Blaschke landscape (10 params), so any acceptance-gated loop (e.g. Hr_adversarial's uncapped
  `while cur is None`) spins forever at 100% CPU with zero output (n=6 s42 job killed after
  2h04m / ~500 silent rejections). FIXES: (i) cap rejection loops + log every rejection with
  (K, diag); (ii) at n ≥ 6 use STRUCTURED families (zero-diag tridiagonal: graded collapse
  reduces extremality to small blocks — certifiable, and they are the G–O-hard configurations
  anyway); (iii) or upgrade the solver (analytic gradients / phase-aware polish) before any
  dense-n≥6 campaign.
- P8. **A stronger sufficient target needs its own falsification gate.** L20/L21 prove a
  complete-bounded similarity statement, and L44 is stronger still than the exact small-gain
  inequality. Before investing in a sharp-edge certificate, test the similarity constant on
  general matrices and audit L44 at high precision in its equality corner. Failure of either
  stronger target is not evidence against scalar Crouzeix.
- P9. **Always residual-check discrete Lyapunov solves.** SciPy's automatic real bilinear
  branch returned a relative equation error of order `1e-1` on a stable nonnormal `12 x 12`
  partial isometry, while the complex bilinear and direct branches had residuals below
  `1e-15`. The shared Stein solver now checks the defining equation and retries in complex
  arithmetic. Never trust a Lyapunov output solely because the input spectral radius is below
  one.
- P10. **Cheap exact falsifiers before structural proof routes.**  L308 and L309 together
  consumed roughly four and a half hours on raw and prepared odd-response parity before the
  same scalar-copy colligation killed both:
  `S=[[0,3/5,-4/5],[0,4/5,3/5],[0,0,0]]`,
  `V=e1`, `W=e3`, `B_1=-4/5`.  It gives the exact nonzero traces
  `33264/15625` and `66384/15625`.  Before opening any new parity,
  vanishing, or sign proof route in the repeated endpoint sector,
  test it first against this witness and every other banked small
  exact witness.  Cheap refutation first; structural proof second.
- P11. **Do not detach a fixed model-kernel multiple from L342's coupled
  residual.**  A299 first falsifies
  `S_phi >= 3D G_g^−1D` at moderate roots, then falsifies even
  `S_phi >= D G_g^−1D` at separated boundary roots.  The complete
  shape matrices remain positive; only the isolated-kernel
  comparisons fail.  Test every proposed Gau--Wu Gram against both
  witnesses and keep both endpoint residual squares coupled to the
  fixed port and disk-fibre elimination.
- P12. **Do not drop L353's first conformal-curvature reserve.**
  The tempting frame-only candidate `G_phi=−Xi` has a negative
  eigenvalue between `−2.95` and `−4` in every standard model through
  dimension eight.  The full first-jet loss is positive only after
  retaining the positive `2I−B_1` piece.  Also do not carry the
  second-support curvature into the factor: L353 proves that it
  cancels exactly against `2P_phi`.  Factor `2I−B_1−Xi` as one
  coupled object before attempting port domination.
- **Meta-pattern (important):** every relaxation of extremality so far admits sharp counterconfigurations: abstract lemma (R–S, non-unital α), domain-only constants (MMOR, thin quadrilaterals), scalar localization (L10, odd-symmetric mechanism — ours), free-map S ≤ 1, soft D2 classes (convex; odd+G'↑). Conjecture-strength inequalities must engage TRUE extremal/critical structure (global maximality of f₀ + positive extremal measure + critical domain), not just its first-order shadows.
