# APPROACH_LEDGER.md

Every attempted approach; why it succeeded/failed. Includes known-failed approaches from literature (do not repeat blindly).

## Known dead ends / pitfalls (from literature — do not repeat)
1. **Improving the abstract C–P lemma alone.** Ransford–Schwenninger: sharp at 1+√2 abstractly. Any improvement MUST inject concrete structure of (A, W(A), Cauchy transform).
2. **Invalid degree reduction via Cayley–Hamilton.** Replacing p by its remainder mod char poly changes max over W(A) (can increase it), so "wlog deg p < n" is FALSE for the sup side. Valid only as: ‖p(A)‖ computable from remainder.
3. **Pointwise-to-operator-norm leaps.** Scalar estimates on W(A) do not transfer to ‖·‖ for nonnormal A.
4. **Proving the completely-bounded version as a shortcut.** Possibly strictly harder; a cb-proof failure says nothing about scalar version.
5. **Von Neumann on a disk containing W(A).** Gives constant depending on disk vs W(A) geometry; radius arguments alone max out well above 2 for eccentric shapes.

## Campaign attempts
| ID | Track | Idea | Status | Outcome / failure condition |
|---|---|---|---|---|
| A1 | A | Reconstruct C–P line-by-line; locate constant entry | DONE | Constant enters as c ≤ 1 in K² ≤ 2dK + c (S–dV Thm 5). Full dissection in proof/track_A_crouzeix_palencia.md |
| C1 | C | Randomized + gradient search max R, dims 3–5, poly deg ≤ 6 | DONE (round 1) | Max R_inner ≈ 1.9747 (n=3,d=3). No violation. Optimizer stalls below 2 at nonsmooth points; landscape matches G–O |
| A2 | A | (⋆): c ≤ 2L with L = ∮λ_min(P)ds (CGL slack) | ABANDONED — WRONG SHAPE | Not ⊕-stable: M⊕(normal junk) sends L→0, keeps c. Global operator quantities cannot work. Lesson → localized quantities |
| A3 | A | L6 refined master inequality (vector-localized) + L7′ target | SUPERSEDED (→ P1/P2 → H-r) | L6 proved. Chain: L7′ → L7@ext → P1/P2 (audit) → H-r (SV24 §6 anchor) — current central target |
| C2 | C | Adversarial optimization of L7′ violation over (M,p,x₀) | DONE (no violation) | All positives were artifacts (P1–P4); machinery reused for H-r searches |
| A4 | A/B | H-r reduction: ρ ≥ 0 at extremal pairs on Ω = int W(A) ⟹ Crouzeix (SV24 Thm 6.1 + shrinking) | ACTIVE — CENTRAL | proof/strategy_S.md, rho_positivity_program.md |
| A5 | B | 2×2 closed form ρ = 1 − π/(2K(m)) on confocal ellipses | PROVED (mod α=0 write-up) | rho_2x2_theorem.md; K>1 hypothesis essential |
| A6 | B | sym3 ζ=z² collapse → Landen theorem ρ = 1 − π/(2K(k₁)) | PROVED | landen_theorem.md; collapse ≡ Landen transformation |
| A7 | B | Level-4 nodal/Pick closed form (L15) + phases + even-phase structure (L16/L18) | PARTIAL | `slice_closed_form.md`; even-sector midpoint globality and EL4 are proved. Parity/symmetry-breaking classification and odd/Möbius positivity remain |
| A12 | B | Globality of the even midpoint over the full two-node Pick body | PROVED | Boundary data reduce to constants or disk automorphisms; exact determinant shows the automorphism orbit is uniquely maximized at its hyperbolic midpoint when K>1. `proof/even_pick_globality.md` |
| A13 | B/G | Assume Z₂ symmetry forces a parity extremal | DEAD — FALSE | Shifted degree-one Möbius phase on slice `(.8,2.4,1.1,.3)` beats both parity sectors. Must include symmetry breaking explicitly; matches the phenomenon in Kenan Li's sym3 phase diagram |
| A14 | F | Complete-2 similarity SDP for the elliptic slice | **PROVED — COMPUTER-ASSISTED (L20/L59)** | L21 dualizes the optimum exactly as `sup tr(Z−TZT*)_-/tr(Z−TZT*)_+`; L22 reduces every slice matrix to three parameters and 2×2 parity blocks. The downstream KKT/transfer chain L27--L59 proves the trace ratio ≤4 uniformly. `proof/slice_similarity_duality.md` |
| A15 | F | Decompose L20 into modal-block and parameter-boundary theorems | **PROVED AS PART OF L59** | L23 gives the sharp nome bound; L24/L26 prove `||C||,||B||≤2`; L25 proves the sharp `c→0` face. The genuinely coupled witnesses are subsequently closed by A16/A18/A22. `proof/slice_boundary_theorems.md`, `proof/slice_upper_block_theorem.md` |
| A16 | F | Eliminate the contraction LMIs with Stein defects and classify coupled KKT rank faces | **PROVED AS PART OF L59** | L27 parameterizes every contraction metric by two PSD 2×2 defects through a Szegő kernel. A17 closes rank-one/full. L40 reduces rank-one/rank-one to an explicit two-parameter `2×2` Blaschke--Potapov transfer, which A18/A22 now prove norm ≤2. `proof/slice_coupled_defects.md` |
| A17 | F/B | Convert the rank-one/full coupled face to an odd two-node Blaschke block | **PROVED (2026-07-21)** | L28 identifies the face with an odd Blaschke block and proves the lower block. L30–L32 reduce the upper block to two endpoint residuals; L33 and L36–L39 now certify every nome, including the singular multiscale ridge. Hence L29 and the generic rank-one/full KKT face are closed. `proof/slice_odd_block_reduction.md` |
| A18 | F/B | Convert rank-one/rank-one to a matrix-valued transfer | **PROVED — L40--L59** | Two spectral Gram rotations give `R_{a,b}(T)=−S⁻¹(T−A)(I−AT)⁻¹S`. Its modal node function is `2×2` rational inner. L41 converts the norm test to a quartic polynomial block LMI; L42 closes high nome; L47--L58 factor every singular face and reduce the interior to projective charts; L59 certifies the last interval. `proof/slice_coupled_defects.md`; `proof/slice_core_projective_reduction.md` |
| A20 | F/B | Couple the two parity-block norms through their common modal orientation | **PROVED MILESTONE (L47/L48)** | Exact traces/determinants reduce `||B||||C||≤2` to one rational scalar inequality. Elementary theta bounds and four blow-up charts give a 13-second exact integer Bernstein certificate. Orthogonal factorization then proves (RT) on `a=0` and `b=0`. This did not by itself control `ab≠0`, but supplied the first sharp coupled invariant beyond the separate L24/L26 bounds; L59 later closes the interior. `proof/slice_coupled_defects.md` §6.3; `experiments/slice_block_product_certificate.py` |
| A21 | F/B | Resolve the zero-node transfer ridge with a sharper nome estimate | **PROVED BOUNDARY — L49/L50** | Jacobi's first two product factors give `k≤4c/(1+4c²)` for `c≤1/2` by a 24-coefficient exact Bernstein check. On `p=0`, BE reduces to one scalar inequality. The high half follows from `k≤1`; on the low half, ridge-centered and largest-coordinate blow-up charts give a finite exact rational Bernstein cover. Thus BE/RT holds on the complete zero-node face. The centered ridge guided the later interior certificate closed by L59. `proof/slice_coupled_defects.md` §6.4; `experiments/slice_sharp_nome_certificate.py` |
| A22 | F/B | Prove the full polynomial transfer core by projective principal-minor charts | **PROVED — L51--L59** | L54--L57 cover both signs through the previous compact frontiers. L58 factors the sharp positive edge. L59 retains the complete deficit/nome/spatial correlation and certifies ten exact boxes from `c+` through `.63`, where it overlaps L42. The forced-regeneration log covers both determinant and all three minor charts with no tolerance. `proof/slice_core_projective_reduction.md`; `experiments/positive_tail_full_20260722.log` |
| A19 | F/G | Test L21's contraction-similarity route on general matrices before further slice certification | **NUMERICALLY SURVIVES; SCALAR-SHIFT SHORTCUT CLOSED** | The initial certificate-gated sweep accepted 102/120 varied matrices (`n=3..8`) with no `t*>4`. A new repeated-Crabb equality-locus gate accepts 118/120 genuinely perturbed cases; max `3.999844312562`, with every sampled support gap positive. The two strongest cross directions remain below four through `delta=1e-5` and show stable first-order drops `(4-t*)/delta≈1.557,1.637`. Exact repeated-block calibrations resolve the `~1e-7` SDP noise floor. Hartz–McCarthy scalar-shift search is an exact restatement, not a CP bridge; an order-three Toeplitz SDP numerically excludes every positive-state scalarization on one dense 3×3 case. L21 remains credible finite evidence for a potentially stronger cb statement. `proof/general_similarity_probe.md` |
| A23 | F/G | Linearize the Riemann map and L21 metric SDP at repeated Crabb equality blocks | **LOCAL FIRST-ORDER SIGN PROVED (L61)** | Nilpotence leaves only `p−1` support Fourier coefficients. The tangent SDP dual collapses to one density matrix and its optimum is `−4` times a Jensen gap, hence nonpositive for every perturbation. A strict feasible-metric lift gives a nonpositive upper Dini derivative under uniform conformal expansion. Two solvers reproduce all 12 nonlinear slopes to relative error `≤1.20e-4`. Next classify Jensen equality and compute second order there; also close nonsmooth map regularity. `proof/general_similarity_tangent.md` |
| A24 | F/G | Resolve the zero-Jensen-gap face at second order | **FINITE REDUCTION PROVED (L62); SINGLE-BLOCK SIGN CLOSED BY L65** | Second Hermitian eigenvalue perturbation, the normal-angle correction, and a Schwarz integral give finite `G,H`; first-order complementarity then reduces all second-order PSD conditions to three affine block LMIs in `X,Y,e`. An independent five-point conformal fit, two resolutions, and two solvers reproduce 12 nonlinear quadratic coefficients. L63--L65 subsequently prove the sign for every single Crabb size; repeated common-maximizer faces are treated separately by L74--L76. `proof/general_similarity_second_order.md` |
| A25 | F/G | Eliminate the single-Crabb second-order SDP in closed form | **PROVED IN EVERY DIMENSION (L63–L65)** | L63–L64 give exact all-coordinate square identities at `p=3,4`. L65 uses circle grades and the path Green function to treat arbitrary `p`: each nonzero grade collapses to `−t*ᵊ_{r,k}t`, where `ᵊ^{-1}=(I−H_r)/4+c_kqq*` for `k≥3`, and the two low modes are PSD limits of the same formula. The grade-zero perturbation is an exact weighted shift and stays within four by the numerical-radius power inequality. The resulting form is NSD of rank `p(p−2)`. Next classify its large equality space and extend the common-maximizer calculation to repeated blocks. `proof/general_crabb_second_variation.md`. |
| A26 | F/G | Quotient the L65 equality kernel before computing higher order | **PROVED LINEAR CLASSIFICATION (L66)** | The raw kernel has dimension `p(p+2)`, but unitary conjugation, translation, and real scaling span an exact constant-value orbit of dimension `p²+2`. The genuine quotient is only `2p−2` dimensional. Circle grades reduce it further to one complex direction in mode 1, two in mode 2, and one in each mode `3,...,p−2` (with the `p=3` endpoint exception). Full-form/orbit reconstruction for `p=3..8` confirms every dimension and annihilation identity to `9.76e-14`. Next derive the first nonzero higher-order term only on these representatives. `proof/crabb_second_order_equality.md`. |
| A27 | F/G | Resolve the higher-order `p=3` equality quotient with formal Riemann and Stein series | **FULL LOCAL THEOREM CLOSED BY L73** | Exact normal-angle reparameterization and rank-one Stein Gramians give `4−171ε⁶/4096+O(ε⁷)` on the pure residual mode-one ray. On straight rays with mode-two coordinate `ω≠0`, the quartic is `−4|ω|⁴−31|ζ|²|ω|²/8`. L69--L72 identify and control the weighted disk center; L73 turns the resulting analytic normal form into a full complete-`2` neighbourhood. `proof/p3_crabb_higher_order.md`; `proof/p3_crabb_local_theorem.md`. |
| A28 | F/G | Build the exact `p=3` affine-unitary normal slice and couple its negative directions to the residual quotient | **EXACT SLICE PROVED (L69); FULL SIGN CLOSED BY L73** | The orbit-normal slice is the seven-real-parameter matrix `E(z,w,s,v)` in L69. L63 restricts to `−8s²−21|v|²/4`, so `(z,w)` are the complete flat coordinates and `(s,v)` the complete negative coordinates. L70--L72 resolve the weighted center and L73 proves the certificate nonpositive on a full slice neighbourhood. `proof/p3_crabb_local_slice.md`; `proof/p3_crabb_local_theorem.md`. |
| A29 | F/G | Resolve the finite weighted `p=3` slice and identify its last center | **FULL LOCAL THEOREM PROVED (L73)** | Exact low-order invariant extraction makes the full weighted sixth-order certificate a sum of three negative squares. L71 identifies its unique recentered jet with an exact disk-matrix curve; L72 proves ambient stationarity. L73 analytically eliminates the defect and strong normals, and symmetry reduces the last sign to `−(25/56)|z|²|U|²−4|U|⁴` plus an absorbable remainder. `proof/p3_crabb_local_theorem.md`. |
| A30 | F/G | Replace indefinite higher-order center expansion by a Kippenhahn-locus calculation | **PROVED STRUCTURAL CENTER (L71)** | The characteristic discriminant and Kippenhahn polynomial share the analytic factor `81e⁴R²+(1152e²−4096)R+4096`. On its root through `R=1`, the Kippenhahn conic is a circle and its center is a double eigenvalue. The root series exactly matches the independently extracted center corrections. This converts the flat formal jet into the classical disk case and avoids an order-by-order grind. `proof/p3_crabb_disk_center.md`; `experiments/p3_crabb_disk_center.py`. |
| A31 | F/G | Prove that L71's disk locus is a true critical manifold of the rank-one certificate | **PROVED EXACT STATIONARITY (L72); USED IN L73** | The disk curve has canonical Schur form `T_l` and the Stein endpoint derivative is one explicit functional `4ℓ(Re G)`. A three-pole support residue identity shows the first Riemann correction has exactly the same functional, so every complex first variation cancels. L73 uses this to remove every term of soft degree below two. `proof/p3_disk_center_tangent.md`. |
| A32 | F/G | Convert L70--L72 into a uniform local theorem by analytic elimination and symmetry | **PROVED COMPLETE-2 NEIGHBOURHOOD (L73)** | Positive defect Hessian gives an analytic rank-one certificate. Negative strong Hessian eliminates `(s,V)`. The remaining equivariant analytic germ has no cubic term and only `|z|²|U|²,|U|⁴` at degree four, with exact negative coefficients; all higher monomials are small multiples of these. This closes the single `3×3` block but does not address repeated blocks or `p≥4`. `proof/p3_crabb_local_theorem.md`; `experiments/p3_disk_morse_bott.py`. |
| A33 | F/G | Classify L61's repeated-`C3` common-maximizer cross directions before attempting second order | **PROVED LINEAR QUOTIENT (L74); PURE-PAIR SIGN CLOSED BY L76** | For each selected/orthogonal copy pair, 14 independent support-Fourier equations leave 22 real dimensions; exact unitary mixing removes 16. The residual quotient is only three complex directions with sparse canonical `(X_j,Y_j)` representatives. For multiplicity `m` the cross quotient is `3(m−1)` complex dimensional. L75--L76 close one pure pair through second order; higher multiplicity and diagonal/cross mixtures remain. `proof/repeated_p3_common_maximizer.md`. |
| A34 | F/G | Eliminate the repeated-`C3` second boundary variation on the L74 quotient | **PROVED FINITE SUPPORT/CONFORMAL REDUCTION (L75); SIGN CLOSED BY L76** | Degenerate perturbation gives a diagonal `2×2` effective support matrix. Its maximum is one mode-0/2 trigonometric polynomial plus a single absolute cosine. The resulting boundary function is pi-periodic, so its first Fourier coefficient vanishes; `C3³=0` kills all remaining nonconstant modes. Hence `Tε=A0+εE−ε²κ̂(0)A0+o(ε²)`. L76 inserts the exact mean into a full second-order Stein certificate. `proof/repeated_p3_second_support.md`. |
| A35 | F/G | Prove the finite repeated-`C3` Stein sign and isolate its equality plane | **PROVED PURE-PAIR SECOND-ORDER SIGN (L76); FLAT PLANE CLOSED BY L77** | The first metric tangent and recursive second metric make all lower/Stein Schur complements exact. The upper endpoint is scalar and, after the L75 conformal correction, equals `−8|α1|²−4√2|d|/(3π)`. Thus no pure cross-pair direction increases at second order. Equality is exactly the two-complex-dimensional `(α0,α2)` plane, which L77 resolves at cubic order. `proof/repeated_p3_stein_sign.md`. |
| A36 | F/G | Resolve the L76 repeated-pair equality plane at the first nonzero order | **PROVED STRICT CUBIC DESCENT (L77)** | Third degenerate support perturbation is one off-diagonal scalar `c(q)`; the support branch is `|c|`, and parity kills the Fourier mode that could survive as `C3²`. Propagating the exact third Schur endpoints makes the similarity endpoint `−16 mean|c| I2`. The factorization of `c` proves strictness off the origin. Next combine cross directions across copy multiplicity and with the diagonal single-block slice. `proof/repeated_p3_third_sign.md`. |
| A37 | F/G | Extend the pure-pair cross sign to simultaneous star coupling at arbitrary multiplicity | **PROVED SECOND-ORDER STAR SIGN (L78)** | The effective support maximum loses pi-periodicity at multiplicity above two, so its first conformal mode had to be retained; exact propagation shows that mode cancels. The endpoint becomes `16(mean Q−mean λmax(Q) I)` minus a positive generator-1 Gram block. Matrix Jensen order proves NSD in every dimension. Higher-order equality and diagonal/internal-copy blocks remain. `proof/repeated_p3_star_second_sign.md`. |
| A38 | F/G | Classify equality in the arbitrary-multiplicity star theorem before expanding again | **CORRECTED: CERTIFICATE EQUALITY CLASSIFIED; OPTIMIZATION CLOSED BY L80** | The trace reversal `s(q)=tr R(−q)` makes the particular endpoint equality equivalent to `a1=0`; rank-one `span{a0,a2}` reduces to L77. For rank two, a nonlinear SDP probe falsified the proposed fourth-order route and exposed a better metric tangent, which A39/L80 now proves strict at second order. `proof/repeated_p3_star_second_sign.md` §5. |
| A39 | F/G | Find and prove the missing rank-two star metric tangent | **PROVED STRICT SECOND-ORDER DESCENT (L80)** | A one-parameter change in the free level-0/1 metric block has endpoint `diag(0,−5 adj(H)/72)` at zero and derivative `(5√2/3)diag(−tr K,K)`. Rank-two positivity leaves room in the orthogonal block while the selected entry immediately decreases, so a small positive parameter makes the full endpoint negative definite. This closes every nonzero pure star ray together with L77/L79. `proof/repeated_p3_star_second_sign.md` §6. |
| A40 | F/G | Test and prove the first diagonal--cross coupling on a strict common-maximizer face | **PROVED RADIAL-GAP MODEL (L81)** | Radially growing the selected copy and shrinking the others creates explicit first-order metric/Stein slack. Two free cross-metric entries then minimize independently and give the strict sum `−25||a0||²/8−8||a1||²−50||a2||²/9`. This survives arbitrary star multiplicity. The `d→0` transition is singular and must be coupled to L77/L80 rather than inferred uniformly. `proof/repeated_p3_radial_gap.md`. |
| A41 | F/G | Replace L81's radial gap by an arbitrary unique-winner support compression | **PROVED FIXED-DIRECTION REDUCTION (L82)** | A losing compression `B_-(q)≤0` with no common kernel has strictly negative mean, hence strict first-order tangent slack by L61 duality. Exact copy parity makes the selected second endpoint equal the L65 single-block term plus L81's universal star penalty; internal losing blocks never enter it. This includes nonuniform gaps touching zero. The remaining obstruction is the multiple-winner sector and uniform weighted transition as the mean gap closes. `proof/repeated_p3_unique_winner.md`. |
| A42 | F/G | Extend the tied-winner star calculation to every edge of the winner graph | **PROVED NONPOSITIVE SECOND-ORDER SIGN (L83)** | Pairwise L76 tangents on a complete graph yield `16(mean Q−mean lambda_max(Q)I)−8H1²`. Matrix Jensen order closes the sign for arbitrary winner multiplicity. The exact triangle audit caught a false shortcut: cycle means contain oriented mixed generator-0/2 paths and cannot be written as a naive weighted sum of `Hj²`. L84 subsequently adds all relative diagonal residuals; graph equality remains. `proof/repeated_p3_winner_graph_sign.md`. |
| A43 | F/G | Classify and insert every tied diagonal support-kernel residual | **PROVED RELATIVE DIAGONAL/GRAPH SIGN (L84)** | The diagonal support kernel is rank 11: eight unitary-orbit directions plus a complex generator-zero loop and a real generator-one loop. Adding those loops to every graph vertex leaves L83's endpoint formula unchanged after placing the real loop on `diag(H1)`. This includes all loop/edge mixed paths. The common single-block motion is not gauge; L85 subsequently couples it. `proof/repeated_p3_winner_diagonal_sign.md`. |
| A44 | F/G | Couple the common single-block motion to every graph edge and loop | **PROVED COMPLETE TIED-WINNER SIGN (L85)** | After global affine normalization only common modes `w(E10+E21)` and `vE20` are new. Keeping the first conformal Frechet term and normal-angle correction gives `16(Qbar−mean lambda_max(Q)I)−8H1²−21|v|²I/4`. The common flat `w` terms cancel exactly; omitting the Frechet term would give a false coefficient. Fixed-direction second-order sign is now complete on the entire tied winner sector. `proof/repeated_p3_complete_winner_sign.md`. |
| A45 | F/G | Merge an arbitrary strict losing sector into the complete L85 winner sector | **PROVED COMPLETE FIXED-DIRECTION SIGN THROUGH SECOND ORDER (L86)** | The two-winner/one-loser radial audit polarizes L81 into the negative Gram penalty `−25A0A0*/8−8A1A1*−50A2A2*/9`. Maximality of the winner space makes the losing mean compression strictly negative, so L82's tangent-Slater argument absorbs every losing internal block without changing the winner endpoint. Positive Jensen gap is strict at first order; its zero face is nonpositive at second order. Uniform higher-order gap collapse remains. `proof/repeated_p3_complete_fixed_direction.md`. |
| A46 | F/G | Intersect every negative term in L86 to isolate the true higher-order center | **PROVED EXACT EQUALITY REDUCTION (L87)** | Endpoint equality requires one vector simultaneously in the common top eigenspace of every `Q(q)`, `ker(H1)`, and `ker(Aj*)`, with common `v=0`. In its copy basis the selected branch has only generator-0/2 star data and common flat single-block modes. Generic graphs and all losing/gen-1/strong-mode directions are already strict and should not enter the next expansion. `proof/repeated_p3_equality_reduction.md`. |
| A47 | F/G | Replace every flat oriented edge/loop by one copy matrix and split normal from nonnormal data | **PROVED EXACT NORMAL-STRATUM THEOREM (L88)** | All flat data equal `Z* tensor X0+Z tensor Y0`, and their second support is the four-term polynomial in `Z,Z*`. Normal `Z` unitarily reduces the repeated matrix to a direct sum of blocks in L73's neighbourhood; domain monotonicity transfers the complete-2 bound to their convex-hull numerical range. Schur reduction leaves only a strictly upper, generator-0 nonnormal core. `proof/repeated_p3_flat_copy_matrix.md`. |
| A48 | F/G | Classify the first genuinely nonnormal Schur core (`3 x 3` copy space) | **PROVED COMPLETE NONNORMAL THREE-COPY SIGN (L89)** | For `N=[[0,a,b],[0,0,c],[0,0,0]]`, a common top branch exists only on `abc=0`. The `b=0,ac!=0` path is exactly L80's rank-two star; `ac=0` is square-zero and unitarily one L77 pair; all three nonzero gives strict Jensen gap. This supplies the base case for a path/star induction on Schur size. `proof/repeated_p3_flat_three_copy.md`. |
| A49 | F/G | Insert the common flat single-block mode into the copy-matrix support formula | **PROVED EXACT LAURENT PENCIL (L90)** | The common mode adds only a scalar `r_w I` and the Hermitian linear pencil `ell_w Z+conj(ell_w)Z*` to L88's `Q_Z`. This exposes every Schur-diagonal/common-mode interaction as finitely many ordinary matrix coefficients and removes further conformal fitting from the second-level equality classification. `proof/repeated_p3_flat_common_support.md`. |
| A50 | F/G | Use the outer Laurent modes to classify equality when the common flat mode is nonzero | **PROVED RIGIDITY (L91)** | Modes `±3` isolate `Z` and `Z*`; a common top vector must reduce `Z`. Thus every irreducible nonnormal block with `w!=0` is already strict, while equality blocks are one-dimensional L73 cases. Only the weighted `w->0` transition and the `w=0` nonnormal Schur core remain. `proof/repeated_p3_common_mode_rigidity.md`. |
| A51 | F/G | Close the full two-copy nonnormal core, including opposite diagonal loops | **PROVED STRICT CUBIC TRACE-ZERO THEOREM (L92)** | Nonnormal two-copy equality at `w=0` forces `tr Z=0`; then the second support is scalar. Its exact third support is traceless and nonzero, and the shared full third-metric propagation gives `−16 mean lambda_max(Q3)I`. Together with L88/L91 this closes every fixed two-copy flat direction and strictly extends L77 beyond zero diagonal. `proof/repeated_p3_flat_two_copy.md`. |
| A52 | F/G | Replace larger-copy Schur casework by a metric-kernel flag and classify its terminal blocks | **PROVED ARBITRARY-COPY FIXED-DIRECTION THEOREM (L93)** | The L80 free tangent extends to every copy projection and has an exact negative Gram compression independent of internal complement data. Its descending kernel flag either makes the endpoint strict or finds a reducing block. A terminal irreducible block satisfies `Z²=alpha I`, `ZZ*+Z*Z=beta I`; two anticommuting Hermitian symmetries reduce it to dimension at most two. L88/L92 close those blocks, eliminating any need for a `4 x 4`, `5 x 5`, ... Schur grind. Weighted uniformity across collapsing gaps remains. `proof/repeated_p3_flat_metric_flag.md`. |
| A53 | F/G | Quantify the cubic margin on L93's two-dimensional terminal block | **PROVED COERCIVE LOWER BOUND (L94)** | The uncancellable middle Fourier coefficient in L92's third support gives `m3≥|a|(2|a|²+4|d|²)/128`, hence endpoint at most `−|a|(|a|²+2|d|²)/4`. This is uniform off the exact normal face and is the first quantitative input for the weighted flag/terminal merger. `proof/repeated_p3_flat_two_copy.md`. |
| A54 | F/G | Quantify the second-order transition away from the trace-zero terminal face | **PROVED PAULI-VARIANCE BOUND (L95)** | Writing `Z=zI+D`, the non-scalar second support is a three-dimensional Pauli-vector curve `B+C(q)`. Its exact mean energy perpendicular to `B` factors as `|z|⁴a²(4|d|²+a²)`. The elementary triangle-defect identity converts this into a uniform negative canonical endpoint, strict exactly when both the trace split and nonnormal edge are present. Together L94/L95 quantify the cubic/second transition near the two-copy terminal block; common `w` and analytic remainders remain. `proof/repeated_p3_flat_two_copy.md`. |
| A55 | F/G | Quantify the common-mode transition on the trace-zero terminal face | **PROVED FOURIER COERCIVITY (L96)** | The common `w` pencil has zero mean and an upper-cross Fourier coefficient `sqrt(2)aw/16`. The canonical endpoint is therefore at most `−sqrt(2)|aw|`. Together L94--L96 now control trace split, common mode, and terminal cubic descent with a common zero only on the exact normal stratum. The coupled `z,w` cancellation regime and analytic remainder absorption remain. `proof/repeated_p3_flat_two_copy.md`. |
| A56 | F/G | Rule out cancellation between trace splitting and the common mode | **PROVED JOINT EVEN/ODD BOUND (L97)** | Pairing boundary points `q,-q` turns the trace support into the even vector and the common support into the odd vector; the average norm cannot fall below either component. A two-regime estimate combines L95 with L96 into one explicit endpoint bound, positive for every `a,w!=0` uniformly in `z`. The terminal leading margins now have no unexplored cancellation; analytic remainder/normal-manifold absorption is the remaining two-copy step. `proof/repeated_p3_flat_two_copy.md`. |
| A57 | F/G | Resolve every bounded second-order flat recentering of a terminal direction | **PROVED STRICT WEIGHTED MATRIX-JENSEN ENDPOINT (L98)** | The order-two support stays scalar. A tracezero tangent `D1` is absorbed by the differentiated canonical metric; retaining the full inverse-map Fréchet term and arbitrary first conformal mode then collapses the order-three endpoint to `16(Q3bar−mean top(Q3)I)`. A fixed Fourier coefficient handles `z=0`; for `z!=0`, the mean and mode-two coefficient have commutator proportional to `[D*,D]`, so no common top line exists. This closes all bounded weighted terminal charts away from the normal face. `proof/repeated_p3_flat_two_copy_weighted.md`. |
| A58 | F/G | Quantify the terminal cubic as the Schur edge tends to the normal face | **PROVED UNIFORM NORMAL-FACE COERCIVITY (L99)** | The diagonal third-support coefficient `3d³/128` complements L94's off-diagonal coefficient. A two-case estimate gives `m3≥(2|d|²+|a|²)^(3/2)/(192sqrt(3))`, so the unweighted cubic cannot collapse anywhere on the normalized trace-zero terminal sphere. This identifies the normal-face gain as convex-hull enlargement of opposite single-block domains. Weighted normal-manifold recentering still needs the final compactness patch. `proof/repeated_p3_flat_two_copy.md`. |
| A59 | F/G | Locate the sharp weighted center on the normal terminal face and test transverse stability | **PROVED EXACT CENTER + TRANSVERSE GAP (L100)** | Exact Laurent extraction gives the unique center `w=3conj(d)²/(8sqrt(2))`; the conjugation matters and an initial unchecked `d²` guess failed regeneration. At the center the traceless cubic support vanishes, as required by L88's exact normal manifold. The nonnormal cross coefficient is independent of every weighted coordinate and gives endpoint at most `−a(a²+2|d|²)/4`, so the center is linearly coercive transversely. A tubular analytic-remainder/scale-selection argument is still required before claiming a uniform neighborhood. `proof/repeated_p3_flat_two_copy_weighted.md`. |
| A60 | F/G | Remove the smooth-active-branch assumption from the repeated-block conformal tangent | **PROVED CROSSING-STABLE SHAPE DERIVATIVE (L101)** | A support perturbation of the disk first gives the same radial perturbation by the polar half-plane formula. Taking `log(Psi(z)/z)` turns that radial data into the Schwarz integral, proving the normalized Riemann-map tangent for every continuous profile. Repeated-block `lambda_max` profiles are uniformly Lipschitz and have uniform spectral perturbation remainders, so L61 no longer assumes a smooth support branch. The same radial lemma is available at recentered weighted scales; the remaining work is the finite normal-manifold remainder absorption, not conformal regularity at crossings. `proof/general_similarity_nonsmooth_tangent.md`. |
| A61 | F/G | Check whether a quartic copy split spoils L100's transverse cubic gap | **SUPPORT IDENTITY PROVED; METRIC SCOPE GAP EXPOSED (L102)** | The fourth Feshbach support coefficient, including the eigenvalue-dependent resolvent correction, is scalar at the weighted normal center and has zero full edge derivative. However, independent audit correctly noted that this does not control the simultaneous similarity metric: the default tight propagation has a nonzero linear transverse fourth endpoint `(25d³/8)[[0,1],[1,0]]`. Thus the support calculation is valid but its initial “no linear quartic competitor” interpretation was too strong; A62 supplies the missing metric choice. `proof/repeated_p3_flat_two_copy_weighted.md`. |
| A62 | F/G | Use the singular-metric freedom to eliminate L102's full fourth-endpoint competitor | **PROVED EXACT THIRD-METRIC CANCELLATION (L103)** | A general free third-metric block `aU` changes the transverse fourth endpoint to `(5d/8){5d²+8sqrt(2)(U01−U10)}[[0,1],[1,0]]`. The skew choice `U=5d²[[0,−1],[1,0]]/(16sqrt(2))` cancels it exactly; all retained third/fourth conformal coefficients cancel, and the normal endpoint remains scalar. Hence the total fourth remainder is quadratic in transverse distance after simultaneous selection and is absorbable by L100's cubic gap. The live terminal task is now genuinely order-five-and-higher uniform factorization, then the L93/L86 flag lift. `proof/repeated_p3_flat_two_copy_weighted.md`. |
| A63 | F/G | Determine whether a new traceless metric obstruction can recur at fifth or any later finite order | **PROVED UNIVERSAL ADJACENT-ORDER SURJECTIVITY (L104)** | A homogeneous free level-zero/level-one block at metric order `k` propagates as `P12=2P01`, preserves every active coefficient through that order, and changes order `k+1` by the same map found in L103. On a `J`-equivariant recursive branch, copy sign symmetry puts every linear edge coefficient in the off-diagonal Hermitian range of that map, so it can be removed at each finite order for fixed `d!=0`. Products recur at order `2k`, and the inverse grows like `1/|d|`; this rules out a new finite-order obstruction but does not prove convergence or a uniform tube. `proof/repeated_p3_flat_two_copy_weighted.md`. |
| A64 | F/G | Replace the order-by-order forced metric recursion by an exact nonlinear chart | **PROVED ANALYTIC LOWER/STEIN ELIMINATION (L105)** | Parameterize the whole level-zero/range cross block by `B`, make `P−I` tight by an exact Schur formula, and solve the tight Stein Schur complement for the range block `C`.  Its Jacobian at the repeated Crabb metric is triangular and invertible in every copy multiplicity.  The analytic IFT gives `P(T,B)` and leaves one exact upper endpoint.  Hence divergence of forced metric coefficients is not the terminal obstruction; only the uniform free-`B` selection and scalar sign remain. `proof/repeated_p3_exact_metric_chart.md`. |
| A65 | F/G | Avoid differentiating the nonsmooth numerical-range Riemann map in the transverse normal-tube comparison | **PROVED SAME-MAP/FROZEN-DOMAIN FACTORIZATION (L106)** | For the actual `f=phi_A`, every diagonal compression `Bj` has `W(Bj)⊂W(A)`, so L73's metric for `phi_j(Bj)` also contracts `f(Bj)` by disk-self-map functional calculus.  Keeping this same `f`, a fixed-contour resolvent series expands `f(N+E)−f(N)` with a uniform explicit factor `E`; `f` is only a bounded parameter.  This removes support-crossing regularity from the higher-order remainder problem.  A non-automorphic disk self-map may create Stein slack; A66/L107 embeds it exactly and A67/L108 safely removes it on the normal anchor. `proof/repeated_p3_frozen_domain.md`. |
| A66 | F/G | Embed L106's possibly Stein-slack normal certificate in the analytic metric chart | **PROVED PRESCRIBED-SLACK CHART (L107)** | Add the Stein Schur complement `H` as a nonnegative parameter to L105's implicit equation.  The Jacobian is unchanged.  After normalizing each L73 metric to minimum eigenvalue one, its lower Schur complement is zero and its Stein Schur complement is PSD, so the direct-sum frozen-domain metric is exactly `P(TN,BN,HN)`.  This is an exact parameterization, but freezing `HN` can lose the transverse upper bound; A67/L108 proves the safe zero-slack replacement on the normal anchor. `proof/repeated_p3_exact_metric_chart.md`. |
| A67 | F/G | Decide whether the inherited normal Stein slack should be frozen or removed before the transverse expansion | **ZERO-SLACK BRANCH PROVED SAFE AND SHARP (L108)** | Freezing all of `HN` preserves Stein feasibility but numerical exact-chart probes make the transverse upper endpoint positive; zero `H` matches the full SDP deficit.  Analytically, the normal chart decouples by copies and `D_H E=h22+2h11≥||H||` on each PSD slack block, so moving from `HN` to zero can only improve the scalar upper endpoint nearby.  L109 subsequently derives the complete weighted interpolation and confirms that only zero slack cancels the normal/transverse quadratic terms while retaining L100's cubic descent. `proof/repeated_p3_exact_metric_chart.md`; `experiments/repeated_p3_slack_transfer.py`. |
| A68 | F/G | Compute the normal tightening and transverse perturbation together instead of estimating either one separately | **JOINT CUBIC JET PROVED (L109)** | The actual inherited frozen-normal slack has a quadratic diagonal coefficient and a mixed cubic coefficient.  Exact propagation with slack fraction `theta` gives `E2(N)=−5(1−theta)a²I/8`, `E2(A)=5theta a²I/8`, and the common cubic `E3=−16(1−theta)m3I`.  Hence at zero slack the negative normal quadratic exactly pays for the positive transverse quadratic, while the actual endpoint retains `−delta(delta²+2r²)I/4`.  This explains why both freezing the slack and discarding the normal endpoint fail.  The remaining gate is now only the uniform transverse-factored remainder, not another finite coefficient. `proof/repeated_p3_exact_metric_chart.md`; `experiments/repeated_p3_slack_transfer.py`. |
| A69 | F/G | Turn L109's joint jet into a uniform exact terminal tube without differentiating the moving Riemann map | **TWO-COPY TERMINAL TUBE PROVED (L110)** | Compare the actual domain with the whole block-diagonal normal numerical range.  The terminal edge has zero top-support compression, so their support gap is `O(delta(r+delta))`; the inclusion-induced disk self-map is equally close to identity and creates a Stein slack of weighted degree two.  Subtract the inherited normal endpoint before expanding L105's analytic chart.  Every unretained monomial then contains either a transverse variable or that slack, giving `C delta(r+delta)^3`; L109 leaves the uniform negative cubic.  This closes all two-copy weighted ratios and routes the campaign to uniform arbitrary-copy metric-flag lifting, not more two-copy coefficients. `proof/repeated_p3_terminal_tube.md`. |
| A70 | F/G | Check whether repeated equivalent `2 x 2` scalar-support blocks create new large-multiplicity equality tangents | **NO NEW MODULI (L111)** | Linearize the two scalar-support relations at `Z_ir tensor I_k`.  In both the square-zero and invertible nonnormal strata, the homogeneous tangent has dimension `3k²`, exactly the unitary commutator orbit; allowing the complex square and real anticommutator scalars to move adds only three repeated two-dimensional parameters.  Hence multiplicity mixing is basis motion, not a new terminal family.  The arbitrary-copy lift can glue L110 blockwise once normal collisions and proper common-top flags are controlled. `proof/repeated_p3_scalar_support_rigidity.md`; `experiments/repeated_p3_scalar_support_rigidity.py`. |
| A71 | F/G | Resolve the collision where a full scalar-support block becomes normal with arbitrary `+/-` eigenvalue multiplicities | **RECTANGULAR EDGE SPLITS INTO L110 PAIRS (L112)** | Linearized equality forces scalar-opposite diagonal blocks but leaves a rectangular pair `B,C`.  A unitary orbit kills `C` and leaves `B−C*`; its SVD is a direct sum of independent trace-zero `2 x 2` Schur edges plus unmatched normal coordinates.  Thus even the singular normal boundary introduces no higher-dimensional terminal model.  The only flat-core gate left is uniformity when L93's common-top space is proper and its descending flag changes rank. `proof/repeated_p3_normal_collision.md`; `experiments/repeated_p3_normal_collision.py`. |
| A72 | F/G | Uniformize L93 when the common-top space is proper and the descending leakage flag changes rank | **FIXED-MULTIPLICITY FLAT CORE CLOSED (L113)** | The flag leakage forms are coercive transverse to their exact kernel, and L111--L112 identify that kernel with already covered unitary/blockwise strata.  A block-sign unitary makes the active endpoint even in every cross coupling, improving both the support inclusion slack and analytic endpoint remainder to quadratic transverse order.  Thus `−c s²||R||²` absorbs `C s³||R||²`; gap collapse merely enlarges the common-top block and moves to a lower finite flag stratum.  Compactness closes the full flat core for each fixed multiplicity.  Next merge through L86; do not return to copy-matrix classification. `proof/repeated_p3_flat_flag_tube.md`. |
| A73 | F/G | Glue the flat-core tube through the positive-Jensen, generator-one, common-strong, and winner--loser variables | **FULL REPEATED-`C3` LOCAL THEOREM (L114)** | Off the zero-Jensen face L61 is strictly descending.  On it, L86 is a complete sum of negative matrix/Gram forms and L87 says every kernel is a reducing flat block.  L113 controls those blocks; at their zero-margin disk strata L72 supplies ambient stationarity, so the strong remainder is quadratic and L86 is coercive on the complementary active slice.  L82 and finite kernel promotion handle losing-gap/rank collapse.  A stratified compactness cover proves a full complete-`2` neighbourhood of `I_m tensor C3` for every fixed `m`.  The campaign must now leave this local model and attack other equality blocks/general matrices. `proof/repeated_p3_local_neighbourhood.md`. |
| A74 | F/G | Intersect the arbitrary-size Crabb second-order kernel with the known disk-matrix tangent manifold before computing higher jets | **ONE COMPLEX NORMAL SURVIVES (L115)** | Lewis--Overton's tangent equations kill support modes `2,...,p`.  All but one complex coordinate of L66's residual quotient are tangent to the exact circular-range family, already controlled by Berger--Okubo--Ando; the sole soft normal is mode two, represented by `C_p*`, and generates the exact ellipse `z+c conjugate(z)`.  Thus the larger-block campaign should study one dimension-independent Chebyshev--Lobatto elliptic family plus coercive transverse directions, not grind `2p−2` unrelated jets. `proof/crabb_disk_tangent_intersection.md`. |
| A75 | F/G | Determine the exact similarity constant on the arbitrary-size pure elliptic Crabb axis before attempting a transverse tube | **COMPLETED BY L116--L117** | L116's degree-`p−1` Chebyshev--Blaschke alternation and DCT reversal give the exact lower bound `k(c^(2p−2))/c^(p−1)`.  L117 constructs the matching all-size diagonal metric by turning the optimizer's Szegő Gramian into a DCT-I Toeplitz-plus-Hankel kernel.  Its weights are a periodized-sech sequence, with Jacobi reflection and termwise monotonicity giving the sharp metric endpoints.  Thus the axis is complete-`2` in every size and the gap stated in Li's thesis is closed. `proof/crabb_elliptic_axis.md`. |
| A76 | F/G | Extend the exact L117 elliptic-axis metric to a tubular neighbourhood using L115's disk-manifold reduction and L65's coercive transverse complement | **ACTIVE — TRANSVERSE ABSORPTION GATE** | Work in a tubular chart around the arbitrary-center circular-range manifold with the one complex soft coordinate `cC_p*`.  Use L117 on that axis and L65 on the strong normal complement.  The key difficulty is quantitative: the axis gap is `4−t_*=16c^(2p−2)+O(c^(4p−4))`, so a naive fixed-axis margin degenerates rapidly as `c->0`; the transverse certificate must match the weighted order and merge with the exact disk anchor at `c=0`.  First derive the transverse variation of L117's rank-one Stein metric/defect and look for a congruence or Schur-complement absorption, rather than restarting all residual jets or a generic `5 x 5` slice grind. |
| A77 | F/G | Put L65 and L117 on one analytic rank-one metric branch, then test the only dangerous weighted transverse derivative | **ANALYTIC REDUCTION PROVED (L118); CANCELLATION COMPLETED BY L120** | The rank-one Gramian's defect-vector Hessian is diagonal positive (`8,...,8,8/3`), so an IFT produces a unique analytic feasible envelope.  Its matrix Hessian is L65 and its exact elliptic-axis value is L117.  Strong-variable maximization reduces the local problem to L115's disk-flat coordinates plus one elliptic normal.  If the strong gradient on the axis is `o(c^(p−1))`, its squared optimization gain is negligible beside L117's `−16c^(2p−2)` margin.  The sparse support-resolvent/Riemann/Stein engine discovered the cancellation in low sizes; L119--L120 now prove it in every size. `proof/crabb_rank_one_envelope.md`. |
| A78 | F/G | Replace the differentiated Stein envelope by the matching scalar lower extremal at the elliptic axis | **EXACT TOUCHING REDUCTION (L119); COMPLETED BY L120** | The fixed-defect Stein upper function and fixed Chebyshev--Blaschke norm lower function sandwich `t_*` and agree exactly at every L117 axis point, so their derivatives agree in all matrix directions.  The top singular pair is the reversal endpoint pair, reducing the full gradient to `<e_0,D[B_c(phi_A(A))]e_L>`.  Jacobi multiplication further identifies `B_c o phi_c` as the degree-`p−1` Chebyshev polynomial descent from `E_c` to `E_(c^(p−1))`; its matrix value is a nested family of `2 x 2` reversal blocks.  L120 proves the endpoint cancellation by fibre trace, Lobatto quadrature, and path counting. `proof/crabb_touching_gradient.md`. |
| A79 | F/G | Push the L119 endpoint shape derivative through the Chebyshev polynomial fibres and the outer `2 x 2` reversal block | **COMPLETED — ALL-SIZE PURE ELLIPTIC STRONG TUBE (L120)** | The polynomial fibre trace commutes with endpoint compression, and a root-of-unity differential quadrature identifies the averaged mapped support variation with the support variation of `Pi DP[A][Y]Pi`.  Riemann shape derivatives therefore descend modulo a harmless disk automorphism.  The exact gradient is the classical `2 x 2` elliptic derivative applied to the outer compression.  Dickson path counting supplies `c^floor((p−1)/2)` and the `2 x 2` derivative supplies `c^(p−1)`, proving the L118 `o(c^(p−1))` gate with room to spare.  Next merge the exact disk anchors with the elliptic coordinate; no strong-space calculation remains. `proof/crabb_descent_gradient.md`. |
| A80 | F/G | Test whether L65 equality removes the first L120 endpoint path before computing disk-flat quartics | **PROVED STRICT FLAT SELECTION (L121)** | The complete endpoint functional has an explicit circle-grade formula.  Its first coefficient is `Y_(L0)` for odd `L` and `sqrt(2)(Y_(L−1,0)+Y_(L,1))` for even `L`: exactly L65's bottom-mode equality obstruction (with the `p=3` exception killed by the circular-tangent equation).  Thus every disk-flat derivative gains one power, becoming `O(c^(L+floor(L/2)+1))`.  If the numerically observed disk-flat deficit is a coercive quartic, Young's inequality now gives a strict exponent above the `c^(2L)` axis scale.  The quartic and higher mixed remainder remain the live gate. `proof/crabb_flat_endpoint_selection.md`. |
| A81 | F/G | Parametrize the disk-flat anchor exactly and derive its fourth-order rank-one metric deficit | **EXACT CHART/QUARTIC PROVED; COERCIVITY DISPROVED (L122)** | Writing `K=H+R*HR` and `X(H)=2K^−1/2HRK^−1/2` gives an exact unit-disk matrix for every positive Hermitian `H`.  Toeplitz `H=I/2+sZ(z)` spans the full disk-flat quotient.  The explicit defect `q=He_0` gives condition square `4−32s^4(||z||^4−|z^TJz|²)+O(s^5)`.  The quartic is a Gram determinant and vanishes exactly when `z=omega J conjugate(z)`.  Exact size-four defect optimization proves the analytic envelope has the same null, so the planned coercive Young merger is impossible.  Route next through this phase-palindromic equality candidate, not a positive-definite quartic. `proof/crabb_disk_toeplitz_quartic.md`. |
| A82 | F/G | Classify L122's phase-palindromic quartic null by factoring its Stein metric and constructing a matching scalar extremal | **COMPLETED — EXACT ALL-SIZE EQUALITY FAMILY (L123)** | A diagonal phase gauge makes the coefficient operator companion-like.  The rank-two update `M=K−qq*+2rr*` is an exact rank-one-defect Stein metric with relative eigenvalues `1/2,1,2`.  Removing the forced zero from the characteristic polynomial gives `g`; the quotient `B=g/g^sharp` is inner and satisfies `B(A)=4e_0r*`, hence has physical norm two.  Therefore every positive phase-palindromic Toeplitz chart point has `t_*=4` and scalar Crouzeix norm two.  The union over phase is singular at the Crabb apex, so the next step is a stratified rather than ordinary normal form. `proof/crabb_palindromic_equality.md`. |
| A83 | F/G | Resolve the singular phase union into a best fixed-phase equality component plus a disk-normal component | **COMPLETED — EXACT STRATIFIED NORMAL FORM (L124)** | Reversal conjugation is an antiunitary involution.  Optimizing its phase and taking the real `+/-` projections gives `z=u+v` with `Q(z)=4||u||²||v||²` and `||v||=dist(z,E)`.  Hence the L122 disk margin is quadratic in normal distance with coefficient proportional to equality amplitude squared; the coefficient necessarily vanishes at the Crabb apex.  The next calculation must supply the elliptic deficit created by `u`, rather than demand uniform disk coercivity. `proof/crabb_palindromic_normal_form.md`. |
| A84 | F/G | Measure and prove the elliptic deficit along exact phase-palindromic disk equality anchors | **COMPLEX OFFSET ONE + DIVISOR GRADES PROVED (L132–L134); FULL FACE OPEN** | Along `H=I/2+aZ(u)`, the optimized rank-one envelope and full SDP agree to `3.5e−9` and have candidate leading change `−64a² sum_k |u_k|²c^(2k)` beyond the exact Crabb-axis margin `−16c^(2L)`.  L125 proves scalar grade filtration.  L133–L134 prove `−64|u|²c²` for arbitrary complex offset-one phase, and L132 transfers its upper face to every divisor grade.  The remaining coefficient gates are distinct-grade polarization and nondivisor unequal-residue localization; after those, establish a dimension-uniform remainder. `proof/crabb_palindromic_elliptic_face.md`; offset-one real/complex proofs and checkers. |
| A85 | F/G | Reconstruct A84's optimized amplitude Hessian over exact rational power series before attempting the all-size induction | **FINITE GENERAL RECURRENCE THROUGH `p=8`; COMPLEX OFFSET ONE PROVED ALL-SIZE (L133–L134)** | Expanding `T=T_0+aT_1+a²T_2`, the L117 rank-one metric, the Stein Gramian, both generalized endpoint eigenvalues, and the defect-vector minimizer gives exact vanishing below `c^(2k)` and coefficient `−64` for every first offset through `p=8`.  The optimizer begins at the L123 equality defect and acquires a forced `−8c` component two coordinates later.  The `p=8,k=3` extension exposed and repaired an internal elliptic-map guard bug.  L133–L134 convert `k=1`, including arbitrary phase, into an all-size Stein proof; L131 supplies the all-grade Faber endpoint vector.  Mixed-grade and nondivisor metric normal forms plus uniform remainder remain open. `proof/crabb_palindromic_elliptic_hessian.md`; offset-one real/complex proofs; `proof/crabb_faber_reflection.md`; corresponding checkers. |
| A86 | F/G | Explain A85's dimension-independent `−64` by descending a central offset to its active terminal block | **CENTRAL METRIC DESCENT PROVED EXACTLY (L126--L130); LOCALIZE GENERAL OFFSETS NEXT** | In size `2k+1`, `P_(k,c)(C+cJCJ+aE)` is exactly affine in `a` and has an orthogonal reducing outer `3×3` block equal to the full `C3` ellipse/equality family at parameter `c^k`; the coordinate Gramian reduces the same block.  L127 gives exact positive primal and dual finite-Blaschke transfers.  L130 factors the outside critical points of `B`, proves a constant unweighted fiber-trace theorem, and constructs a full rank-one metric with exactly the size-three active endpoints.  Thus `t_*(T_k(a,c))=t_*(T_1(a,c^k))` locally for every fixed `k`, proving the central `−64a²c^(2k)` event.  The remaining A84 step is to localize each noncentral offset to its central `2k+1` window, control interactions with the unused tails, and polarize distinct grades. `proof/crabb_central_dickson_descent.md`; `proof/blaschke_stein_composition.md`; `proof/crabb_central_dual_lift.md`; corresponding checkers. |
| A87 | F/G | Replace L126's opaque inactive complement by a fixed operator-valued normal form | **PROVED EXACT (L128); USE IN METRIC SCHUR COMPLEMENT NEXT** | In two residue layers the entire inner descended pencil is `[[H,I],[rI,H]]+2a[[-rI,H],[H,−I]]`, the inner coordinate Gramian is `[[I,2aI],[2aI,I]]`, and `H²=rI`.  A diagonal gauge turns `H` into `sqrt(r)` times reversal, so there are only two algebraic `2×2` fiber types; the nonunitary gauge weight is the remaining condition-number content.  This routes the central lift to a dimension-free `2×2` operator Schur complement rather than coordinate-by-coordinate Stein propagation. `proof/crabb_central_inner_fibers.md`. |
| A88 | F/G | Prove that no descended inner central fiber can become active in the singular local regime | **PROVED UNIFORMLY (L129)** | At the apex the inner block is `N=[[0,I],[0,0]]`; `diag(I/2,I)` has Stein slack `I/2` and condition square two.  L128 gives a dimension-independent `O(|a|+c)` perturbation, so the same metric stays below three while the size-three outer value stays above three.  Thus `t_*(B(T))` is exactly the outer size-three value locally.  The only remaining central issue is the quantitative condition cost of lifting this active metric through L127. `proof/crabb_central_inner_inactivity.md`. |
| A89 | F/G | Lift the active size-three dual witness and recover the full central metric from its one-dimensional kernel | **PROVED EXACTLY (L130)** | Write the real Chebyshev--Blaschke product as `B=N/D`, factor `N'D−ND'=kappa Q Q#` with `Q` carrying the outside critical points, and put `F=Q/D`.  A residue theorem gives `(1/k)sum_(B(t)=u) f(t)/F(t)=<f,F>/||F||²` for every `f in K_B`, independent of `u`.  L130 proves the matching central subcritical compression `Pi P_m(S(a,c))Pi=0` for `1<=m<k`.  Hence `J=F(T)^−*O` explicitly maps an outer defect to the full defect, and `sum gamma_j w_j=0` follows from `F=sum gamma_jf_j`.  The metric reduces the outer block and has its exact size-three compression; the apex inner-level gap persists locally.  Exact symbolic checks cover the central compression through `k=12`; critical-factor/fiber/metric audits cover the main grid and stress degrees through eight. `proof/crabb_central_dual_lift.md`; `experiments/blaschke_critical_trace.py`; `experiments/crabb_central_dual_lift.py`. |
| A90 | F/G | Replace noncentral coefficient-by-coefficient Hessian propagation by a Faber endpoint normal form | **ENDPOINT REFLECTION PROVED (L131); METRIC NORMAL FORM OPEN** | The exact identity `e_(0,L)*DP_L(S0)[E_j+cJE_(L−j)J]=0` shows that a phase-palindromic equality tangent is invisible in the two degree-`L` endpoint rows.  Adding the equality polynomial coefficients leaves the analytic row `4(u_j)` and the reflected row `4(u_jc^j)`.  Parseval gives exactly the candidate energy `16 sum|u_j|²c^(2j)` and forbids cross-grade terms.  This supplies the missing all-size algebraic localization target.  It is not yet A84: prove that the remaining Faber rows and model-space lift are inactive/removable above this first Newton face, using L130 for each one-coordinate central normal and the strict inner gap. `proof/crabb_faber_reflection.md`; `experiments/crabb_faber_reflection.py`. |
| A91 | F/G | Generalize central descent from `L=2k` to every divisible grade `L=qk` | **PROVED AS AN EXACT RANK-ONE ENVELOPE LIFT (L132)** | Degree-`k` Dickson descent reduces the residue-zero coordinates to the complete size-`q+1` first-offset equality/ellipse family at parameter `c^k`; the coordinate Gramian reduces identically and all other residue chains are strictly inactive near the apex.  The subcritical compression needed by L130 still vanishes, so the same outer-critical multiplier lifts the outer L118 defect with no condition cost.  This gives `t_outer<=t_full<=Gamma_outer`, enough to transfer any proved offset-one upper face to every divisor grade.  Do not promote the sandwich to equality for `q>2`: the global SDP optimality of L118's rank-one branch remains open.  Nondivisible grades require unequal-residue localization. `proof/crabb_divisible_dickson_descent.md`; exact and floating checkers. |
| A92 | F/G | Prove the offset-one `−64` coefficient once in arbitrary size and transfer it through L132 | **REAL PHASE-ONE FACE PROVED (L133)** | The exact defect jet is the disk seed `2e_1+2e_(L−1)` plus the universal elliptic correction `−8ce_3`.  Its quadratic Stein forcing stabilizes at the two chain ends for `L>=7`; the endpoint metric coefficients are always `(48,128)`, giving `128−4(48)=−64`.  The defect-gradient residual is `O(c²)`, so L118's analytic optimizer has the same `c²` coefficient.  L132 now propagates this upper face to every real phase-one divisor grade.  This does not yet polarize complex/mixed grades, localize a nondivisor grade, or provide the uniform Newton remainder. `proof/crabb_offset_one_face.md`; `experiments/crabb_offset_one_face.py`. |
| A93 | F/G | Test and prove phase isotropy of the offset-one face before attempting mixed-grade polarization | **COMPLETED FOR ARBITRARY ONE-GRADE PHASE (L134)** | The pure-imaginary companion tangent has exact defect jet `i(-2e_1+2e_(L−1)+8ce_3)` and the same endpoint metric pair `(48,128)`.  Conjugation symmetry kills the real/imaginary mixed term, proving `−64|u|²c²` for arbitrary `u`.  L132's descent and metric lift complexify without condition cost, so every divisor grade now has the complex one-pair upper face `−64|u|²c^(2k)`.  Distinct-grade cross terms, nondivisor localization, and uniform remainder remain. `proof/crabb_offset_one_complex_face.md`; exact Gaussian-pair and complex-descent checkers. |
| A94 | F/G | Polarize two distinct equality grades and test the first allowed mixed Newton coefficient `c^(k+l)` | **EXACT FINITE CROSS TERMS VANISH; ALL-SIZE SCHUR/FILTRATION COMMUTATION OPEN** | Exact optimized Hessian polarization gives zero through and including `c^(k+l)` for every grade pair through size seven; deeper jets remain identically zero through orders nine/eleven in selected cases, including the nontrivial same-parity pair `(1,3)`.  L125 puts the first possible cross term at weight `k+l`, L131 makes the associated Faber rows orthogonal, and L65 makes the unperturbed metric Hessian circle-mode diagonal.  The proof gate is to show that L118's defect Schur complement commutes with this associated-graded projection; terminal aliases must gain strictly higher weight.  Do not cite Parseval alone—the naive defect transport is known false. `proof/crabb_mixed_grade_face.md`; `experiments/crabb_mixed_grade_face.py`. |
| A95 | F/G | Falsify the stronger possibility that the complete optimized amplitude Hessian is block diagonal in distinct Faber grades for every fixed ellipse | **SURVIVES EXACT REAL `c^17` AND COMPLEX FLOATING PROBES; L135 PROVES THE DCT/DST BRIDGE; SCHUR KERNEL OPEN** | The size-five real `(1,2)` cross jet is exactly zero through `c^17`, and all three size-seven real pairs are exactly zero through `c^11`, including same-parity `(1,3)`.  Ordinary-`c` real and mixed complex-phase residuals scale like the square of the amplitude step, identifying quartic contamination.  The blocks are not phase-isotropic at fixed nonzero `c`: real and imaginary diagonal values can differ, while L134 equates only their first face.  L135 proves that the grade-`j` companion row is a scaled DST-I sine mode in L117's DCT-I axis basis, with no terminal alias.  Remaining theorem: prove L118's optimized defect Schur kernel is DST-I block diagonal and expand its within-grade eigenvalues. `proof/crabb_mixed_grade_face.md`; `proof/crabb_spectral_sine_factorization.md`; exact/high-precision checkers. |
| A96 | F/G | Reduce a nondivisor one-pair grade by the common divisor `d=gcd(L,k)` before attacking unequal residue chains | **PROVED EXACTLY (L136); ONLY COPRIME LENGTH–GRADE PAIRS REMAIN** | Degree-`d` Dickson descent reduces the full grade-`k=ds` equality/ellipse pencil and Toeplitz coordinate Gramian to the size-`q+1` grade-`s` family at `c^d`, with vanishing polynomial cross blocks and subcritical outer compressions.  L132's critical-factor metric reconstruction then gives the same rank-one sandwich with no condition cost.  This strictly extends L132 (`s=1`) and means the unresolved diagonal problem can assume `gcd(L,k)=1`; it does not solve those coprime unequal-residue cases. `proof/crabb_gcd_dickson_descent.md`; exact checker. |
| A97 | F/G | Bound every grade-`k` amplitude Hessian by the minimal central window `L=2k` at the same ellipse parameter | **FALSIFIED AWAY FROM THE NEWTON APEX** | Although all lengths have the same conjectured first coefficient `−64c^(2k)`, the full fixed-`c` ordering is false.  A stable finite-amplitude Hessian probe at `c=.5` gives `Q_(9,3)≈−0.65425` versus the central `Q_(6,3)≈−0.92969`, reversing the proposed `Q_(L,k)<=Q_(2k,k)` comparison by about `.275`.  Larger-`c` optimization becomes ill-conditioned and is not needed for the falsification.  Do not replace the coprime first-face/DST principal-symbol proof by an all-`c` minimal-window monotonicity claim. |
| A98 | F/G | Identify the invariant completed square behind the reflected Faber row | **UNIFORM EQUALITY-STRATUM TUBE PROVED (L137–L150); OFF-STRATUM MERGER OPEN** | L137--L146 prove the compact primal/dual face.  L149 constructs its convergent all-amplitude reflected Hardy lift and kills the complete one-reflection sector by the endpoint resolvent `1/(zB)`.  L150 avoids an optimized-Stein induction: L145's explicit model-complement metric has a nonnegative gap that vanishes on the equality divisor, hence is automatically quadratic; zero-diagonal PSD polarization preserves the compact face.  The uniform half-face estimate follows for all relative scales.  Remaining: L124's best-phase normal and L118's strong variables. `proof/crabb_reflected_hardy_lift.md`; exact and floating checks. |
| A99 | F/G | Interpolate the disk characteristic and elliptic Chebyshev inner functions, then optimize the first mixed Faber coefficient | **FULL EQUALITY-STRATUM PRIMAL/DUAL LIFT PROVED (L140–L150); OFF-STRATUM MERGER OPEN** | L140--L146 close the corrected compact dual face and its one-grade primal complement.  L149--L150 promote it to a convergent arbitrary-amplitude theorem: the Hardy logarithmic mean removes one reflected leg, and the canonical model-complement gap supplies the primal upper remainder without assuming global optimality.  The Faber/Blaschke equality-stratum work is complete; next glue L124's phase-normal disk deficit near the singular apex. `proof/crabb_reflected_hardy_lift.md`; seven checkers. |
| A100 | F/G | Extend L145's orbit-complement metric exactly over the whole Toeplitz disk chart and use it to merge disk-normal/reflected descent | **EXACT COMPLEMENTARITY FALSIFIED; GRADED GAP ROUTE OPEN** | Rank-one model complementarity fixes one generalized endpoint but does not force the opposite singular line to be the least generalized eigenline.  The rational disk point `(z1,z2)=(1/20,1/30)` gives an exact nonzero lower-endpoint residual, hence `kappa(P_model)>||B(A)||_K²`.  Small-amplitude binary64 gaps were high order and hid this.  The viable replacement is to prove zero disk-normal and reflected compact faces of the nonnegative model gap and eliminate the mixed face by PSD polarization. `proof/crabb_disk_flat_marked_merger.md`; `experiments/crabb_disk_model_complement.py`. |
| A101 | F/G | Extend L149's zero one-reflection sector from the equality cone to every Toeplitz disk point | **EXACT ZERO FALSIFIED; SUFFICIENT `O(Q)` THEOREM PROVED (L154–L156)** | At `(z1,z2)=(1/20,1/30)` the exact dual-norm derivative is the nonzero rational `≈−8.48e−6`, so global stationarity remains false.  The correct replacement is now proved: the endpoint residual has zero full first jet on the equality cone, and analytic cone division gives `|one-leg|≤C_L|c|^kQ`.  The disk-normal deficit can therefore absorb every marked dual one-leg term. `proof/crabb_disk_one_reflection.md`; both exact checkers. |
| A102 | F/G | Replace literal `Q` divisibility by a uniform singular-cone Taylor lemma | **PROVED AND APPLIED (L154–L155)** | Value-plus-gradient vanishing on the equality cone automatically forces fourth-order apex start: after a real coordinate change the cone is the generic `2×(L−1)` rank-one locus, and its second symbolic ideal equals the square of its quadratic maximal-minor ideal.  L124's best-phase split and normal Taylor expansion then give `|F|≤C_LQ`.  L156 supplies the required gradient for every marked one-reflection coefficient, so no explicit `Q` factorization, Łojasiewicz estimate, or separate apex recurrence is needed. `proof/crabb_quartic_cone_division.md`; `proof/crabb_disk_one_reflection.md`. |
| A103 | F/G | Express the missing first-normal cancellation as a Toeplitz bordered determinant | **PROVED BY INVERSE-FREE ENDPOINT RECURRENCE (L156)** | In the phase-one gauge, `K dotA` has only a direct top row, a reverse-conjugate bottom row, and one rank-one interior term.  Explicit left/right companion endpoint vectors reduce the determinant derivative to `xi(P_hg−2g#P_h^flat)`.  Rootwise rank-one adjugate factorization and polynomial continuation give `D_hg=2P_h^flat`, while the endpoint cofactor derivative is `2P_h`; hence the residual first jet vanishes in every disk direction.  Exact recurrence and stronger ambient-gradient audits reach length ten. `proof/crabb_disk_one_reflection.md`; exact checker. |
| A104 | F/G | Show that A100's nonzero orbit-complement gap lies strictly above the disk quartic face | **PROVED `0<=DELTA(z,0)<=C_LQ(z)^2` (L157)** | The normalized orbit defect is determined by a square observability system.  Laurent moment extraction expresses its canonical-defect residual as a finite functional of L156's endpoint numerator `T−g#`, so L155 gives `d_model−q_can=O(Q)`.  The lower generalized Rayleigh residual is exactly half this vector; Temple's bound squares it, while model complementarity fixes the top level.  Thus the false exact equality fails only above the required disk-normal face.  Exact full-coordinate defect jets reach length ten. `proof/crabb_disk_flat_marked_merger.md`; exact checker. |
| A105 | F/G | Merge A104's pure disk gap with L150's reflected compact gap without computing a mixed recurrence | **PROVED BY NONNEGATIVE GRADIENT CONTROL (L158)** | In the independent real reflected-Rees polydisk, `Delta>=0`.  A uniform second-derivative bound implies `||D_eta Delta(z,0)||²<=C Delta(z,0)`; L157 therefore makes the whole mixed linear row `O(Q)`.  The compact `eta` Hessian is zero at the apex by L145--L146 and hence arbitrarily small nearby.  Thus `Delta<=CQ²+CQ||eta||+epsilon||eta||²`, while L152/L154/L144 give the matching negative dual faces.  Shrinking proves `U−4<=−aQ−b||eta||²`; no exact all-disk complementarity or mixed coefficient induction is needed. `proof/crabb_disk_flat_marked_merger.md`. |
| A106 | F/G | Compare prepared reflected coordinates with raw Toeplitz grades through the singular physical pullback | **TWO-SIDED COMPARISON DISPROVED EXACTLY (L159-old); ONE-SIDED LOWER TARGET OPEN** | Ordinary coefficient linearization does not survive anisotropic pullback.  For `L=6`, `z_1=t`, exact characteristic elimination gives `a_3=32t^7/((6t²−1)(16t⁴−14t²+1))`; with `c=t^5`, the prepared norm has order `t^44` while the proposed raw norm has order `t^52`.  The upper comparison fails by a diverging factor.  This alias is favorable for L158's negative sign.  Either prove only `N_prep>=c_LN_raw` or keep the prepared norm in the strong-variable merger. `proof/crabb_disk_flat_marked_merger.md`. |
| A107 | F/G | Absorb all L118 strong directions directly into L158's Toeplitz prepared deficit | **FULL-STRONG TARGET FALSIFIED; FULL-DISK TUBULAR ROUTE RESTORED; OFFSET-ONE NORMAL FACE PROVED (L160)** | On non-palindromic Toeplitz disk paths, the completed gain from the full strong Hessian equals the entire quartic disk deficit to leading order.  The cancelling directions recenter onto the larger Lewis--Overton circular-range manifold, so this is geometry rather than a violation.  The correct L115 normal quotient, rather than a Euclidean Hessian-range intersection, leaves `2p−4` coercive directions after the elliptic soft normal.  The only offset-one compact cross is the bottom mode; its exact coefficient and L65 square leave the positive residual `R_L`.  Next prove the higher-grade selection/extra-`c` rule and lift the resulting normal form uniformly over the complete disk manifold. `proof/crabb_circular_normal_face.md`; exact and adversarial probes. |
| A108 | F/G | Test whether higher reflected grades gain an extra weighted order against true circular normals | **PROVED ALL GRADES `k>=2` BY L171--L172** | L161 proves grade two termwise.  L171 splits the general target by reflection count and kills every ordinary singular-Hessian term by disjoint support.  L172 closes the remaining direct endpoint row as a Cauchy-residue selection: the only product powers are `L−2k+1` and `L+1`, both positive.  Uniform tubular division is downstream. `proof/crabb_grade_two_normal_selection.md`; `proof/crabb_all_grade_normal_bridge.md`; `proof/crabb_dual_endpoint_residue.md`. |
| A109 | F/G | Replace raw-grade division by a full ambient tubular chart around the exact phase-palindromic equality ridge | **LOAD-BEARING FIRST-ORDER GATE PROVED (L162); UNIFORM NORMAL ESTIMATE OPEN** | L162 proves that L123's explicit feasible rank-one certificate has zero first derivative in every normalized ambient direction at every positive equality anchor.  The proof is not extrapolated from Taylor data: a touching finite-Blaschke lower bound identifies the derivative, L156's endpoint resolvents give its boundary density, and an exact transfer/kernel identity matches the Schwarz correction.  This supplies the critical-ridge half of a Morse--Bott/tubular argument and explains L161's cancellation structurally.  Next build a prepared full-disk chart and combine L160's coercive normal face, L161's higher-grade cancellation, and the existing axis margins into one uniform negative normal estimate through the stratified Crabb apex. `proof/crabb_equality_ambient_stationarity.md`; exact and full-ambient numerical checker. |
| A110 | F/G | Identify the all-grade compact coupling between reflected Hardy coordinates and true circular normals | **PROVED COMPLETE CROSS VANISHING FOR `k>=2` (L171--L172)** | Reflection count and L162 remove the zero-reflection sector; L166 transfers the one-reflection row; sparse support kills its ordinary quadratic Hessian.  L172 explicitly evaluates the remaining L149/L156 functional rather than invoking a vague Hardy projection: after converting the relative defect mode to the absolute endpoint residual, neither reflected Blaschke power can produce the `z^(−1)` residue. `proof/crabb_all_grade_normal_selection.md`; `proof/crabb_all_grade_normal_bridge.md`; `proof/crabb_dual_endpoint_residue.md`. |
| A111 | F/G | Bypass the sharp circular-normal Schur calculation by proving the off-equality ambient gradient is `O(Q)` | **FALSIFIED, INCLUDING THE TRANSPORTED COERCIVE NORMAL PROJECTION** | An exact size-four apex ray has `Q=25281x^4/15625000000` but characteristic-dual derivative `R(E_12)=−424x²/15625`, rigorously disproving the full ambient target.  Projecting numerically onto transported support modes `3,...,p` gives rank `2p−4` and retains a nonzero `sqrt(Q)` coefficient through `p=7`; equality controls are `~1e−14` and ratios are stable over four scales and FFT resolutions `1024,...,8192`.  L157 makes the optimized-upper/dual gradient difference only `O(Q)`, so it cannot remove this term.  Return to L163's covariant joint cancellation; scalar cone division is structurally too coarse. `proof/crabb_disk_ambient_gradient_division.md`; exact and floating checkers. |
| A112 | F/G | Use reciprocal reversal to collapse L163's two endpoint recurrences | **STRUCTURAL REDUCTION PROVED (L164); DOWNSTREAM ROW CLOSED BY L172** | For every real persymmetric normalized path, rank-one inverse-Stein duality and uniqueness of L118's defect optimizer give `JP^(-1)J=alpha P` exactly.  Reversed reciprocal eigenpairing determines one endpoint series from the other and `alpha=(det P)^(-2/p)`.  Self-duality alone constrains only the sum of relative endpoint derivatives; L171--L172 close the needed difference by the independent sparse-dual/residue route. `proof/crabb_reciprocal_reversal.md`; exact checker. |
| A113 | F/G | Replace L163's two weight-`2d` endpoint lifts by an adjoint condition-gradient recurrence | **PROVED HALF-ORDER/ENDPOINT-FLUX REDUCTION (L165); BALANCE CLOSED BY L172** | Solving `Z−TZT*=v_+v_+*/lambda_+−v_-v_-*/lambda_-` gives `Zq=0` by defect stationarity and an endpoint-source commutator law.  The target is a `d+1`-term endpoint/characteristic convolution.  Exact grades distinguish the nonzero grade-one exception, termwise grade-two zero, and nontrivial grade-three/four telescopes.  L171--L172 prove their all-grade total by marked-sector, sparse-support, and residue selection rather than expanding every term. `proof/crabb_adjoint_normal_recurrence.md`; exact checker. |
| A114 | F/G | Transfer L163's compact reflected/normal row from the optimized Stein envelope to the sharp prepared Blaschke lower certificate | **PROVED BY ASSOCIATED-GRADED POSITIVITY (L166); DUAL ROW CLOSED BY L172** | For each nonzero scale, `Delta=U_Stein−R_Blaschke>=0` and vanishes on the exact axis, so its amplitude/normal Hessian is PSD.  Vanishing of the pure reflected gap face and strong scaling force the mixed gap below weight `2k+2`; hence the optimized and sharp dual rows agree.  L171 kills the common row's ordinary singular-Hessian part and L172 kills its direct endpoint residue. `proof/crabb_dual_normal_transfer.md`; `proof/crabb_dual_endpoint_residue.md`. |
| A115 | F/G | Explain the grade-one exception by claiming all higher eligible normals preserve the zero characteristic root | **FALSIFIED EXACTLY IN GRADE THREE** | Grade two does preserve `det T` through its weight-six face, but this is accidental.  In the central grade-three path, `partial_s det T` has exact nonzero coefficients `-4 epsilon^5`, `-2(-17+8sqrt(2))epsilon^7`, and `-4(-17+12sqrt(2))epsilon^8`, even though the condition derivative cancels at weight eight.  Thus the L149/L162 bridge cannot freeze the zero eigenvalue or a characteristic factor; it must transport the moving characteristic polynomial together with the endpoint basis. `proof/crabb_adjoint_normal_recurrence.md`; `experiments/crabb_companion_fixed_zero_adversary.py`. |
| A116 | F/G | Identify L163's normal/defect coefficient inside the scalar endpoint companion transfer | **LEADING INNER TANGENT PROVED (L168); FUNCTIONAL KERNEL CLOSED BY L172** | For grade `k>=2`, exact path counting makes normal minus optimized-defect response `k(z^(-m)-z^m)`.  The discrepancy is the single relative mode `z^m`; after multiplication by `F_0` it is the absolute mode `z^(1-k)`.  L172 applies L156's actual Cauchy functional and shows that neither reflected Blaschke power can make a residue. `proof/crabb_leading_endpoint_transfer.md`; `proof/crabb_dual_endpoint_residue.md`. |
| A117 | F/G | Replace the coefficientwise moving-characteristic problem by the characteristic transfer of the optimized rank-one Stein colligation | **EXACT ALL-ORDER INNER TRANSFER PROVED (L169); ASSOCIATED FUNCTIONAL CLOSED BY L172** | Every rank-one Stein pair completes to a unitary colligation with scalar inner transfer `theta(z)=d+zq*(I-zT)^(-1)r=omega det(zI-T*)/det(I-zT)`.  L164 gives `r=sqrt(alpha)Jq`, so the moving endpoint, defect, determinant, and feedthrough data are encoded by one exact inner function.  A115's later determinant motion is included as `d`.  L172 identifies the only associated mixed functional needed by L163 as a residue and kills it by power selection. `proof/crabb_colligation_transfer.md`; `proof/crabb_dual_endpoint_residue.md`. |
| A118 | F/G | Continue L149's degree-`p-1` Blaschke factor through A115's moving characteristic feedthrough without selecting an eigenvalue | **PROVED BY ONE SCHUR STEP (L170); NORMING FUNCTIONAL CLOSED BY L172** | For L169's inner function put `d=theta(0)` and `B=(theta-d)/(z(1-conj(d)theta))`.  This is a real-analytic degree-`p-1` Blaschke product and equals L149's prepared factor when `d=0`.  At the Crabb point, grade one maps to `dot B=0`, while every `k>=2` gives `dot B/B=-k(z^(-m)-z^m)`.  L172 evaluates the remaining L156 functional explicitly as a residue. `proof/crabb_characteristic_schur_step.md`; `proof/crabb_dual_endpoint_residue.md`. |
| A119 | F/G | Separate L165's long physical convolution by reflection count and isolate the higher-grade endpoint row | **PROVED REDUCTION (L171); ENDPOINT CLOSED BY A120/L172** | L149's marking and L162 kill the zero-reflection sector.  L166 transfers the one-reflection row to the dual, and sparse support kills both ordinary singular-Hessian terms.  This leaves exactly L156's differentiated endpoint/cofactor functional.  Exact grade-four bivariate algebra confirms cancellation but also shows six nonzero terms, so termwise vanishing is false. `proof/crabb_all_grade_normal_bridge.md`; two exact checkers. |
| A120 | F/G | Evaluate L171's final endpoint functional rather than assuming positive Hardy projection | **PROVED ALL-SIZE RESIDUE KERNEL (L172)** | Cauchy functional calculus makes the row `[z^(−1)] dot(B)_(w_k) dot(R)_s`.  L140 supplies Blaschke powers `L−k,L+k`; L168--L170 turn the relative discrepancy into the absolute residual power `1−k`.  Their only products are `L−2k+1` and `L+1`, both positive for `k<=L/2`, so the residue is zero.  The central fold gives power one.  A separate full bilinear-jet calculation gives exact higher-grade zeros and preserves the grade-one value `−4(5L−1)/L`. `proof/crabb_dual_endpoint_residue.md`; both dual checkers. |
| A121 | F/G | Control A111's genuine `sqrt(Q)` circular-normal gradient by the exact joint Schur complement rather than an `O(Q)` shortcut | **SHARP HOMOGENEOUS APEX FACE PROVED (L173); GAUGE-AUDITED; NONLINEAR TUBE NEXT** | The quadratic gradient factors through `h wedge J conj(h)` and its support-mode rows lie on disjoint Plücker anti-diagonals.  After mapping coefficient-gauge support directions into L65's physical Crabb gauge, the curvature splits into a flux term exactly equal to the `512` threshold and a strictly positive rank-one null-lift term.  Thus every scalar ratio remains `<512`, exactly what the `−32Q` disk face requires.  This repairs both the low-size false cancellation and an initially omitted gauge conjugacy while preserving the theorem.  Transport/compact blow-up over positive equality anchors is still required before claiming a neighbourhood theorem. `proof/crabb_circular_normal_plucker_schur.md`; exact and floating checkers. |
| A122 | F/G | Promote L173's homogeneous Schur margin to a nonlinear tube by analytic fibre maximization and singular-cone blow-up | **PROVED ON TOEPLITZ DISK SLICE (L174); FULL CIRCULAR-RANGE BASE OPEN** | The transported true-normal Hessian remains uniformly negative, so the analytic envelope has a unique maximizing graph.  L123/L162 pin that graph to zero with first-order contact on the entire equality cone.  The Schur-maximized quartic retains a strict `−cQ` margin by L173; L155/L124 then absorb every higher analytic term even on paths approaching the singular cone faster than radially.  This gives `Gamma<=−aQ−b||y−y_*||²` on the Toeplitz disk × coercive-normal chart.  It deliberately does not claim a tube over all non-Toeplitz circular-range anchors or include the elliptic/compact coordinates. `proof/crabb_disk_circular_normal_tube.md`. |
| A123 | F/G | Identify the full-H quartic cancellation and test the first genuine normal face after exact-disk recentering | **LEADING IDENTITY PROVED (L176--L177); LOW-SIZE NEXT FACE PASSED** | Reconstructing the full L65 Hessian and physical derivative of L122's general-H chart first found a gain equal to `32Q` in 16/16 records through `p=7`.  L176 proves the correction energy and L177 proves the complete ambient response, so the homogeneous face is exactly one negative square.  Recentered exact-disk paths show sixth-order base descent versus eighth-order circular-normal gain in the tested `p=5,6` sizes; all 36 residuals are positive.  A126 shows this exponent pattern does not extrapolate: cubic normal response begins at `p=7`, producing a genuine sixth-order Schur face. `proof/crabb_full_disk_weighted_face.md`. |
| A124 | F/G | Extract L175's disk-tangent maximizer explicitly and prove its curvature energy | **PROVED EXACT ISOMETRY (L176); RESPONSE SUPPLIED BY L177** | The correction is a size-independent interval-pulse transform of `h wedge J conj(h)`.  In each paired L65 mode its reduced vectors are nested path fluxes with exact inverse-curvature Gram `64I`; the terminal diagonal weighted-shift face gives `32I`.  Hence the correction energy is exactly `32Q` in every size.  L177 identifies the same interval vectors in the unprojected ambient response, completing the leading face. `proof/crabb_full_disk_correction_isometry.md`; exact checker. |
| A125 | F/G | Derive the complete full-disk ambient response rather than infer it from fitted Hessians | **PROVED COMPLETE RESPONSE IDENTITY (L177)** | The degree-two characteristic/reversed-Horner and first Riemann recurrences give an interval vector minus its zero-Fourier projection in every active paired mode; inactive modes vanish by circle character.  L176's inverse-curvature identity maps the explicit correction to that vector.  The terminal weighted-shift row is fixed by one adjoint chart equation and the radial gauge.  Thus `g_2=2C_pE_z` on the entire ambient space and the L175 face completes exactly.  Every coordinate is independently checked in exact arithmetic through `p=7`; reruns are byte-identical. `proof/crabb_full_disk_response_identity.md`; exact checker. |
| A126 | F/G | Extract the first nonlinear face after L176--L177 recentering and test its true-normal Schur complement | **ALL-SIZE SIXTH FACE PROVED BY L184; FULL NONLINEAR CLOSURE BY A134** | The canonical endpoint excess vanishes through degree five and is generically negative at degree six; exceptional strata start at degree eight.  Cubic normal response begins at `p=7`.  L179--L181 first closed the first two active complex sizes; L182 derived every response; L183--L184 identify the base deficit as a Hardy residual square and prove the arbitrary-size Schur inequality, including the sharper constant `9` at `p=7`.  L185--L186 classify and lift its kernel; A134 subsequently proves arbitrary-order first-residual absorption and the nonlinear tube. `proof/crabb_full_disk_sixth_hardy_factor.md`. |
| A127 | F/G | Resolve A126's sharp terminal two-coefficient edge before attacking the full sixth-order tensor block | **PROVED ALL-SIZE TERMINAL FACE (L178); GENERAL BLOCK OPEN** | For `k=L−3` and only `z_k=a,z_(k+1)=b` nonzero, the exact base deficit is `256k²|a|⁴|b|²/(k+2)²`, the sole cubic response is `16k(4k−1)a²conj(b)/(L²(k+2))`, and L173's curvature gives gain/base ratio `6(4k−1)²/[6(4k−1)²+169k(k−1)(k−2)]<1`.  Thus the real `p=7` constant-9 equality edge is strictly safe for the actual Schur face, with a margin supplied by the same null-lift term as L173.  The formula is exactly regenerated through `L=15`; the remaining target is the full anti-diagonal block decomposition on `z tensor (z wedge J conj(z))`. `proof/crabb_full_disk_terminal_sixth_face.md`. |
| A128 | F/G | Convert the floating real `p=7` Gram evidence into an exact facially reduced certificate | **PROVED EXACT REAL FACE (L179); COMPLEX FACE LATER L180** | The real phase-palindromic union has a 21-dimensional cubic equality ideal.  On its explicit binomial basis, the required polynomial `P_6−1089C_6²/290` equals `q^TQq` for an exact rational rank-seven matrix `Q=FS^(−1)F^T`.  The seven displayed leading principal minors of `S` are positive, proving `Q>=0` without a numerical solver.  This closes every real direction in the first active size; L180 separately controls complex directions.  The sharper constant `9` and arbitrary-size theorem remain open. `proof/crabb_full_disk_real_sixth_certificate.md`; exact checker. |
| A129 | F/G | Lift the first-active sixth face to complex tensor coordinates without assuming the real Gram complexifies | **PROVED EXACT COMPLEX FACE (L180); ALL-SIZE OPEN** | A naive Hermitian lift of L179 fails because the target has nonzero global-phase `±2` sectors.  A generic-complex characteristic/Riemann audit first proves that mode three is the only cubic normal mode and equals `−22C_6/45`.  On the invariant tensor `z tensor (z wedge J conj(z))`, exact coefficient reduction then leaves a rational PSD family.  Its clean rank-ten endpoint is six squares in the real parts of twenty selected Pluecker-tensor coordinates plus four squares in their imaginary parts; independent endpoint recurrence matches all 137 polarized coefficients.  This proves the required complex `p=7` inequality, not the sharper constant `9` or the arbitrary-size anti-diagonal block theorem. `proof/crabb_full_disk_complex_sixth_certificate.md`; two exact checkers. |
| A130 | F/G | Test whether L180's complex grade-block SOS persists in the second active size without trusting dense symbolic expansion | **PROVED EXACT COMPLEX `p=8` FACE (L181); ALL-SIZE RECURRENCE NEXT** | A dense six-variable characteristic expansion grew past 9 GB without completing, so exact homogeneous polarization replaced it.  The 364-point sparse set has full cubic evaluation rank; 4,368 exact normal-polarization evaluations prove the two response formulas and all inactive-mode cancellations.  Exact tensor facial reduction then yields a fifteen-square certificate on 37 coordinates, with real/imaginary ranks `9+6` and all 299 coefficients matched independently.  The `L=6,7` factors share literal tail vectors and grade blocks, strongly indicating an interval-flux LDL recurrence.  Do not continue a blind fixed-size SOS sequence: derive that arbitrary-size recurrence. `proof/crabb_full_disk_complex_L7_sixth_certificate.md`; two exact checkers. |
| A131 | F/G | Replace the fixed-size cubic-response tensors by one arbitrary-size interval-flux transform | **PROVED ALL-SIZE RESPONSE FORMULA (L182); BASE CLOSED BY L184** | Writing `S_t=sum_(i<t−i)(t−2i)W_(i,t−i)`, the full mode-`k` response is `16(4k−1)/L²` times the two circle-selected triangular sums of `(a−t−1)z_aS_t/(t+2)` and its conjugate.  The formula comes from a one-step coefficient recurrence after inserting L176's mean-zero interval pulses, not from fitting dimensions.  It specializes to every earlier response and independent exact recovery at `L=9` has full ranks `11,10,9,8`.  L183--L184 subsequently factor the base deficit through a cubic Hardy residual and dominate this transform by anti-diagonal Cauchy--Schwarz, so a rational LDL is unnecessary. `proof/crabb_full_disk_cubic_response.md`; exact checker. |
| A132 | F/G | Factor the sixth-order base deficit through the disk-chart Hardy model instead of constructing a large interval LDL | **PROVED ALL-SIZE SIXTH FACE (L183--L184); KERNEL CLOSED BY L185--L186** | Berger/Bessel observability gives `M≤4hK` with an exact Hardy orbit-complement defect.  Along L176's path the first surviving residual is a cubic skew matrix `R_L`, so `D_(6,L)=8||R_L||²`.  L182's response coordinates are weighted projections onto disjoint anti-diagonals of `R_L`; Cauchy--Schwarz is exactly strong enough for L173's flux curvature, and its null-lift term supplies strictness whenever a response survives.  This proves the arbitrary-size sixth face without a fixed-size SOS or rational LDL.  L185--L186 subsequently classify `R_L=0` and prove its eighth-order fallback. `proof/crabb_full_disk_sixth_hardy_factor.md`; exact checker. |
| A133 | F/G | Classify L184's cubic equality variety before attempting a blind eighth-order tensor expansion | **PROVED KERNEL + EIGHTH FALLBACK (L185--L186); NONLINEAR GATE CLOSED BY A134** | The cubic residual is an explicit weighted Wronskian two-form.  Its zero equations reduce to `pq'−p'q=cx^(n−2)`, so a two-critical-point rational-map classification leaves only the phase-palindromic cone and one-reversal-pair planes.  On each pair plane the fourth Hardy residual is one central anti-diagonal entry, giving the exact positive eighth deficit `512d²(|a|²−|b|²)^4/L²`; all quartic true-normal responses vanish.  A transverse second-order correction can cancel that fourth residual, so this finite ladder is not itself uniform; A134's full Hardy equality graph and first-residual theorem supply the correct nonlinear closure. `proof/crabb_full_disk_kernel_eighth.md`; exact checker. |
| A134 | F/G | Replace the weighted pair kernel ladder by the full nonlinear Hardy residual and its equality graph | **PROVED FIRST-RESIDUAL TUBE (L187--L189); EQUALITY CODOMAIN REPAIRED BY L193; FULL CHART CLOSED BY A135** | Exact row elimination now identifies the nonlinear zero set as `H^−1` Hermitian Toeplitz, repairing the earlier shortcut that treated a merely first-order-Hermitian raw residual as an exact Hermitian-codomain map.  Residual zero gives both canonical condition square four and characteristic-Blaschke norm two, plus full normalized ambient stationarity.  Hadamard division makes every leading normal response a weighted skew projection of the first residual in arbitrary order; its Frobenius square absorbs all projections by L173's flux/null curvature.  Curve selection proves the nonlinear general-H disk/circular-normal tube. `proof/crabb_full_disk_leading_residual_tube.md`; L193 gives the exact all-block reflection identity. |
| A135 | F/G | Merge A134's general-H first-residual tube with the elliptic/marked compact chart | **PROVED COMPLETE SINGLE-CRABB LOCAL THEOREM (L190--L192)** | The unrestricted characteristic coefficients give analytic coordinates on the full Hardy equality manifold and the Faber lift has independent reflected legs `r=c^L,w_j=c^ju_j`.  Defect-one innerness and the model-complement gap give a strict compact face over every equality anchor.  The missing residual/reflection row vanishes because L156's full normalized endpoint-residual first jet is zero at the apex; exact optimized recurrences confirm the resulting sum of negative squares.  L188, L160, L163, and L120 then make the simultaneous residual/reflection/true-normal associated face strict without spending one curvature mode twice.  Curve selection closes the full fixed-size single-block operator chart.  Next lift through repeated-block metric flags; do not claim the global conjecture. `proof/crabb_full_local_chart_merger.md`; `experiments/crabb_full_equality_elliptic_merger.py`. |
| A136 | F/G | Replace the direct-sum repeated equality stratum by the natural operator-valued Hardy residual manifold | **PROVED EXPLICIT INVERSE-BLOCK-TOEPLITZ MANIFOLD (L193); REPEATED TUBE OPEN** | Normalizing the rank-`m` defect removes a spurious copy-space condition factor.  More importantly, exact row elimination factors the entire interior defect through the adjacent-principal difference of `H^−1`, so the equality graph is explicitly `H=Toep(Z_0,...,Z_(L−1))^−1`, not merely an IFT graph.  Arbitrary noncommuting coefficient blocks show scalar diagonalization is the wrong repeated equality model.  The next attack is an exact analytic repeated metric chart followed by an operator-valued first-residual/normal face and descending copy-space flag.  Do not infer scalar lower equality at the noncommuting points or substitute a noncommuting copy matrix into scalar elliptic formulas. `proof/crabb_block_hardy_equality.md`; `experiments/crabb_block_hardy_equality.py`. |
| A137 | F/G | Generalize L105's exact repeated-`C3` metric chart to every Crabb length before expanding operator-valued endpoint faces | **PROVED ALL-LENGTH ANALYTIC CHART (L194); ENDPOINT SIGN OPEN** | The lower Schur formula is unchanged.  At the general Crabb metric, the Stein range Jacobian is a weighted diagonal-difference recurrence whose boundary values start on the first range row/column; forward propagation is an explicit inverse in all `L,m`.  Hence the IFT gives a convergent metric for arbitrary nearby operator, free level-zero/range row, and prescribed Stein slack.  All constraints reduce to one final `m x m` upper endpoint.  This removes formal-series convergence debt but does not select the free row or control the endpoint.  Next express L193's normalized Hardy certificate in this chart and compute the operator-valued first-residual endpoint/flag. `proof/repeated_crabb_exact_metric_chart.md`; `experiments/repeated_crabb_exact_metric_chart.py`. |
| A138 | F/G | Identify L193's canonical Hardy metric inside L194 and compute the first operator-valued upper endpoint | **PROVED ZERO-SLACK BRIDGE + COPY-GRAM FACE (L195); FLAG MERGER OPEN** | Rank-`m` Stein forcing with invertible level-zero block has identically zero Stein Schur slack, and the exact lower endpoint gives zero lower slack, so chart uniqueness identifies the canonical metric with `P(T,B,0)`.  Hardy quotient projection then upgrades L188's scalar `−4||F||_F²` to the matrix face `−4 sum F_(r,c)*F_(r,c)`.  Its kernel is the common right kernel of all residual blocks, giving the correct descending copy flag without optimizing another metric.  Next prove stable-kernel promotion into a smaller-copy L193 block and merge L188's circular-normal null lift plus the elliptic matrix-Jensen alternative. `proof/repeated_crabb_first_residual_endpoint.md`; `experiments/repeated_crabb_first_residual_endpoint.py`. |
| A139 | F/G | Promote L195's common residual kernel using the explicit inverse-block-Toeplitz equality coordinates | **PROVED FIRST KERNEL PROMOTION (L196); SCHUR-ORTHOGONAL INDUCTION OPEN** | Subtracting block-diagonal means makes `N=H^−1−Toep(H^−1)` an exact linear transverse coordinate.  Its adjacent-principal difference is invertible by diagonal integration.  Because the base residual is that difference times a block reversal, a copy kernel of the residual Gram forces both columns and rows of the leading `N` coefficient to vanish on the same copy space.  Hence recentering at the Toeplitz inverse Gram raises the transverse order on the kernel and its cross blocks.  The next step must identify the residual after Schur orthogonalization against earlier active Hardy ranges; do not claim the full flag induction yet. `proof/repeated_crabb_inverse_gram_kernel.md`; `experiments/repeated_crabb_inverse_gram_kernel.py`. |
| A140 | F/G | Replace order-by-order repeated residual promotion by the analytic Schur flag of the exact nonpositive endpoint | **PROVED FINITE DISK FLAG (L197); NORMAL/ELLIPTIC MERGER OPEN** | For any analytic arc, `−E` is an analytic PSD copy matrix.  Split the range of its first coefficient, eliminate its cross block by an analytic triangular congruence, and repeat on the positive Schur complement.  Valuations strictly rise and dimension strictly falls, so at most `m` steps occur; an identically zero terminal block is an exact analytic kernel bundle.  This gives precisely the later residuals orthogonalized against all earlier active Hardy ranges and avoids demanding a new inverse-Gram formula at each order.  The disk residual induction is complete.  L199 absorbs normals on the first raw residual face; lifting that argument through later Schur quotients and merging the elliptic marked face remain. `proof/repeated_crabb_schur_flag.md`; `proof/repeated_crabb_circular_jensen.md`. |
| A141 | F/G | Completely amplify L188's circular-normal absorption without first using support-Jensen scalarization | **ONE-SIDED VERSION DISPROVED; JORDAN VERSION CONDITIONAL AND UNNEEDED (L198)** | The naïve gain `16P*P/C` is false: a square-zero copy matrix makes its residual indefinite.  The symmetric expression `8(P*P+PP*)/C` is absorbed exactly by the reflected Hardy Gram, but identifying that expression with the actual repeated endpoint would require an independent ordered block-jet calculation; scalar L173/L188 does not suffice.  Do not stack work on that unproved identification.  A142/L199 avoids it by proving that every normal coefficient surviving the earlier Jensen gate is scalar. `proof/repeated_crabb_circular_jordan_absorption.md`; `experiments/repeated_crabb_circular_jordan_absorption.py`. |
| A142 | F/G | Use support-Jensen scalarization to avoid a false noncommutative lift of the first circular-normal face | **PROVED FIRST RAW FACE (L199); LATER FLAG RESPONSE OPEN** | L61 supplies the missing alternative: a nonscalar true-normal copy coefficient has a positive first-support Jensen gap.  On a zero-Jensen maximal winner, independent normal Fourier profiles force every coefficient to be scalar and remove winner/loser cross rows.  The first raw block residual has Hardy reflection `F_ab=F_(L−b,L−a)*`; every copy state is therefore an admissible scalar L188 residual, with its energy bounded by L195's copy Gram.  Equality kills all normal amplitudes and reduces the first residual kernel, exactly as L196 requires.  Later L197 quotients are Schur-orthogonalized against earlier active ranges, so their reflected form and the absence of mixed analytic normal response still need proof.  The elliptic marked face also remains. `proof/repeated_crabb_circular_jensen.md`; `experiments/repeated_crabb_circular_jensen.py`. |
| A143 | F/G | Lift L190 to noncommuting inverse-block-Toeplitz equality anchors by applying Faber reflection coefficientwise to a matrix companion polynomial | **FALSE AS STATED (L200); USE GENUINE BLOCK COLLIGATION/SCHUR TRANSFER** | The coefficientwise Dickson/Faber identity is valid, but the load-bearing scalar quotient is not.  At repeated `C3`, a square-zero copy coefficient gives exact positive block-Toeplitz equality data for which both `G(G#)^−1` and `(G#)^−1G` fail unitarity on the circle.  Thus there is no matrix-inner logarithmic variation or model-complement theorem for the raw quotient.  L114 already proves the repeated-`C3` neighbourhood by another metric-stratification route, so this is not evidence against the bound; it blocks only the proposed all-length shortcut.  Construct the actual rank-`m` colligation transfer furnished by L193 (equivalently, a matrix Schur/Levinson factorization) and only then identify reflected Gram legs. `proof/repeated_crabb_matrix_faber_obstruction.md`; `experiments/repeated_crabb_matrix_faber_obstruction.py`. |
| A144 | F/G | Replace A143's false raw quotient by the characteristic transfer of L193's canonical partial isometry | **PROVED GENUINE INNER/FABER GRAM (L201); PREPARED ENDPOINT BRIDGE NEXT** | The canonical equality metric makes `C=M^(1/2)AM^(−1/2)` a pure partial isometry with defect frames `V,W`.  Its true transfer `B_H=W*(I−zC*)^−1V` is matrix inner and has no constant coefficient.  Dickson reflection is therefore exact for its convergent Taylor series, producing the oriented Gram `sum|c|^(2n)B_n*B_n`; at the apex only the unitary coefficient `B_L` survives, and nearby it stays invertible, giving uniform terminal coercivity.  This is the correct noncommutative reflected coordinate system.  Next derive the rank-`m` analogue of L149's total one-reflection identity and L150's orbit-complement endpoint so that this Gram is an actual similarity loss, not merely an inner-model energy. `proof/repeated_crabb_inner_faber_transfer.md`; `experiments/repeated_crabb_inner_faber_transfer.py`. |
| A145 | F/G | Derive the physical elliptic first jet directly from the exact canonical equality metric before attempting all reflected grades | **PROVED GRADE-ONE NORMAL CORNER (L202); ENDPOINT SIGN/HIGHER GRADES OPEN** | Block-Toeplitz elimination gives `P=2I−VV*+2WW*`.  Both active right/left Gram derivatives of the balanced ellipse tangent vanish, but an adversarial tangent-space audit caught the omitted defect-to-defect corner: the two Gram identities alone do not imply full partial-isometry tangency.  Exactly `Z_gamma=W*dot(S)V=4gamma B_1`, with `B_1` L201's genuine transfer coefficient, so the noncommutative normal energy is `16|gamma|²B_1*B_1`.  This repairs the overclaim and identifies the physical grade-one reflected coordinate.  The remaining task is to convert that normal Gram into the negative model-complement endpoint and lift the same mechanism through higher transfer coefficients. `proof/repeated_crabb_elliptic_first_jet.md`; `experiments/repeated_crabb_elliptic_first_jet.py`. |
| A8 | B | Frame-free level-4 stationarity laws / PSLQ closed-form hunts | DEAD | Law is frame-coupled; algebraic only after adjoining p_ij, δ (L15 explains) |
| A9 | B | Soft D2 classes: free univalent / convex / odd+G'-increasing | ALL FALSIFIED (certified) | D2_landscape.md; Koebe 1+δ²; z+tz² exact; odd+G'↑ via linearized step-deficit + 40-dps counterexample. 5th instance of the no-soft-proof meta-pattern |
| A10 | B | EL4 deformation path: dV/dk ≤ 0 with Möbius-Jacobian kernel on elliptic velocity field | SUPERSEDED — EL4 PROVED | Gate was correct, but Schwarzian comparison is stronger and shorter: `SG≥0 ⇒ D2`; squared-ellipse `SG≥0` is a positive Weierstrass Fourier series. `proof/el4_schwarzian_theorem.md` |
| A11 | B | Schwarzian/Sturm comparison for D2 | PROVED; ACTIVE FOR BI-CONIC | Closes EL4 analytically. Next test whether the off-slice bi-conic collapsed map has `SG≥0` on its critical real interval; if not, use the more general potential ordering directly |

## Methodological pitfalls discovered (Track G)
- P1. Offset-curve construction silently breaks on matrices with nonsmooth W(M) (near-normal, corners/flats): FFT ringing → garbage integrals that still satisfy holomorphic-identity checks. FIX: certify positivity of P(σ) pointwise + convex tangent winding + mass 2I + Cauchy reconstruction; reject otherwise. COVERAGE GAP: such matrices currently unsearchable — needs smoothed support-function domains.
- P2. Background process hygiene: stale processes with old code contaminated logs → phantom "violations". FIX: harness-tracked jobs only; verify claims by re-evaluating saved records with current code.
- P3. Catastrophic cancellation: optimizer drives poly coefficients to ~1e50 with boundary cancellation; consistency certificates fooled (both sides share garbage). FIX: theorem guards — computed ‖p(M)‖ > 1+√2 or ‖γ_Φ(p)x₀‖ > 2 under certified hypotheses PROVES numerics lie (they contradict established theorems) → reject. Plus coefficient-magnitude cap.
- P4. Near-singular layer-potential quadrature (outer→inner contour, gap ε/2): N=1024 insufficient; passed all other certificates while Φ values were wrong (fake viol +0.358 at n=3, killed at N=4096). FIX: unitality certificate Φ(1)=1 through the same kernel + N-vs-2N stability check.
- P5. **Family-restricted optimizers miss the global phase** (found 2026-07-21): scalar α-searches
  inside one Blaschke family (e.g. odd {0,±α}) can converge to non-global stationary points —
  the true extremal may be in another phase (even {±α}, or Möbius = odd family's α→1 endpoint).
  FIX: always cross-check with the unrestricted best_extremal solver (deg 1..n−1, multistart);
  boundary-hugging zeros (|z| ≈ 1) in its output are removable (unimodular factors), not real.
  Related DATA BUG: sym4_sweep_s61.jsonl OTHER-branch records have broken taus (τ₂ ≡ 0) and
  inconsistent f0e — recompute; the ρ/K/qs columns are fine.
- P6. **Inadmissible probe families in map-class falsification** (found 2026-07-21): test maps
  built from tanh/step smoothings can have poles INSIDE the disk (tanh(κ(z²−t₀²)) poles at
  z² = t₀²+iπ/(2κ)) — huge "violations" from non-analytic non-univalent junk. FIX: before
  trusting any V > 1, verify analyticity (pole locations) and univalence (Re G' > 0
  Noshiro–Warschawski, or boundary-curve simplicity). Entire (polynomial) smoothings +
  explicit Re G' bounds give certified counterexamples (D2_landscape.md).
- P7. **Certification capability gap at n ≥ 6 (dense)** (found 2026-07-21): the true-extremal
  criterion diag = |⟨f₀(A)x₀,x₀⟩| < 1e-5 is currently UNREACHABLE for arbitrary dense 6×6
  matrices — both find_extremal_blaschke and best_extremal stall at diag ~1e-3 on the deg-5
  Blaschke landscape (10 params), so any acceptance-gated loop (e.g. Hr_adversarial's uncapped
  `while cur is None`) spins forever at 100% CPU with zero output (n=6 s42 job killed after
  2h04m / ~500 silent rejections). FIXES: (i) cap rejection loops + log every rejection with
  (K, diag); (ii) at n ≥ 6 use STRUCTURED families (zero-diag tridiagonal: graded collapse
  reduces extremality to small blocks — certifiable, and they are the G–O-hard configurations
  anyway); (iii) or upgrade the solver (analytic gradients / phase-aware polish) before any
  dense-n≥6 campaign.
- P8. **A stronger sufficient target needs its own falsification gate.** L20/L21 prove a
  complete-bounded similarity statement, and L44 is stronger still than the exact small-gain
  inequality. Before investing in a sharp-edge certificate, test the similarity constant on
  general matrices and audit L44 at high precision in its equality corner. Failure of either
  stronger target is not evidence against scalar Crouzeix.
- **Meta-pattern (important):** every relaxation of extremality so far admits sharp counterconfigurations: abstract lemma (R–S, non-unital α), domain-only constants (MMOR, thin quadrilaterals), scalar localization (L10, odd-symmetric mechanism — ours), free-map S ≤ 1, soft D2 classes (convex; odd+G'↑). Conjecture-strength inequalities must engage TRUE extremal/critical structure (global maximality of f₀ + positive extremal measure + critical domain), not just its first-order shadows.

## Epoch 2 outcome
2×2/ψ-domain exact extremal machinery built (extremal_pullback.py). Empirical laws at true extremals: c small (0 on disk, ~ε²-ish off), G ≈ |β|, ReC ≤ 0 generic, L7@ext holds broadly. L12-strong false.

## Epochs 4–5 outcome (2026-07-20/21) — see RESEARCH_STATE.md NEWEST + proof/slice_closed_form.md
H-r reduction anchored (A4); 2×2 and sym3 PROVED in closed form (A5, A6 — Landen theorem);
level-4 closed-form theory built (A7: L15 plus conditional L16 structure); EL4 subsequently
proved by A11; soft-class hunts all falsified with certificates (A8, A9). Adversarial H-r floors
positive n=3..5; direct ratio searches n≤7 found no violation.

## Epoch 3 (historical): P2 target, audit-adopted (chatgpt/FABLE_RESEARCH_AUDIT.md — verified before adoption)
P1 (proved): K² + ReC ≤ Kq. P2 (target): q ≤ 2 + ReC/2 at extremals ⟹ Crouzeix. See proof/P2_target.md.
Attacks: (i) falsification-first: exact 2×2, Ω = W(A) ellipse (elliptic Riemann map) + ψ-family adversarial ascent on −s_phase = q − 2 − r/2; (ii) if P2 survives: Euler–Lagrange at extremals (Blaschke-zero variations + singular-vector stationarity) → Stinespring leakage bound; (iii) fallback: pre-Cauchy–Schwarz angle form of P1.
Audit adoptions verified by hand: P1/P2 algebra, Stinespring identities (D), D2-exactness, SV24 §6 baseline (ρ = ReC, K²+ρ≤2K — our β-subtraction is the increment). Audit hygiene items fixed (ledgers synced, 2410.10678 corrected, run tables filled).
