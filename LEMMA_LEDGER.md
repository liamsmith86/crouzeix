# LEMMA_LEDGER.md

Candidate lemmas. Status ∈ {PROVED, DISPROVED, NUMERICAL, HEURISTIC, UNRESOLVED}.
Every status change requires: precise hypotheses, tests on small/defective/highly-nonnormal matrices, equality cases, dimension-dependence check.

| ID | Statement | Status | Evidence / notes |
|---|---|---|---|
| L0 | (C–P 2017) For convex Ω ⊇ W(A), f ∈ A(Ω): ‖f(A) + g(A)*‖ ≤ 2‖f‖_{∂Ω}, where g = C(f̄) conjugate Cauchy transform | PROVED (literature) | Positivity of operator double-layer kernel; total mass 2 |
| L1 | (C–P abstract lemma) ‖a+b*‖≤2, plus contractive f↦g structure ⇒ ‖a‖ ≤ 1+√2 | PROVED (literature); SHARP abstractly (R–S) | Cannot improve without concrete structure |
| L2 | Normal A: R(A,p) ≤ 1 | PROVED (classical) | spectral theorem + σ(A) ⊆ W(A) |
| L3 | Exact energy identity K² = ⟨γ_Φ(f₀)x₀, γ(f₀)x₀⟩ − conj(∫Φ(f₀)f₀dμ) at extremal pair | PROVED | Algebra from S–dV Prop 1/3 + multiplicativity. proof/refined_master_inequality.md |
| L4 | Free scalar subtraction: ⟨γ(f₀)x₀,(γ_Φ(f₀)−ω₀)x₀⟩ independent of ω₀ ∈ C | PROVED | Needs K>1 (Prop 1) |
| L5 | W² ≤ 2∮\|f₀\|²dν, ν=⟨Px₀,x₀⟩ds; W=2 ⟹ \|f₀\|=1 ν-a.e. | PROVED | Double Cauchy–Schwarz on PSD operator measure |
| L6 | K² ≤ K√(W²−\|β\|²) + c at extremal pair (K>1) | PROVED | Strictly refines S–dV Thm 5 (verified numerically: gap ≥ 0 always, =0 at exact extremality) |
| L7′ | √(W²−\|β\|²) ≤ 2 − c/2 whenever ⟨p(M)x₀,x₀⟩=0, ‖p‖_Ω ≤ 1, Ω convex smooth ⊇ W(M) | NUMERICAL (no verified violation; adversarial searches produced only quadrature artifacts, all killed by strict re-eval) | Suspect TRUE but possibly not provable without extremality; central variant is L7@extremals |
| L7@ext | √(W²−\|β\|²) ≤ 2 − c/2 at TRUE extremal pairs (f₀,x₀) with K>1 | **UNRESOLVED — CENTRAL TARGET** | ⟹ Crouzeix (with L6). Holds at every computed true extremal so far (slacks +0.11…+0.76) |
| L10 | Scalar reduction: 2∮\|p\|²dν − \|∮p̄wdσ\|² ≤ (2−c/2)² for admissible scalar (w,p) | **DISPROVED (2026-07-20)** | Odd-symmetric mechanism: ellipse b=0.6, η=(δ_x+δ_{−x})/2-Cauchy, p odd near-unimodular deg 5; viol +0.33, clean certificates. Mechanism: Φ(p)(x) ≠ 0 off-disk while L5-slack W² < 2∮\|p\|²dν is REAL operator structure (V*-compression loss). Lesson: scalar data (ν,η) alone insufficient — operator alignment obstruction essential |
| L8 | No ⊕-unstable (global-operator-norm) sufficient condition can prove the conjecture | HEURISTIC | Formalize: for any candidate bound B(M) with B(M₁⊕M₂) ≥ B-degradation... make precise before use |
| L9 | Saturation rigidity: W=2 ⟹ P^{1/2}(σ)y = e^{−iψ}f₀(σ)P^{1/2}(σ)x₀ a.e. (y = normalized γ_Φ(f₀)x₀) | SUPERSEDED by Stinespring defect identities (proof/P2_target.md) | Exact: 4−q² = 4[‖(I−Q)Uξ‖² + \|⟨Uξ,ξ⟩\|²] |
| P1 | K² + ReC ≤ Kq, q = √(W²−\|β\|²), at extremal pairs (K>1) | PROVED (2026-07-20, phase-preserving refinement of L6; verified vs SV24 §6 which has K²+ρ≤2K) | β-subtraction is the new content over arXiv:2409.15954 Thm 6.1 |
| P2 | q ≤ 2 + ReC/2 at extremal pairs | UNRESOLVED (Epoch-3 route, superseded as central by H-r — still a valid parallel refinement) | ⟹ Crouzeix via (K−2)(K−r/2) ≤ 0. Difficult case r<0 (generic off-disk). Equivalent to Stinespring leakage bound. Sharp at Crabb |
| L6/L7@ext | (former central target) | SUPERSEDED by P1/P2 (audit-adopted: modulus wasted the ImC phase) | All L7@ext numerical support transfers a fortiori to P2 |
| L13 | Clark-type transition: ⟨q(A)x₀,u₀⟩ = K∫q·conj(f₀)dμ ∀q ∈ A(Ω) (Ω smooth simply conn. = Dirichlet; K>1 extremal pair) | PROVED (2026-07-20): additive-perturbation stationarity gives positive rep. measure ρ̂ with density; q = f₀r shows ρ̂, μ share analytic moments; Dirichlet-uniqueness ⟹ ρ̂ = μ | CAUTION: corollary "resolvent diagonal = Cauchy(μ) at eigenvalues" is FALSE (f₀/(z−λⱼ) ∉ A(Ω)) — do not reuse |
| L14 | sym3 three-factor: ρ = α²(g₀(e)−g₀(0))·Re⟨P₀x₀,x₀⟩, P₀ = I−A²/e² | PROVED for doubly-symmetric 3×3 (algebra verified numerically) | H-r(sym3) ⟺ sign correlation; interlacing conjecture: g₀(0) ≤ 0 ≤ g₀(e) at criticality (sweep-supported) |
| L15 | Level-4 nodal closed form: K² = (T+√(T²−4δ²F₁²F₂²))/2, T = p₁₁F₁²+2p₁₂F₁F₂+p₂₂F₂², p_ij = tr(Q_iᵀGQ_j), δ = det A₊; frame identities c_j = (T_j+S_j)/(4K), ΣF_jc_j = K; odd stationarity law Σ_j(T_j+S_j)τ_j(1−τ_j⁴)/(e_j(1−α²τ_j²)²) = 0 | PROVED (2026-07-21; trivial algebra + Hellmann–Feynman; verified 1e-16 / 1e-10) | Explains PSLQ negative: law algebraic only after adjoining (p_ij, δ). proof/slice_closed_form.md §2–3 |
| L16 | Even-phase structure: extremal f₀ = ±b_v∘(φ²) with v = α² = pseudo-hyperbolic midpoint of (τ₁²,τ₂²); q₁ = q₂ = 1/2; ρ = 1 − Θ explicit | **PROVED (2026-07-21)** | Midpoint stationarity + involution identity in `slice_closed_form.md`; globality for the full two-node Schur/Pick body proved in `proof/even_pick_globality.md` |
| EL4 | Θ(k,U₂) ≤ 1 on (0,1)×(0,K) (even-phase level-4 elliptic inequality; U₂→0 edge = Landen theorem) | **PROVED (2026-07-21)** | Schwarzian-D2 lemma + `SG≥0` for `G(k sn²U)=sin²(sU)`; the sign is a positive Weierstrass Fourier series. `proof/el4_schwarzian_theorem.md`; regression check `experiments/el4_schwarzian_check.py` |
| L17 | If `G'>0` and `SG≥0` on a real node interval, then the two-node distortion functional D2 is ≤1 | **PROVED (2026-07-21)** | Hyperbolic coordinate gives `S(G∘tanh)≥−2`; ground-state/Dirichlet-form comparison with constant potential `−1` gives the sharp `2 coth D` bound. Equality model: Möbius maps |
| L18 | For complementary rank-one idempotents and two real Pick nodes, `sup ||F(u₁)Q₁+F(u₂)Q₂|| = max(1,d||Q₁−Q₂||)`; if the second term exceeds 1, the midpoint automorphism is the unique nonconstant maximizer up to phase | **PROVED (2026-07-21)** | Pick-boundary reduction + exact determinant along the full complex automorphism orbit. `proof/even_pick_globality.md`; regression `experiments/even_pick_globality_check.py` |
| L19 | Z₂ symmetry of the slice guarantees a definite-parity global extremal | **DISPROVED NUMERICALLY (2026-07-21)** | Slice `(.8,2.4,1.1,.3)`: shifted `b_β`, β=.65306547, K=1.569762 beats odd numerical 1.511514 and even global 1.489136; full degree-3 search collapses to the same degree-1 phase. `experiments/slice_phase_audit.py`. General symmetry alone was never a proof |
| L20 | The elliptic slice admits a contraction similarity with condition ≤2: for `T=φ(A)`, ∃ `I≤P≤4I` with `T*PT≤P` | **PROVED — COMPUTER-ASSISTED, ELLIPTIC 4×4 SLICE ONLY (2026-07-22)** | L21/L22 give the exact trace-cone/modal reduction. L27--L29 close one-block and rank-one/full faces; L40 bounds rank-one/rank-one witnesses by (RT); L41--L43 and L45--L59 prove (RT), with the final forced-regeneration log in `experiments/positive_tail_full_20260722.log`. Hence `W(S_a+cS_a^T)` is a complete 2-spectral set for arbitrary positive three-weight `S_a`, `0<c<1`. Not a theorem for arbitrary 4×4 matrices. `proof/slice_similarity_duality.md` |
| L21 | For strictly stable `T`, the optimal similarity-square is `t*(T)=max(1,sup_{Z≥0} tr(Z−TZT*)_-/tr(Z−TZT*)_+)` | **PROVED (2026-07-21)** | Slater holds via the discrete Lyapunov series; explicit SDP duality and positive-part minimization give the formula. If `JTJ=−T`, both primal metric and a sharp dual witness may be parity-block restricted. Dimension-independent. `proof/slice_similarity_duality.md` §1–2 |
| L22 | Every elliptic-slice `T=φ(A)` has an exact three-parameter modal form `(c,r,u)` with `tan v=r tan u`; its metric SDP is equivalent to four coupled 2×2 modal LMIs | **PROVED (2026-07-21)** | Derived from diagonal symmetrization, the SVD of the bidiagonal block, and odd functional calculus. Independent nodal reconstruction agrees to `8.9e-15`; primal/dual values agree to `1.1e-9` on defaults. `proof/slice_similarity_duality.md` §3–4; `experiments/slice_similarity_duality.py` |
| L23 | If the ellipse nome is `q=c²` and `k` is its modulus, then `k/c ≤ 4/(1+c²)²` | **PROVED (2026-07-21)** | Jacobi's modulus product plus the termwise log-convex inequality `(1+q^(2n))²≤(1+q^(2n−1))(1+q^(2n+1))`; sharp as `c→0`. `proof/slice_boundary_theorems.md` §1 |
| L24 | In the L22 parity form `T=[[0,B],[C,0]]`, the lower modal block always satisfies `||C||≤2` | **PROVED (2026-07-21)** | The 2×2 determinant is a reciprocal quadratic in `tan²u`, reducing all orientations to `tan²u=1/r`; L23 and only `r≤p=τ₂/τ₁≤1` finish two scalar cases. `proof/slice_boundary_theorems.md` §2–3 |
| L25 | The L20 similarity bound holds on the singular `c→0` face, sharply: `t*(T₀)≤4` | **PROVED (2026-07-21)** | `T₀` permutes to a nilpotent scalar weighted shift with weights `(2a,2b,2d)` and `b²=(1−a²)(1−d²)`; every consecutive product is ≤2, giving an explicit diagonal contraction similarity. Equality includes Crabb weights `(√2,1,√2)`. `proof/slice_boundary_theorems.md` §4 |
| L26 | In the L22 parity form, the upper modal block always satisfies `||B||≤2` | **PROVED, COMPUTER-ASSISTED SCALAR LEMMA (2026-07-21)** | The reciprocal determinant factors at `tan²u=1/r`; its upper root is a Möbius barrier. Positivity of the inverse ellipse map's odd coefficients gives a cubic minorant, reducing the theorem to three scalar nome inequalities. Exact rational bounds near `c=0` plus 13,500 outward-rounded algebraic interval boxes certify them. `proof/slice_upper_block_theorem.md`; `experiments/slice_upper_block_certificate.py` |
| L27 | Every modal contraction metric is parameterized by two PSD Stein defects through a two-node Szegő-kernel formula; any hypothetical coupled KKT optimum with `t*>4` lies on a rank-one/rank-one or rank-one/full defect face | **PROVED (2026-07-21)** | Eliminating the two contraction LMIs gives `H_o=K∘(Q_o+cΣQ_eΣ)`, `H_e=K∘(Q_e+c^{-1}ΣQ_oΣ)`. Complementary slackness gives `rank Z_i+rank Q_i≤2`; both dual blocks cannot be full, and L24/L26 close the one-block faces. The remaining coupled faces have `Q_o=aa^T`, `Q_e=bb^T`, allowing either vector to vanish. `proof/slice_coupled_defects.md`; floating-point KKT regression `experiments/slice_coupled_defects.py` |
| L28 | A generic sign-separated rank-one/full KKT boundary has exact dual value `sup_{a∈[-1,1]} ||B R_a(CB)||²` (or the parity swap); the swapped lower odd block obeys `||C R_a(BC)||≤2` | **PROVED REDUCTION + LOWER BLOCK (2026-07-21)** | Equality of two rank-two Gramians produces an orthogonal colligation and the Möbius quotient. Modal calculus identifies it with the odd Blaschke product `f_a(z)=z(z²−a)/(1−az²)`. The lower block follows by convexity in its two nodal values, L24 at equal-sign rectangle vertices, and a reciprocal-orientation estimate at opposite signs. `proof/slice_odd_block_reduction.md` |
| L29 | The upper odd block satisfies `||B R_a(CB)||≤2` for every elliptic-slice parameter and `a∈[-1,1]` | **PROVED — EXACT + DIRECTED-INTERVAL (2026-07-21)** | L30–L32 eliminate orientation and `a`, leaving two endpoint residuals on a branch. L33 closes `c≥12599/20000`; L36 closes `c≥1/12`; L37–L39 close the singular center, all of `c≤1/20`, and the remaining bridge. Together with the already-proved lower block, this closes L28's generic rank-one/full KKT face. `proof/slice_odd_block_reduction.md` (13at)–(13bg); three regenerating certificate scripts |
| L30 | The L29 determinant is equivalent to two signed equal-diagonal inequalities; one sign is globally closed and the other reduces to a two-variable quadratic discriminant at the two cubic-envelope endpoints | **PROVED REDUCTION (2026-07-21)** | For `M=[[α,β],[γ,α]]`, `||M||≤2 iff det(M)+2|β+γ|≤4` when `|det M|<4`. In the inner-node Blaschke coordinate `t`, each signed residual becomes a quadratic. The minus-sign quadratic is concave, hence L26 at `t=±1` closes it. The plus-sign quadratic needs only `4AC−B²≥0` when `A>0, |B|<2A`; `t` and the orientation are both eliminated. `proof/slice_odd_block_reduction.md` §3; regression `experiments/slice_odd_block_check.py` |
| L31 | L30's discriminant is a difference of one positive term and one square: `c²(1+r)²(4AC−B²)=16rg²p(1−c²)²(1−d²)−(Qr−S)²` | **PROVED IDENTITY (2026-07-21)** | Exact polynomial expansion; `Q=2g(1+c²p)−d(kp+4c)`, `S=−2g(c²+p)+d(kp+4c)`, and `1−d²=(1−k²)(1−k²p⁴)/(1−k²p²)²`. The expression is concave in `r`, so its conformal-envelope minimum is at a cubic endpoint. `proof/slice_odd_block_reduction.md` (13q)–(13t); exact SymPy audit in `experiments/slice_odd_block_check.py` |
| L32 | L31 has the cancellation-free form `c²(1+r)²(4AC−B²)=4[rF₁F₂−g²D²]`, with `D=(r−p)−c²(1−pr)+λ(1−r)` | **PROVED IDENTITY (2026-07-21)** | The auxiliary root discriminant factors as `p(1−c²)²(1−d²)−ab=F₁F₂/(4g²)`. At the cubic endpoints, `p−r` is evaluated directly, separating the small positive width from the small center displacement. Exact SymPy audit in `experiments/slice_odd_block_check.py`; formulas (13z)–(13ad) |
| L33 | The upper odd-block theorem L29 holds whenever `cℓ≥1`; in particular it holds for `12599/20000≤c<1` | **PROVED (2026-07-21)** | Convexity reduces the nodal rectangle to four vertices. Equal signs are L26. The opposite-sign determinant factors as `−16cF₋F₊/ℓ⁴`; `F₊>0`, while `F₋≤0` for `ℓ≥1/c` by its value and decreasing derivative at `1/c`. A five-factor product truncation proves `cℓ≥1` above `12599/20000` by an exact increasing-polynomial audit. Orientation then follows from the reciprocal quadratic. `proof/slice_odd_block_reduction.md` (13u)–(13y) |
| L34 | In L32's conformal variables, `F₁=cg(1+p)G₁(p)/(1−c²g⁴p²)`, where `G₁` is quadratic, `disc G₁=−g²H`, and its exact vertex is `p*=(g²−4c²)/(2g²(1−2c²g))` | **PROVED IDENTITY (2026-07-21)** | Completing the square gives (13ag), with `p*=1/2+3c²/2+O(c⁶)`. This identifies the small-nome sharp ridge and supplies the stable coordinate `p=p*+c⁴x`; L35--L39 later supply the needed sign and finish L29. Exact SymPy audit in `experiments/slice_odd_block_check.py`; formulas (13ae)–(13ag) |
| L35 | L32's upper and lower cubic-envelope residuals are strictly positive for `0<c≤1/20` in the sharp tube `p=p*+c⁴x`, `|x|≤8` | **PROVED — EXACT COMPUTER-ASSISTED (2026-07-21)** | Rational theta tails bound the series remainders. The upper numerator is `c¹⁰[T(c²,x)+R]`; exact Bernstein coefficients bound `T/K` below by `0.812`, `2.347`, `14.317` on the three x-ranges, while the tail is `<0.1K` centrally and `<0.75K` outside. The lower is `c⁸[K_L+R_L]`, `|R_L|<0.9K_L`. The script regenerates every polynomial rather than trusting saved expansion data. `proof/slice_odd_block_reduction.md` (13ah)–(13ao); `experiments/slice_odd_tiny_edge_certificate.py` |
| L36 | L32's two cubic-envelope residuals are nonnegative on the discriminant branch for `1/12≤c≤12599/20000`, `0≤p≤1` | **PROVED — DIRECTED-INTERVAL (2026-07-21)** | Second-order Taylor intervals with explicit theta-tail derivative bounds certify a finite adaptive cover. Ridge-coordinate charts use 15,047 splits (depth 12); direct charts use 17,594 (depth 11); no boxes remain. Every operation is one-ulp outward rounded, branch-excluded boxes are logically discarded, and `p*∈[0,1]` is interval-audited. `proof/slice_odd_block_reduction.md` (13ap)–(13ar); `experiments/slice_odd_compact_certificate.py` |
| L37 | Both L32 endpoint residuals are positive for `0<c≤1/20`, `|(p−p*)/c²|≤50`; they are also positive for `0<c≤1/12`, `|p−p*|≤4c⁴` | **PROVED — EXACT COMPUTER-ASSISTED (2026-07-21)** | Deep rational theta expansions give a Bernstein-certified `|x|≤16` tube and a coefficient-dominated `16c²≤|y|≤50` annulus. A second tube bridges through `c=1/12`. The checker regenerates 309,479- and 565,425-term polynomials. `proof/slice_odd_block_reduction.md` (13at)–(13bc); `experiments/slice_odd_centered_completion_certificate.py` |
| L38 | L32's two endpoint residuals are positive on the whole discriminant branch for `0<c≤1/20` | **PROVED — EXACT COMPUTER-ASSISTED (2026-07-21)** | Exact branch numerators exclude `p<c/4` and `p≥3/4`. Boundary coefficient domination and a Bernstein middle estimate cover `|2p−1|≥96c²`; the identity `1/2≤p*≤1/2+2c²` puts the complement inside L37. Worst regular correction ratio is `0.907895`. `proof/slice_odd_block_reduction.md` (13bd)–(13be); `experiments/slice_odd_small_edge_certificate.py` |
| L39 | Outside `|p−p*|≤4c⁴`, both L32 endpoint residuals are nonnegative on their branch for `1/20≤c≤1/12` | **PROVED — DIRECTED-INTERVAL (2026-07-21)** | Exact ridge-complement charts plus outward-rounded second-order Taylor intervals close the bridge with 15,267 splits (depth 17), 952 branch-excluded and 17,715 proved evaluations. L37 supplies the omitted central tube. `proof/slice_odd_block_reduction.md` (13bf); `experiments/slice_odd_compact_certificate.py` |
| L40 | Every rank-one/rank-one dual witness is bounded by `||R_{a,b}(T)||²`, where `R_{a,b}(T)=−S⁻¹(T−A)(I−AT)⁻¹S`; its cancellation-free block formula extends to `(a,b)∈[−1,1]²` and its modal node function is a degree-two `2×2` Blaschke--Potapov function | **PROVED REDUCTION (2026-07-21)** | Spectral positive/negative factors and two row-Gram rotations give the transfer exactly. The node function is paraunitary and has determinant `(ab−z²)/(1−abz²)`. This left `||R_{a,b}(T)||≤2` as the rank-face theorem; L59 now proves it. `proof/slice_coupled_defects.md` §4–6; exact/floating regression `experiments/slice_rank_one_transfer.py` |
| L41 | The transfer norm test `||R_{a,b}(T)||≤2` is equivalent in the open parameter square to a quartic polynomial `4×4` LMI with diagonal blocks already PSD by L24/L26 | **PROVED REDUCTION (2026-07-21)** | The operator-ball defect identity removes both resolvents. After multiplying by `(1−a²)(1−b²)`, the core has diagonal blocks `E_o,E_e` and coupling `−3[a(1−b²)B+b(1−a²)C*]`; elementary bilinear estimates prove `E_o,E_e≥0`. This left the `2×2` Schur coupling, later closed by L59. `proof/slice_coupled_defects.md` §6; exact audit `experiments/slice_rank_one_transfer.py` |
| L42 | The rank-one transfer theorem holds for `c≥2^(−2/3)` | **PROVED (2026-07-21)** | In symmetric modal coordinates the transfer is the direct sum of two values of the rational inner matrix `K_{a,b}`, hence is a contraction. Conjugating back costs `cond(diag(D,√cD))=c^(−3/2)≤2`. `proof/slice_coupled_defects.md` §5 |
| L43 | L41's polynomial transfer core has the exact square completion `2Q=H*H−16Δ*Δ`, hence is PSD iff an explicit four-block small-gain matrix `S=4ΔH⁻¹` has norm at most one | **PROVED REDUCTION (2026-07-21)** | With `A_q=2(4−q²)/(1−q²)`, `D_q=2(4q²−1)/(1−q²)`, `E_q=6q/(1−q²)`, the identity `A_qD_q−E_q²=−16` gives the factorization. Block inversion leaves only resolvents of `BC,CB` at contracted parameters `u=3a/(4−a²)`, `v=3b/(4−b²)`. `proof/slice_coupled_defects.md` §6.1; exact audit `experiments/slice_rank_one_transfer.py` |
| L44 | For the four `2×2` blocks of L43's small-gain matrix, `Σ_ij ||S_ij||²≤1` | **SUPERSEDED — NUMERICAL SUFFICIENT TARGET, NOT NEEDED BY L59** | This stronger inequality implies `||S||≤1` by the scalar block comparison and Frobenius bound. Global searches approach equality only at `c→0,a,b→0`, but it was never certified. L51--L59 prove the exact weaker transfer core directly, so L44 is no longer a proof debt. `proof/slice_coupled_defects.md` (BE); `experiments/slice_rank_one_transfer.py --global-energy --envelope-energy` |
| L45 | L41's polynomial core is the sum of two explicit residual squares plus the central block defect `(1−a²)(1−b²) diag(I−C*C,I−B*B)` | **PROVED IDENTITY (2026-07-21)** | For all vectors `x,y`, the quadratic form equals `3(1−b²)||x−aBy||²+3(1−a²)||y−bCx||²+(1−a²)(1−b²)(||x||²−||Cx||²+||y||²−||By||²)`. It closes the parameter edges and the center exactly and isolates the interior compensation problem. `proof/slice_coupled_defects.md` (29a)–(29b); exact SymPy audit in `experiments/slice_rank_one_transfer.py` |
| L46 | On L44's exact boundary `p=a=b=0`, its block energy is `k(1+c²)/(4c)`, independently of the modal angle, and is strictly below one for every `c>0` | **PROVED BOUNDARY STATEMENT (2026-07-21)** | At this boundary the only nonzero small-gain blocks are `C/2,B/2`; their squared norms sum to `(k/c)(1+c²)/4`. L23 gives energy `≤1/(1+c²)<1`. An 80-decimal scaled scan finds no hidden excess down to `c=10^−20` and margin asymptotic `3c²`; the neighbourhood scan is evidence, not proof of full L44. `proof/slice_coupled_defects.md` (36)–(38); `experiments/slice_rank_one_ridge_check.py` |
| L47 | The two elliptic-slice parity blocks obey the sharp coupled tradeoff `||B|| ||C||≤2` | **PROVED — EXACT COMPUTER-ASSISTED (2026-07-21)** | The orientation compresses to one scalar `w`, and the two traces become `x/c,cy` with determinants `kp/c,ckp`. The majorant `U(M)=tr(M*M)−det(M)²/tr(M*M)≥||M||²` reduces the theorem to one rational scalar inequality. Elementary nome bounds and four exact Bernstein blow-up charts certify the singular corner; all coefficients are regenerated as integers. `proof/slice_coupled_defects.md` §6.3; `experiments/slice_block_product_certificate.py` |
| L48 | The rank-one/rank-one transfer theorem (RT) holds on both complete coordinate axes `a=0` and `b=0` | **PROVED (2026-07-21)** | For `b=0`, `R_{a,0}=diag(I,C)[[aI,−sI],[−sI,−aI]]diag(I,B)` and the middle factor is orthogonal. L24/L26 and L47 give `max(1,||B||)max(1,||C||)≤2`; swapping parity proves `a=0`. At this stage the remaining transfer obstruction had `ab≠0`; L59 later closes it. `proof/slice_coupled_defects.md` (50) |
| L49 | The elliptic modulus obeys `k≤4c/(1+4c²)` for `0≤c≤1/2` | **PROVED — EXACT BERNSTEIN (2026-07-21)** | Retain the first two factors in Jacobi's modulus product; every omitted factor is at most one. The cleared residual is `q²Q(q)`, and all 24 Bernstein coefficients of `Q(x/4)` are positive. This bound is asymptotically sharp through the `c³` term and identifies the transfer ridge missed by L23. `proof/slice_coupled_defects.md` §6.4; `experiments/slice_sharp_nome_certificate.py` |
| L50 | The block-energy target (BE), hence (RT), holds on the complete zero-node face `p=0`, `0<c<1` | **PROVED — EXACT BERNSTEIN CHART COVER (2026-07-21)** | On `p=0`, all four small-gain blocks are rank one and BE reduces to (55), using `||B||²+||C||²=k(1+c²)/c` and `||B||||C||≤k(1+c²)/(2c)`. For `c≥1/2`, `k≤1` gives a degree `(4,4,4)` polynomial with 125 nonnegative Bernstein coefficients. For `c≤1/2`, L49 and `x=2c` expose the ridge `b=1,a=x`; two ridge-centered charts, three compact charts, and three largest-coordinate blow-ups at the origin give a finite exact rational Bernstein cover (nine dyadic bisections total). `proof/slice_coupled_defects.md` §6.4; `experiments/slice_sharp_nome_certificate.py`. |
| L51 | The transfer-core PSD test reduces to one leading `3×3` minor and the full determinant; after `P=p²,t=1−o`, their exact vanishing orders at `(P,t)=(0,0)` are 3 and 4 | **PROVED REDUCTION — CLOSED DOWNSTREAM BY L59 (2026-07-21/22)** | Since `E_o≻0` in the open square, its `2×2` Schur complement needs only its first diagonal entry and determinant. Exact modal substitution makes every `p` exponent even. The charts `(P,t)=(w,wv),(wv,w)`, divided by `w³,w⁴`, remove the main zero-node/orientation singularity. The later L52--L59 boundary analysis and continuous certificates prove both minors globally. `proof/slice_core_projective_reduction.md`. |
| L52 | The full transfer theorem (RT) holds on both extreme modal-orientation faces `o=0` and `o=1` | **PROVED — ELEMENTARY FACTORIZATION (2026-07-21)** | After a positive diagonal congruence, the `3×3` minor and determinant factor into `(1−a²)(1−b²)H_{kp²}G_k` and `(1−a²)²(1−b²)²G_kG_{kp²}` (with modal interchange at `o=1`). `H_x≤0` is bilinear in `a²,b²`. Writing `G_x=C₀+C₁(a²+b²)+C₂a²b²+18abcx`, using `2|ab|≤a²+b²≤1+a²b²`, and splitting at `x=c` proves `G_x<0` for `0≤x≤k<min(1,4c)`. Hence both minors are nonnegative. `proof/slice_core_projective_reduction.md` §4; `experiments/slice_orientation_factor_audit.py`. |
| L53 | The nested zero of the projective `3×3` minor is removed by two successive exact order-one blow-ups: first `(v,1−|b|)`, then `(h,1−a)` in the first secondary chart | **PROVED REDUCTION — CLOSED DOWNSTREAM BY L54--L59 (2026-07-21/22)** | Exact sparse collection gives common order one at both stages. The later directed covers certify all three final minor charts continuously over the full nome range. `proof/slice_core_projective_reduction.md` §5; `experiments/slice_projective_core.py`. |
| L54 | The full rank-one transfer theorem (RT) holds throughout the continuous nome interval `1/100≤c≤324/15625` | **PROVED — FOUR-BOX DIRECTED ARB/BERNSTEIN COVER (2026-07-21)** | The two determinant charts and three final minor charts certify the full cubic envelope for both signs on `[.01,.012]∪[.012,.0144]∪[.0144,.01728]∪[.01728,.020736]`. Cancellation-sensitive Taylor coefficients use 80-digit Arb balls; all conversions, remainders, and subdivisions are outward enclosures. This is a local continuous theorem, not the full nome cover. `proof/slice_core_projective_reduction.md` §5; `experiments/slice_projective_interval_certificate.py`. |
| L55 | After cancellation-free low-nome normalization, the determinant has exact order `c^9` and every final minor has exact order `c^2`; the complete polynomial transfer core is nonnegative for `0≤c≤.005` | **PROVED — ALL FIVE CHARTS, BOTH SIGNS (2026-07-21)** | Exact audit covers det1 (20)–(23), det0 (24)–(27), and minor forms (28)–(33). Det1 and det0's 26-chart local hierarchy are certified for both signs. Scale-free nome/main arm charts remove the artificial det0 cutoff; the global complements pass in `235/399` leaves at depths `8/9`. Complete local-plus-global certificates also prove all three final minors (global leaves `81/32/184` and `71/32/188`). Thus the complete low core is proved for both signs on `[0,.005]`; L56 subsequently connects it to L54. Exact Taylor arithmetic is rational through 15 with Arb tails at 16/17. `proof/slice_core_projective_reduction.md` §5. |
| L56 | The full rank-one transfer theorem (RT) holds continuously on `.005≤c≤.01`, completing the low-to-L54 bridge | **PROVED — FOUR-BOX DIRECTED COVER (2026-07-22)** | Both determinant charts and all three final-minor charts pass for both signs on `[.005,.006]∪[.006,.0072]∪[.0072,.00864]∪[.00864,.01]`. Combined with L55 and L54, this proves (RT) for every `0≤c≤.020736`. `proof/slice_core_projective_reduction.md` (34); `experiments/slice_projective_interval_certificate.py`. |
| L57 | The compact directed cover reaches `.63` for the negative sign and `c+=.5566585294072849…` for the positive sign | **PROVED PARTIAL INTERVAL COVER (2026-07-22)** | Exact rational geometric boxes certify all five charts: ratios `6/5` then `11/10` close the negative sign through `.63`, which overlaps L42 because `.63>2^(−2/3)`; ratios `6/5,11/10,21/20,41/40,81/80` close the positive sign through the exact rational `c+` in (35). At this stage only the positive tail remained; L59 later closes it. `proof/slice_core_projective_reduction.md` (35). |
| L58 | On the positive determinant edge `a=b=1`, both projective charts factor into explicit even powers; the first coefficients in `A=1−a` and `B=1−b` are identical and nonnegative | **PROVED EXACT IDENTITY (2026-07-22)** | Equations (37)–(39) give the two face factorizations and the common first variation. Its only non-square factor is `2+3k+3Zk−8Zk²≥0` on the unit square. Exact regeneration checks all `197563` records per chart and gives higher-deficit coefficient l1 norm `41235531913` in both. The global l1 bound is intentionally not treated as sharp; L59 later certifies the full correlated polynomial. `experiments/slice_positive_face_audit.py`; `proof/slice_core_projective_reduction.md` (36)–(39). |
| L59 | The full rank-one transfer theorem (RT), hence L20, holds for every elliptic 4×4 slice parameter | **PROVED — REGENERATING ARB/BERNSTEIN CERTIFICATE (2026-07-22)** | The complete deficit-centered determinants retain all `(A,B,X,R,Y,c)` correlations and cover the full `A,B∈[0,1]` square. Ten exactly adjacent ratio-`81/80` rational boxes certify both determinant and all three minor charts from `c+` through `.63`; bounded recentering rebuilds and independently certifies exact rational physical subboxes, with no tolerance. Since `.63³−1/4=47/10^6>0`, the cover overlaps L42. The clean run regenerated both 197,563-record tables and both 207-term corner squares and exited 0. `proof/slice_core_projective_reduction.md` (42)–(43); `experiments/positive_tail_full_20260722.log`. |
| L60 | Repeated Crabb equality blocks are local maxima of the general L21 similarity square under transverse perturbations | **PROVED FOR BLOCK SIZE 3 BY L114; NUMERICAL CONJECTURE FOR SIZE 4 AND HIGHER (2026-07-22)** | Block sizes 3/4, multiplicities 2/3, and dense/cross/operator-weight directions give 118/120 accepted perturbed main records, none above four. L61 proves the general first-order sign, L65 the arbitrary-size single-block second-order sign, and L114 now proves a full neighbourhood theorem for every fixed multiplicity of `C3`. Repeated `C4` and larger blocks still have only the numerical/finite-order evidence stated here. `proof/general_similarity_probe.md`; `proof/general_similarity_tangent.md`; `proof/repeated_p3_local_neighbourhood.md`. |
| L61 | At a repeated `p×p` Crabb block, the one-sided support derivative and conformal operator tangent reduce to `s_E(θ)=λmax(V_θ* Re(e^{-iθ}E)V_θ)` and `G_E=E−[ŝ(0)A₀+2Σ_{k=1}^{p−2}ŝ(k)A₀^{k+1}]`; the L21 tangent optimum is `d_E=−4[2(p−1)ŝ(0)−λmax Σ_j(E_{j,j+1}+E_{j,j+1}*)/w_j]≤0` | **PROVED LOCAL FIRST-ORDER THEOREM (2026-07-22; REGULARITY CLOSED BY L101)** | The tangent SDP dual collapses exactly to one density matrix. The bracket is nonnegative by averaging the repeated top-support compression and convexity of `λmax`. A strict-tangent feasible-metric lift proves the upper right Dini derivative of `t*` is at most `d_E`. L101 proves the required uniform conformal expansion for continuous largest-eigenvalue profiles even at crossings. Clarabel/SCS and the closed form agree within `3.17e-8` on 12 directions. This is not a neighbourhood theorem: Jensen-equality directions need higher orders. `proof/general_similarity_tangent.md`; `proof/general_similarity_nonsmooth_tangent.md`; `experiments/general_similarity_tangent_probe.py`. |
| L62 | At a single Crabb block, the second support variation and Schwarz integral give finite exact formulas for `Tε=A+εG+ε²H+o(ε²)`; the zero first-order L21 face then reduces to the finite second-order Schur-complement SDP (8)–(10) in Hermitian metric coefficients `X,Y` | **PROVED FINITE REDUCTION; SIGN CLOSED BY L65 (2026-07-22)** | `s=v*H_Ev`, `q=v*H_E(I−H_0)^†H_Ev`, and second normal data `κ=q−(Im(e^{-iθ}F)−s′)²/2` determine `G,H`; nilpotence truncates the Frechet term at degree `2p−1`. Complementarity plus a second-order PSD lemma gives three affine block LMIs. Twelve `p=3,4` directions give `−3.94535170≤e≤−.00284309`; solvers agree within `3.47e-7`, and analytic/fitted gauges within `3.57e-7`. L63–L65 now prove `e≤0` for every single Crabb block size. `proof/general_similarity_second_order.md`; `proof/general_crabb_second_variation.md`. |
| L63 | For a single canonical `3×3` Crabb block and arbitrary complex perturbation `E`, the L62 second-order optimum is `e₃(E)=−2(Re(E₀₁−E₁₂))²−(21/4)|E₂₀|²≤0` | **PROVED EXACT SYMBOLIC IDENTITY + SECOND-ORDER LOCAL THEOREM (2026-07-22)** | Complementarity leaves only `x=X01,z=X02`; eliminating all second metric coefficients gives a convex quadratic with Hessian `8|x|²+(8/3)|z|²`. Substitution of the finite `G,H` formula and exact symbolic collection yields the displayed two-square identity, regenerated from all 18 real perturbation coordinates by `experiments/p3_second_order_identity.py`. Hence the upper second-order Dini quotient of `t*` is nonpositive, and strictly negative unless `E20=0` and `Re E01=Re E12`. The 15-real-dimensional equality space still needs third order/orbit classification. `proof/general_similarity_second_order.md` §8. |
| L64 | For a single canonical `4×4` Crabb block and arbitrary complex perturbation `E`, the L62 second-order optimum is the five-term negative sum of squares (28), hence `e₄(E)≤0` | **PROVED EXACT SYMBOLIC IDENTITY + SECOND-ORDER LOCAL THEOREM (2026-07-22)** | Complementarity leaves `x=X01,y=X02,z=X03`; the metric-variable Hessian is `8|x|²+8|y|²+(8/3)|z|²`. Exact elimination yields rank-eight form `−|Λ(E)|²/2−4(Re(E01−E23))²−(4/9)(Re(E01−2√2E12+E23))²−(2/9)|E20+E31|²−(52/9)|E30|²`. A 32-real-variable SymPy regeneration checks the support eigenpairs and proves the polynomial residual exactly zero; all six saved `p=4` SDP cases agree within `3.17e-8`. The 24-real-dimensional equality space still needs higher-order/orbit classification. `proof/general_similarity_second_order.md` §9; `experiments/p4_second_order_identity.py`. |
| L65 | For every canonical `p×p` single Crabb block (`p≥3`) and every complex perturbation `E`, the L62 optimum satisfies `e_p(E)≤0`; hence the upper second-order Dini change of the L21 similarity square is nonpositive in every direction | **PROVED ARBITRARY-SIZE SECOND-ORDER LOCAL THEOREM (2026-07-22)** | The support defect is a path Laplacian, giving the exact Green formula (10). Circle grading reduces every nonzero mode to `−t*ᵊ_{r,k}t`. For `k≥3`, `ᵊ^{-1}=(I−H_r)/4+[3/(2k(k−1)(k−2))]qq*` is positive definite; `k=1,2` are PSD infinite-coefficient limits, and the two bottom modes are explicit negative squares. Grade zero is an exact weighted shift and admits a condition-two diagonal similarity by the numerical-radius power inequality. The form has rank `p(p−2)`. Formula/elimination checks cover every mode through `p=30` (max absolute residual `2.34e-9`, relative `3.03e-13`), while L63–L64 are exact symbolic base cases. Equality directions and repeated-block common-maximizer faces remain. `proof/general_crabb_second_variation.md`; `experiments/general_crabb_second_order_modes.py`. |
| L66 | The L65 kernel modulo exact affine-unitary motions has real dimension `2p−2`; grade zero and the last unpaired mode are entirely exact-orbit directions, while the residual quotient consists of one complex mode-1 direction, two complex mode-2 directions, and one complex direction in each mode `3,...,p−2` | **PROVED EQUALITY-SPACE QUOTIENT (2026-07-22)** | The raw kernel dimension is `p(p+2)`. The exact tangent space `{[A,K]+βI+sA:K*=−K,β∈C,s∈R}` has dimension `p²+2`, because the skew-Hermitian commutant is `iR I`; subtraction gives `2p−2`. L65's mode kernels give the refined count. Independent full quadratic/orbit reconstruction at `p=3..8` confirms ranks and `QO=0` with maximum residual `9.76e-14`. L67--L70 close every straight `p=3` ray and the full weighted leading sign; its unique analytic center and higher sizes remain. `proof/crabb_second_order_equality.md`; `experiments/crabb_second_order_equality.py`. |
| L67 | On the residual `p=3` mode-one ray `C₃+εR₁(ζ)`, the L21 similarity square is strictly below four for all sufficiently small nonzero `ε` | **PROVED EXACT SIXTH-ORDER FEASIBLE CERTIFICATE (2026-07-22)** | Circle symmetry reduces `ζ` to a real coefficient. Formal support-boundary reparameterization derives the Riemann pullback through order six and independently reproduces L63 at orders one and two. For the rank-one Stein defect `cε=(1,−√2ε/24+77√2ε³/4608,−193ε²/576)`, the exact Gramian has condition number `4−171ε⁶/4096+O(ε⁷)`. Since this is a feasible metric, strict descent follows without claiming rank-one global optimality. This treats one quotient ray, not its mixtures with mode two. `proof/p3_crabb_higher_order.md`; `experiments/p3_crabb_sixth_order.py`. |
| L68 | Every fixed nontrivial straight ray in the canonical representative space for the residual `p=3` quotient strictly decreases the L21 similarity square; when the mode-two coordinate `ω` is nonzero, a feasible Gramian has quartic coefficient `−4|ω|⁴−(31/8)|ζ|²|ω|²` | **PROVED EXACT RAYWISE HIGHER-ORDER THEOREM (2026-07-22)** | The circle action has weights one and two on `(ζ,ω)`, and conjugation leaves only one cubic and three quartic invariants. Exact formal Riemann/Stein calculations at `(1,0),(0,1),(1,1)` give zero cubic and quartics `0,−4,−63/8`, determining the displayed form. L67 handles `ω=0` at sixth order. L69--L70 now close the exact slice and weighted leading sign; their unique higher-order center is the remaining `p=3` debt. `proof/p3_crabb_higher_order.md`; `experiments/p3_crabb_quartic.py`. |
| L69 | Every matrix sufficiently close to `C₃` is affine-unitarily equivalent to `C₃+E(z,w,s,v)` with `E=[[-z/3,s,conj(z)],[w,2z/3,−s],[v,w,−z/3]]`; this seven-real-dimensional space is exactly the Hilbert--Schmidt normal slice to the value-preserving orbit, and `e₃(E)=−8s²−21|v|²/4` | **PROVED EXACT LOCAL-SLICE REDUCTION (2026-07-22)** | The affine-unitary orbit has dimension 11 and the displayed space has dimension 7; direct inner products give orthogonality, hence a full `18=11+7` decomposition. Restricting the group parameters away from the infinitesimal stabilizer, the derivative of the orbit projection is an isomorphism, so the implicit-function theorem gives an exact local slice. L63 supplies the displayed Hessian, with precisely four residual and three negative coordinates. L70 closes the full weighted leading coupling; only its unique analytic center remains. `proof/p3_crabb_local_slice.md`; `experiments/p3_crabb_local_slice.py`. |
| L70 | In the complete weighted `p=3` transition `C₃+εR₁(1)+ε²R₂(u)+ε³(sY+vV)`, the analytic rank-one Stein certificate has sixth-order coefficient `H=−8s²−(21/4)|v+39/448+(4√2/7)u|²−(25/56)|u−3√2/64|²≤0` | **PROVED EXACT WEIGHTED LEADING FORM (2026-07-22)** | Sparse exact Riemann/Stein series extract the previously missing ordinary fifth coefficient `−123√2/256`, the bottom linear coefficient `−117/128`, and the mode/bottom coupling `−6√2`; circle grading determines the complex form. Its only zero is `(u,v,s)=(3√2/64,−9/64,0)`. Thus every other bounded weighted direction has strict feasible-certificate descent. L71--L73 identify the flat center and use this completed square in the full local theorem. `proof/p3_crabb_weighted_slice.md`; `experiments/p3_crabb_weighted_slice.py`. |
| L71 | L70's unique weighted center continues exactly as `u=(3√2/64)R(e), v=−(9/64)R(e), s=0`, where `81e⁴R²+(1152e²−4096)R+4096=0` and `R(0)=1`; every matrix on this curve has a circular numerical range centered at a double eigenvalue, hence satisfies the complete L21 bound `t*≤4` by Berger--Okubo--Ando | **PROVED EXACT DISK-CENTER THEOREM (2026-07-22)** | The analytic root is `R=1+9e²/32+405e⁴/4096+...`, reproducing the independent higher-jet center shift. Modulo its quadratic relation, the characteristic polynomial is `(λ−c)²(λ+2c)` and the Kippenhahn polynomial factors as `(z−2cx)((z+cx)²−r²(x²+y²))`; the isolated point remains inside the circle near zero. Thus the finite-jet flatness is the local disk-matrix locus, not a missed positive term. L72 proves ambient stationarity and L73 supplies the formerly missing uniform normal estimate. `proof/p3_crabb_disk_center.md`; `experiments/p3_crabb_disk_center.py`. |
| L72 | Along L71's normalized disk curve, the explicit rank-one Stein metric `P=diag(1,2,4)` is stationary after Riemann pullback under every complex `3×3` perturbation | **PROVED EXACT CRITICAL-MANIFOLD THEOREM (2026-07-22)** | An additional singular-product identity puts the curve in the exact real Schur form `T_l=[[0,a,−2l],[0,l,a],[0,0,0]]`, `a²=2(1−l²)`. Linearizing its rank-one Stein equation gives `δcond=4ℓ(Re G)`. Exact support-vector residues prove `4 Re S_E(l)=ℓ(Re E)`, while `ℓ(T)=4` and `ℓ(T^(k+1))=2l^k`; hence the conformal correction cancels every first variation. L73 combines this stationarity with L70 and symmetry to prove the full local theorem. `proof/p3_disk_center_tangent.md`; `experiments/p3_disk_center_tangent.py`. |
| L73 | Every complex `3×3` matrix in some full neighbourhood of the single Crabb block `C₃` has its numerical range as a complete `2`-spectral set | **PROVED ANALYTIC LOCAL THEOREM (2026-07-22)** | The rank-one Stein condition has positive defect Hessian `8|x|²+8|y|²/3`, giving an analytic locally optimized feasible certificate `g`. L71--L72 make the disk curve an exact ambient critical manifold. Analytically maximizing the three strong normal variables leaves one complex soft variable `U`; circle symmetry, stationarity, L70, and L68 give `H(z,U)=−(25/56)|z|²|U|²−4|U|⁴+R`, and every allowed remainder is absorbable by those negative terms. Thus `g≤0`, so `t*(φ_A(A))≤4` throughout the slice and hence a full neighbourhood by L69. This is not a repeated-block, larger-size, or general theorem. `proof/p3_crabb_local_theorem.md`; `experiments/p3_disk_morse_bott.py`. |
| L74 | On L61's repeated-`C3` zero-Jensen face with common copy maximizer `y`, each selected/orthogonal-copy cross pair has, modulo infinitesimal unitary mixing, exactly three complex residual directions | **PROVED EXACT CROSS-QUOTIENT CLASSIFICATION (2026-07-22)** | The common-eigenvector Laurent condition `r(q)*(q^−1X+qY*)r(q)=0` has real rank 14 on 36 variables. Cross unitary commutators have rank 16 inside its 22-dimensional kernel. The six-real-dimensional orthogonal quotient has the three explicit complex generator pairs (7). For multiplicity `m` this gives `3(m−1)` complex cross parameters. L75--L80 close pure-pair/star rays; L83--L85 subsequently close the full fixed tied-winner second-order sign. Higher-order equality and losing-sector mergers remain. `proof/repeated_p3_common_maximizer.md`; `experiments/repeated_p3_common_maximizer.py`. |
| L75 | On one multiplicity-two L74 cross pair, the second support coefficient is `κ(q)=M(q)+(√2/24)|Re(qd)|`, with `M` containing only modes 0 and 2 and `d=3α0 conj(α1)+4α1 conj(α2)`; moreover the second conformal correction satisfies `K(C3)=κ̂(0)C3` | **PROVED EXACT SECOND-SUPPORT REDUCTION; SIGN CLOSED BY L76 (2026-07-22)** | The reduced support resolvent is `I−H/4−3H²/4`. The `2×2` effective top-space matrix is diagonal; exact multiplication gives (5)--(7). The absolute cosine is the entire nonsmooth split. Pi-periodicity kills Fourier mode one, and nilpotence kills modes at least two, leaving only `κ̂(0)=5|α0|²/128+|α1|²/4+5|α2|²/72+√2|d|/(12π)`. L76 uses this scalar in a complete finite Stein certificate. `proof/repeated_p3_second_support.md`; `experiments/repeated_p3_second_support.py`. |
| L76 | For one multiplicity-two L74 cross pair with zero diagonal blocks, the upper second-order Dini change of the L21 similarity square is at most `−8|α1|²−(4√2/(3π))|3α0 conj(α1)+4α1 conj(α2)|≤0` | **PROVED EXACT FEASIBLE SECOND-ORDER CERTIFICATE (2026-07-22)** | An explicit first metric tangent annihilates all active compressions. The full second metric is constructed by the two-level Crabb Stein recurrence after inserting the lower and contraction Schur penalties. Its upper endpoint collapses exactly to a scalar; substitution of L75's conformal mean cancels the `|α0|²` and `|α2|²` terms and gives the displayed sign. The certificate is flat exactly when `α1=0`; L77 closes that plane cubically. This is one pure cross pair, not yet diagonal/cross mixtures, arbitrary multiplicity, or a full repeated neighbourhood. `proof/repeated_p3_stein_sign.md`; `experiments/repeated_p3_stein_sign.py`. |
| L77 | On L76's pure-pair equality plane `α1=0`, the upper third-order Dini change is at most `−16 mean_{|q|=1}|c(q)|<0`, with the explicit odd Laurent polynomial `c` in (2), for every nonzero `(α0,α2)` | **PROVED EXACT STRICT THIRD-ORDER CERTIFICATE (2026-07-22)** | The second effective support matrix stays scalar; the third is `[[0,c],[conj(c),0]]`, so its top branch is `|c|`. Pi-periodicity kills the first Fourier mode. The complete third metric is propagated with the exact cubic Schur penalty; every lower-order term cancels and the upper endpoint is `−16 mean|c| I2`. The factorization of `c` proves its mean is positive off the origin. Pure-axis coefficients are `−|α0|³/4` and `−16|α2|³/27`. This closes every pure multiplicity-two cross-pair ray, not mixtures with diagonal blocks or simultaneous multiplicity directions. `proof/repeated_p3_third_sign.md`; `experiments/repeated_p3_third_sign.py`. |
| L78 | For arbitrary multiplicity, simultaneous L74 star coupling between the selected common-maximizer copy and all orthogonal copies has nonpositive upper second-order Dini change | **PROVED ARBITRARY-MULTIPLICITY STAR THEOREM (2026-07-22)** | If `Q(q)` is the effective copy-space support matrix, `k0=mean λmax Q`, and `Qbar=mean Q`, the full recursive metric endpoint is `16(Qbar−k0 I)−8 diag(||a1||²,conj(a1)a1^T)`. The first conformal mode is generally nonzero for `m>2` but cancels exactly. Pointwise `Q(q)≤λmax(Q(q))I` and the Gram penalty prove NSD. A two-independent-copy symbolic polarization audit proves the dimension-free identity. Equality directions still need higher order; diagonal/cross mixtures are outside scope. `proof/repeated_p3_star_second_sign.md`; `experiments/repeated_p3_star_second_sign.py`. |
| L79 | The **explicit L78 certificate endpoint** has zero top eigenvalue exactly when the generator-1 coefficient vector `a1` vanishes; inside that equality set, an orthogonal-copy vector is also a common top support branch exactly when `span{a0,a2}` has dimension at most one | **PROVED CERTIFICATE-EQUALITY CLASSIFICATION; OPTIMIZATION CORRECTED BY L80 (2026-07-22)** | Writing `Q(q)=diag(s(q),R(q))`, exact Laurent algebra gives `s(q)=tr R(−q)`. For `a1=0`, this becomes `s=tr R≥lambda_max R`, so the selected copy is common top and the particular endpoint vanishes. Conversely a null endpoint with `a1≠0` forces a fixed rank-one range and contradicts the Gram-kernel condition. Rank-one `span{a0,a2}` reduces to L77. A nonlinear probe falsified the initial fourth-order inference; L80 now supplies the missing strict second-order tangent for rank two. `proof/repeated_p3_star_second_sign.md` §5. |
| L80 | Every L79 rank-two flat star direction (`a1=0`, `a0,a2` independent) admits a strictly negative feasible second-order metric endpoint | **PROVED EXACT STRICT SECOND-ORDER CERTIFICATE (2026-07-22)** | Perturb the free first-metric block by `U_tau=[[0,(-3√2/8+tau)a0^T],[(1/√2+tau)conj(a2),0]]`. On the active two-copy span, exact propagation gives `E(0)=diag(0,−5 adj(H)/72)` and `E'(0)=(5√2/3)diag(−tr K,K)`, with `H=9 conj(a0)a0^T+16 conj(a2)a2^T` and `K=3 conj(a0)a0^T+4 conj(a2)a2^T`. Rank two makes `adj(H)>0`, while the selected derivative is negative, so `E(tau)<0` for small positive `tau`. Together with L77/L79, every nonzero pure star ray descends at second or cubic order. `proof/repeated_p3_star_second_sign.md` §6; `experiments/repeated_p3_star_second_sign.py`. |
| L81 | If one repeated `C3` copy grows radially as `(1+d epsilon)C3`, the others shrink as `(1−d epsilon)C3` with fixed `d>0`, and the only off-diagonal perturbations are L74 star blocks, then every nonzero star direction has strict negative second-order similarity change | **PROVED EXACT RADIAL-GAP COUPLING THEOREM (2026-07-22)** | The diagonal metric tangent `diag(d,0,−d)` gives each losing copy strict first contraction slack `diag(6d,15d)`. The selected cross tangent has two free scalars; exact second-metric elimination yields `e(x,y)=[(96x²−48√2x−63)/24]||a0||²−8||a1||²+[(96y²−256√2y+208)/24]||a2||²`. At `x=√2/4,y=4√2/3`, this is `−25||a0||²/8−8||a1||²−50||a2||²/9<0`. The coefficient is gap-independent but the asymptotic neighbourhood is not uniform as `d→0`. `proof/repeated_p3_radial_gap.md`; `experiments/repeated_p3_radial_gap.py`. |
| L82 | On a repeated-`C3` zero-Jensen direction with one common winner and no common zero vector in the losing first-support compression, the selected second endpoint is the single-block coefficient plus the universal strict L81 star penalty | **PROVED UNIQUE-WINNER REDUCTION UNDER UNIFORM ONE-SIDED CONFORMAL EXPANSION (2026-07-22)** | After the selected first Riemann correction, the losing compression `B_-(q)≤0`. No common kernel implies `mean B_-<0`; L61's tangent dual and Slater point then give strict first-order metric slack on the entire losing sector, even with isolated pointwise contacts. Copy-sector parity makes every selected second Schur term additive, giving `e_total≤e_selected−25||a0||²/8−8||a1||²−50||a2||²/9`, with `e_selected≤0` by L63/L65. Thus nonzero winner/loser star coupling is strict at second order; arbitrary internal losing blocks are absorbed. Multiple common winners and uniform gap-closing transitions remain. `proof/repeated_p3_unique_winner.md`. |
| L83 | On a tied repeated-`C3` winner sector with zero diagonal quotient blocks and arbitrary L74 quotient data on every winner--winner edge, the pairwise L76 metric has a nonpositive second-order endpoint in every multiplicity | **PROVED COMPLETE WINNER-GRAPH SECOND-ORDER SIGN (2026-07-22)** | If `Q(q)` is the full effective copy-space support matrix, `Qbar=mean Q`, `k0=mean lambda_max Q`, and `H1` is the Hermitian matrix of generator-1 edge coefficients, exact propagation gives `E=16(Qbar−k0 I)−8H1²≤0`. An arbitrary first conformal mode cancels. A full symbolic three-copy triangle proves every oriented two-edge path and hence the dimension-free identity. Caution: `Qbar` is not a simple weighted sum of `Hj²`; mixed 0/2 paths survive on cycles. L84 adds every relative diagonal residual; equality optimization and the common shared motion remain. `proof/repeated_p3_winner_graph_sign.md`; `experiments/repeated_p3_winner_graph_sign.py`. |
| L84 | The diagonal repeated-`C3` support kernel modulo within-copy unitary orbit is one complex generator-zero loop plus one real generator-one loop, and adding arbitrary such loops to every vertex of L83 preserves its nonpositive endpoint | **PROVED COMPLETE RELATIVE-DIAGONAL/GRAPH SECOND-ORDER SIGN (2026-07-22)** | The diagonal Fourier constraint has real rank seven, its unitary orbit has rank eight, and the three canonical real loop directions complete the 11-dimensional kernel. Put the real loop coefficients on the diagonal of `H1`; a symbolic full triangle with all edge and loop variables gives the unchanged identity `E=16(Qbar−k0 I)−8H1²≤0`, including every mixed path. This closes all zero-support relative diagonal motions. L85 subsequently adds the common single-block motion; graph equality optimization and weighted mergers remain. `proof/repeated_p3_winner_diagonal_sign.md`; `experiments/repeated_p3_common_maximizer.py`; `experiments/repeated_p3_winner_graph_sign.py`. |
| L85 | Every fixed first-order direction on a complete tied-winner sector at a repeated `C3` block has nonpositive upper second-order L21 similarity change | **PROVED COMPLETE TIED-WINNER SECOND-ORDER SIGN (2026-07-22)** | Global affine normalization and L84 reduce the common diagonal motion to `W(w)=w(E10+E21)` and `V(v)=vE20`. Retaining the first map `F(z)=conj(w)z³/√2+conj(v)z⁴/4`, its full Frechet derivative, and the normal-angle mean `9|w|²/4+|v|²/2`, exact triangle propagation yields `E=16(Qbar−mean lambda_max(Q)I)−8H1²−21|v|²I/4≤0`. The flat `w` mode cancels exactly; the `v` coefficient matches L63. This exhausts the fixed tied-winner quotient, but equality/higher-order transitions and uniform losing-sector mergers remain. `proof/repeated_p3_complete_winner_sign.md`; `experiments/repeated_p3_winner_graph_sign.py`. |
| L86 | Under the uniform one-sided conformal expansion of L61--L62, at an arbitrary repeated `C3` block every positive-Jensen direction descends at first order and every fixed zero-Jensen direction has nonpositive upper second-order L21 change | **PROVED COMPLETE FIXED-DIRECTION FIRST/SECOND-ORDER THEOREM (2026-07-22)** | Promote every common kernel vector into a maximal winner space. The losing mean compression is then strictly negative and supplies first-order Slater slack. Polarizing L81 on two winners and one loser gives the universal cross penalty `−25A0A0*/8−8A1A1*−50A2A2*/9`. Adding L85's complete winner term gives `16(Qbar−mean lambda_max(Q)I)−8H1²−21|v|²I/4` plus those three negative Gram matrices. Arbitrary losing internal blocks are absorbed. This is fixed-direction and second-order; uniform higher-order equality/gap-closing transitions remain. `proof/repeated_p3_complete_fixed_direction.md`; `experiments/repeated_p3_multiwinner_gap.py`. |
| L87 | A vector lies in the kernel of L86's complete second endpoint exactly when it is a common top eigenvector of `Q(q)`, is killed by `H1` and every winner--loser coefficient adjoint, and the common strong coordinate `v` vanishes | **PROVED EXACT CERTIFICATE-EQUALITY REDUCTION (2026-07-22)** | Write the endpoint as `−16 mean(lambda_max(Q)I−Q)−8H1²−21|v|²I/4−25A0A0*/8−8A1A1*−50A2A2*/9`. A sum of PSD quadratic forms vanishes exactly on their common kernel; continuity upgrades the zero mean to the pointwise common-top condition. After a copy-unitary, the selected branch has no losing coupling, no generator-one coupling, and only the flat generator-zero/two star plus common flat single-block modes. These are precisely the L77/L80 and L67--L73 boundary models. `proof/repeated_p3_equality_reduction.md`. |
| L88 | The complete L87 flat graph/loop quotient is `E_Z=Z* tensor X0+Z tensor Y0` for one arbitrary copy matrix `Z`, with `Q_Z(q)=5(ZZ*+Z*Z)/128−3(q²Z²+q^−2(Z*)²)/128`; if `Z` is normal, the full nearby repeated matrix satisfies the complete `2` bound | **PROVED COPY-MATRIX REDUCTION AND EXACT NORMAL-STRATUM LOCAL THEOREM (2026-07-22)** | The identities `X0=−3Y2/4`, `X2=−4Y0/3` give a bijection between all generator-0/2 edges plus loops and entries of `Z`. Exact reduced-resolvent multiplication proves the displayed support formula. For normal `Z`, a copy unitary diagonalizes the matrix into nearby single-`C3` blocks `C3+conj(zj)X0+zjY0+W(w)`; L73 controls each block and spectral-set domain monotonicity controls their direct sum on the convex-hull numerical range. Only nonnormal Schur strata remain. `proof/repeated_p3_flat_copy_matrix.md`; `experiments/repeated_p3_flat_copy_matrix.py`. |
| L89 | For a zero-diagonal three-copy Schur matrix `N=[[0,a,b],[0,0,c],[0,0,0]]`, L88's matrix-Jensen endpoint is flat only if `abc=0`; every nonzero equality face is already strict by L77 or L80 | **PROVED COMPLETE THREE-COPY NONNORMAL FLAT STRATUM (2026-07-22)** | Since `N²=ac E02`, a common eigenvector with `ac!=0` must be the middle coordinate; the constant support coefficient makes it an eigenvector iff `b=0`. Its complement top defect has positive diagonal and determinant `16|ac|²`, so the path is L80's rank-two star. If `ac=0`, then `N²=0`; unitary singular-value reduction gives one L77 pure pair plus a zero copy. If `abc!=0`, the Jensen gap is strict. Thus the zero-diagonal three-copy nonnormal core closes at second or cubic order. `proof/repeated_p3_flat_three_copy.md`; `experiments/repeated_p3_flat_three_copy.py`. |
| L90 | With the common flat mode `W(w)=w(E10+E21)`, the complete L88 effective support is `Q_Z(q)+r_w(q)I+ell_w(q)Z+conj(ell_w(q))Z*`, with explicit Laurent scalars `r_w,ell_w` | **PROVED EXACT COMMON-MODE COPY-MATRIX FORMULA (2026-07-22)** | The first common compression is the scalar `sqrt(2)(wq^−2+conj(w)q²)/4`. Exact reduced-resolvent multiplication gives `r_w=(2|w|²−q⁴conj(w)²−q^−4w²)/16` and `ell_w=sqrt(2)(q^−1w−q³conj(w))/16`. The scalar term cannot affect the Jensen eigenspaces; all diagonal/common interaction is the Hermitian linear pencil `ell_w Z+conj(ell_w)Z*`. This reduces the remaining Schur classification to finitely many matrix coefficients. `proof/repeated_p3_flat_common_support.md`; `experiments/repeated_p3_flat_copy_matrix.py`. |
| L91 | If the common flat mode `w` is nonzero, every common eigenvector of the L90 effective support family reduces `Z`; hence a unitarily irreducible nonnormal `Z` has strict matrix-Jensen gap | **PROVED COMMON-MODE RIGIDITY (2026-07-22)** | The unique non-scalar Fourier coefficients at modes `+3,-3` are `−sqrt(2)conj(w)Z/16` and `−sqrt(2)wZ*/16`. Projecting a common eigenvector equation onto its orthogonal complement forces both `Zx` and `Z*x` into `span{x}`. Minimal reducing blocks carrying equality are therefore one-dimensional and are exactly nearby single-`C3` blocks covered by L73; higher-dimensional irreducible blocks are strict at second order. The weighted transition `w->0` remains. `proof/repeated_p3_common_mode_rigidity.md`. |
| L92 | Every fixed two-copy direction in L87's complete flat core is either on L88's exact normal stratum or descends strictly at second or cubic order | **PROVED COMPLETE TWO-COPY FLAT CORE (2026-07-22)** | For nonnormal Schur `Z=[[d0,a],[0,d1]]`, a common eigenvector at `w=0` forces `Z²` normal, hence `a(d0+d1)=0`; with `a!=0`, equality requires `tr Z=0`. Then `Z²` and `ZZ*+Z*Z` are scalar. The third effective support is traceless with cross entry `a[3d²q⁴+(2|a|²+4|d|²)q²−7conj(d)²]/(128q)`, nonzero for `a!=0`; the full third endpoint is `−16 mean lambda_max(Q3) I<0`. If `w!=0`, L91 is strict for nonnormal `Z`; normal cases are L88. `proof/repeated_p3_flat_two_copy.md`; `experiments/repeated_p3_flat_two_copy_third.py`; shared `experiments/repeated_p3_third_metric.py`. |
| L93 | Every fixed direction in L87's complete flat copy core is controlled in arbitrary copy multiplicity: exact on normal blocks, strict at second order off scalar-support blocks, and strict at cubic order on the remaining nonnormal blocks | **PROVED ARBITRARY-COPY METRIC-FLAG THEOREM (2026-07-22)** | For a copy projection `P`, vary the free metric block by `PZ(I−P)−3(I−P)ZP/4`. Exact propagation gives compression `−5sqrt(2)PZ(I−P)Z*P−15sqrt(2)PZ*(I−P)ZP/4`. Iterating its kernel either makes the endpoint strict or isolates a reducing block. On an irreducible residual block the support must be scalar, so `Z²=alpha I` and `ZZ*+Z*Z=beta I`. Hermitian-part/Clifford reduction shows every irreducible such block has size at most two; L88 and L92 close those blocks. This is fixed-direction, not uniform as all gaps collapse. `proof/repeated_p3_flat_metric_flag.md`; `experiments/repeated_p3_flat_metric_flag.py`. |
| L94 | The L92 cubic endpoint on `Z=[[d,a],[0,−d]]` has the uniform bound `lambda_max(E3)≤−|a|(|a|²+2|d|²)/4` | **PROVED QUANTITATIVE TERMINAL COERCIVITY (2026-07-22)** | The third effective support is traceless. Its upper cross entry has Fourier coefficient `a(2|a|²+4|d|²)/128`; `mean lambda_max(Q3)` dominates the mean modulus of that entry, which dominates the coefficient modulus. Multiplying by L92's exact endpoint factor `−16` gives the bound. Equality is possible only on the normal face `a=0`; this supplies the uniform cubic margin for L93's terminal blocks. `proof/repeated_p3_flat_two_copy.md`; `experiments/repeated_p3_flat_two_copy_third.py`. |
| L95 | For `Z=zI+[[d,a],[0,−d]]`, the canonical second endpoint has an explicit negative lower bound proportional to `|z|a²(4|d|²+a²)/(2|d|²+a²)^(3/2)` | **PROVED QUANTITATIVE TRACE-SPLITTING JENSEN GAP (2026-07-22)** | The traceless support is `B+C(q)` with constant Pauli vector `B`. Exact averaging gives `mean|C_perp|²=9|z|⁴a²(4|d|²+a²)/(8192b0)`. The Euclidean identity `|h|−e·h=|h_perp|²/(|h|+e·h)`, plus `b0≤2|z|²n` and `max|B+C|≤(5sqrt(2)+6)|z|sqrt(n)/64`, gives endpoint at most `−9|z|a²(4|d|²+a²)/[32(5sqrt(2)+6)n^(3/2)]`. It is strict precisely off the trace-zero or normal faces. `proof/repeated_p3_flat_two_copy.md`; `experiments/repeated_p3_flat_two_copy_jensen.py`. |
| L96 | On a trace-zero two-copy terminal block, the common flat mode gives the quantitative second endpoint `lambda_max(E2,w)≤−sqrt(2)|aw|` | **PROVED COMMON-MODE TERMINAL COERCIVITY (2026-07-22)** | The non-scalar L90 pencil is `ell_wD+conj(ell_w)D*` and has zero mean.  In Schur form its upper cross entry is `sqrt(2)a(q^−1w−q³conj(w))/16`. Mean top eigenvalue dominates the modulus of either Fourier coefficient, and the exact endpoint factor is `−16`. The bound is strict whenever the block is nonnormal and `w!=0`; its zero is the normal stratum. `proof/repeated_p3_flat_two_copy.md`; `experiments/repeated_p3_flat_two_copy_jensen.py`. |
| L97 | Trace splitting and the common flat mode cannot cancel on a two-copy terminal block; jointly `lambda_max(E2,z,w)≤−9|w|a³(4|d|²+a²)/[80(5sqrt(2)+6)(2|d|²+a²)²]` | **PROVED EVEN/ODD JOINT COERCIVITY (2026-07-22)** | The trace-driven Pauli support is even under `q->−q`, while the common-mode support is odd. The parallelogram triangle inequality makes the joint mean norm dominate each component. Hence the L95 gap survives arbitrary `w`, while the common component gives `delta≥sqrt(2)a|w|/16−5sqrt(2)|z|sqrt(n)/64`. Splitting according to which term dominates and using L95 in the other case gives the displayed uniform bound. Its common zero is the normal face `a=0`. `proof/repeated_p3_flat_two_copy.md`; `experiments/repeated_p3_flat_two_copy_jensen.py`. |
| L98 | A trace-zero nonnormal two-copy terminal direction remains strictly descending under every bounded second-order flat recentering | **PROVED WEIGHTED THIRD-ORDER MATRIX-JENSEN THEOREM (2026-07-22)** | For `A_epsilon=I2 tensor C3+epsilon E_D+epsilon²(E_{zI+D1}+I2 tensor W(w))`, the second effective support remains scalar. Differentiating the canonical metric inserts free block `−3sqrt(2)D1/8`. The third traceless support has mean `5(conj(z)D+zD*)/64`, mode two `−3zD/64`, and uncancellable cross coefficient `a(2a²+4|d|²)/128`. Full propagation gives `E3=16(Q3bar−mean lambda_max(Q3)I)`. If `z=0`, the traceless mean vanishes and the cross mode is strict; if `z!=0`, the mean/mode commutator is `−15z²[D*,D]/4096!=0`, excluding a common top vector. Thus every bounded weighted chart is strict; only `a->0` normal-stratum uniformity remains. `proof/repeated_p3_flat_two_copy_weighted.md`; `experiments/repeated_p3_flat_two_copy_weighted.py`. |
| L99 | The L92 cubic mean has the uniform bound `m3≥(2|d|²+|a|²)^(3/2)/(192sqrt(3))` on every nonzero trace-zero two-copy block, including the normal face | **PROVED UNIFORM TERMINAL CUBIC COERCIVITY (2026-07-22)** | Besides L94's cross coefficient, the third support has diagonal Fourier coefficient `3d³/128`. Hence `m3≥max(3|d|³, |a|(2|a|²+4|d|²))/128`. Splitting at `|a|=|d|` gives the displayed bound and endpoint at most `−(2|d|²+|a|²)^(3/2)/(12sqrt(3))`. At `a=0`, the strict gain comes from the convex hull of the two oppositely deformed normal summands. `proof/repeated_p3_flat_two_copy.md`; `experiments/repeated_p3_flat_two_copy_third.py`. |
| L100 | The weighted normal terminal chart has the unique third-order center `w=3conj(d)²/(8sqrt(2))`, and every nonnormal transverse edge obeys `E3≤−a(a²+2|d|²)I/4` even at that center | **PROVED EXACT CENTER + TRANSVERSE COERCIVITY (2026-07-22)** | On `a=z=D1=0`, writing `Q3^0=diag(h,−h)` gives `[q³]h=d(3d²−8sqrt(2)conj(w))/128` and `[q¹]h=−conj(d)(3d²−8sqrt(2)conj(w))/128`; the other modes are conjugate. Thus `Q3^0` vanishes exactly at the displayed center for `d!=0`. For `a>0,z=0`, the mean remains zero and the uncancellable cross coefficient from L98 yields the bound, independent of `w,D1`. This identifies the exact normal/tangential split but does not yet absorb later-scale analytic remainders. `proof/repeated_p3_flat_two_copy_weighted.md`; `experiments/repeated_p3_flat_two_copy_weighted.py`. |
| L101 | A continuous first-order support perturbation `h_epsilon=1+epsilon s+o(epsilon)` has normalized Riemann-map tangent `F_s(z)=s_hat(0)z+2sum_{k≥1}s_hat(k)z^(k+1)`, uniformly for compact families of profiles; no smooth active branch is needed | **PROVED NONSMOOTH CONFORMAL-TANGENT THEOREM (2026-07-22)** | Convex duality first gives the same uniform expansion for the radial function. For `L_epsilon=log(Psi_epsilon(z)/z)`, Caratheodory--Rado boundary convergence and the radial expansion give `Re L_epsilon=epsilon s+o(epsilon)`; the Schwarz integral then gives the analytic tangent. Degenerate Hermitian perturbation supplies a uniform `O(epsilon²)` support remainder for exact linear paths or `O(epsilon²)` matrix remainders; a general `o(epsilon)` matrix remainder gives the `o(epsilon)` support remainder required by the theorem. Functional calculus proves L61's operator expansion without a smooth active branch. The radial proof also applies conditionally after a lower-order analytic recentering has supplied the stated residual radial expansion. `proof/general_similarity_nonsmooth_tangent.md`. |
| L102 | At L100's sharp weighted normal center, the two-copy effective support remains scalar through order four, and the complete fourth coefficient has zero first derivative in the nonnormal Schur edge | **PROVED FOURTH-ORDER SUPPORT PERSISTENCE (2026-07-22)** | The fourth Feshbach coefficient includes the five words `NRNRNRN`, the three placements of `V2` among two `N` factors, `V2RV2`, and the essential energy correction `−M2NR²N`. Exact compression makes its traceless part zero at `w=3conj(d)²/(8sqrt(2))`; replacing any one normal factor by the edge and summing gives zero full compression. Thus the raw support splitting is `O(delta²(r+delta)²)`. This does not control the full similarity-metric endpoint: without an extra metric choice it has a linear `O(delta r³)` term, resolved by L103. `proof/repeated_p3_flat_two_copy_weighted.md`; `experiments/repeated_p3_flat_two_copy_fourth.py`; numerical regression `experiments/repeated_p3_normal_center_probe.py`. |
| L103 | At L100's weighted normal center, a free skew level-zero/level-one block in the third metric cancels the full similarity endpoint's linear transverse fourth-order term | **PROVED SIMULTANEOUS FOURTH-METRIC CANCELLATION (2026-07-22)** | With the free block zero, exact lower/Stein/upper propagation gives `partial_a E4=(25d³/8)[[0,1],[1,0]]`. For a general third-metric block `aU`, the coefficient becomes `(5d/8){5d²+8sqrt(2)(U01−U10)}[[0,1],[1,0]]`; hence `U=5d²[[0,−1],[1,0]]/(16sqrt(2))` cancels it. Arbitrary cubic conformal-response and normal fourth coefficients cancel from the identity, while the normal endpoint is scalar. Thus after simultaneous metric selection the total quartic transverse remainder is `O(delta²(r+delta)²)` and is lower order than L100's cubic gap. Higher-order uniform factorization and the L93/L86 flag lift remain. `proof/repeated_p3_flat_two_copy_weighted.md`; `experiments/repeated_p3_flat_two_copy_fourth_metric.py`; shared propagation `experiments/repeated_p3_third_metric.py`. |
| L104 | At every metric order `k≥2`, the homogeneous free block `Delta Pk=[[0,U,0],[U*,0,2U],[0,2U*,0]]` preserves the active lower/Stein/upper coefficients through order `k` and changes the next endpoint by the universal adjacent-order transfer `5sqrt(2)d[[0,U01−conj(U10)],[conj(U01)−U10,0]]` | **PROVED FORMAL ALL-ORDERS TRANSVERSE TRANSFER (2026-07-22)** | The lower kernel block is zero, while `Delta Pk−C3*Delta Pk C3` has zero active Stein compression because `P12=2P01`; the order-`k` upper endpoint is unchanged. At order `k+1`, convolution pairs the new block only with fixed order-one data, so the transfer is `k`-independent. On a `J`-equivariant recursively selected branch, copy sign symmetry makes every linear edge coefficient Hermitian off-diagonal, in the transfer's range for `d!=0`. Products of the free block reappear at order `2k` and are repaired later. This is formal and fixed-`d`; its inverse grows like `1/|d|`, so it is not a convergence or uniform-neighbourhood theorem. `proof/repeated_p3_flat_two_copy_weighted.md`; `experiments/repeated_p3_flat_two_copy_fourth_metric.py`. |
| L105 | Near `I_m tensor C3`, fixing the complete level-zero/range metric cross block `B` determines a unique real-analytic tight lower/Stein metric `P(T,B)`; the remaining bound `P≤4I` is exactly one analytic `m x m` upper Schur endpoint | **PROVED EXACT ANALYTIC METRIC CHART (2026-07-22)** | Set `P00=I+B(C−I)^−1B*` to make `P−I` tight.  For the Stein Schur complement `F(T,B,C)`, the derivative in `C=[[C11,C12],[C12*,C22]]` at the Crabb metric is `(X11,X12,X22) -> (X11,X12,X22−2X11)`, an invertible real-linear map in every multiplicity.  The analytic IFT supplies `C(T,B)`, after which lower and Stein positivity are automatic.  The upper constraint is equivalent to `E=P22−4I+P2L(4I−PLL)^−1PL2≤0`.  Thus forced metric coefficients converge for every analytic choice of `T,B`; selecting `B` uniformly remains. `proof/repeated_p3_exact_metric_chart.md`; `experiments/repeated_p3_exact_metric_chart.py`. |
| L106 | For `A=N+E` with block-diagonal `N=diag(Bj)`, keeping the actual map `f=phi_A` fixed gives both an exact complete-2 metric for `f(N)` and a uniformly convergent Cauchy expansion of `f(N+E)−f(N)` with an explicit factor `E` | **PROVED FROZEN-DOMAIN TRANSVERSE FACTORIZATION (2026-07-22)** | Each `W(Bj)⊂W(A)`.  If `Sj=phi_j(Bj)` and L73's metric contracts `Sj`, then `f(Bj)=gj(Sj)` for the disk self-map `gj=f∘phi_j^−1`, so the same metric contracts `f(Bj)` by von Neumann; their direct sum contracts `f(N)`.  A fixed contour inside every nearby `W(A)` surrounds both spectra, while `|f|≤1`; the resolvent Neumann series gives homogeneous transverse terms bounded by `C1(C0||E||)^k` uniformly over all normalized maps.  This bypasses differentiation of `A->phi_A`.  A non-automorphic `gj` may create Stein slack; L107 parameterizes it and L108 safely replaces it by zero on the normal anchor.  L110 combines this factorization with the whole-normal inclusion map to prove the two-copy analytic remainder bound. `proof/repeated_p3_frozen_domain.md`; `proof/repeated_p3_terminal_tube.md`. |
| L107 | L105 extends analytically to an arbitrary nearby prescribed Stein Schur complement `H`; after scaling L106's direct-sum normal metric to minimum eigenvalue one, it is exactly the chart point `P(TN,BN,HN)` with `HN≥0` | **PROVED SLACK-COMPATIBLE NORMAL BRIDGE (2026-07-22)** | Solve the same implicit equation as L105 with right side `H`; its Jacobian is unchanged, and Stein positivity is equivalent to `H≥0`.  Each scaled L73 block has `Pj−I≥0` singular with positive physical-level range block, hence zero lower Schur complement.  Its Stein Schur complement `Hj` is PSD even when the disk self-map in L106 is non-automorphic.  Direct sums give `PN=P(TN,BN,HN)` by local uniqueness.  Holding `HN` fixed preserves lower and Stein feasibility under transverse motion, but can lose the upper bound; L108 proves the sharper zero-slack normal tightening. `proof/repeated_p3_exact_metric_chart.md`; `proof/repeated_p3_frozen_domain.md`; `experiments/repeated_p3_exact_metric_chart.py`. |
| L108 | On L106's block-diagonal normal stratum, replacing the inherited Stein Schur slack `HN≥0` by zero in L105's analytic chart preserves `P≤4I`; hence the transverse analysis may use exactly the zero-slack branch of L98--L104 | **PROVED LOCAL ZERO-SLACK NORMAL TIGHTENING (2026-07-22)** | The chart decouples copy by copy.  For one copy and `H=[[h11,h12],[conj(h12),h22]]≥0`, the scalar upper endpoint has base derivative `D_H E=h22+2h11≥tr H≥||H||`.  Uniform analyticity gives `E(Tj,Bj,H)−E(Tj,Bj,0)≥(1−Cs)||H||≥0` nearby.  Since the inherited endpoint at `H=Hj` is nonpositive, the zero-slack endpoint is also nonpositive; direct sums preserve the result in arbitrary multiplicity.  This monotonicity is asserted only on the decoupled normal anchor, not for coupled `H`; L109 computes its sharp weighted effect. `proof/repeated_p3_exact_metric_chart.md`; `experiments/repeated_p3_exact_metric_chart.py`. |
| L109 | At L100's weighted two-copy normal center, zero-slack tightening cancels the complete positive transverse quadratic endpoint and leaves the same sharp negative cubic endpoint on both the frozen normal and actual matrices | **PROVED JOINT NORMAL/TRANSVERSE FINITE-JET CANCELLATION (2026-07-22)** | With `Delta m=5a²/128`, the inherited frozen-normal slack begins with `H2=diag(4Delta m,8Delta m,4Delta m,8Delta m)` and an explicitly derived `H3` containing diagonal `4m3,8m3` terms and signed mixed entries `15sqrt(2)a²d/256`. Retaining a fraction `theta` gives `E2(N)=−5(1−theta)a²I/8`, `E2(A)=5theta a²I/8`, and `E3(N)=E3(A)=−16(1−theta)m3I`. Thus full frozen slack creates the observed positive actual quadratic endpoint, whereas `theta=0` cancels the normal/transverse quadratics and preserves L100's bound `−delta(delta²+2r²)I/4`. Full slack also exactly reproduces the unperturbed normal metric through third order. This is the required joint finite jet, not yet the uniform analytic remainder theorem. `proof/repeated_p3_exact_metric_chart.md`; `experiments/repeated_p3_slack_transfer.py`. |
| L110 | Every sufficiently small two-copy terminal block in L93's flat core admits an exact zero-Stein-slack metric with `I≤P≤4I`; the estimate is uniform as the nonnormal Schur edge collapses onto the normal stratum | **PROVED TWO-COPY TERMINAL-TUBE THEOREM (2026-07-22)** | Pinching to the normal block diagonal gives `W(N)⊂W(A)`.  Because every flat transverse edge has zero base top-support compression, `0≤h_A−h_N≤C delta(r+delta)`.  The whole-normal disk map `h=f_A∘f_N^−1` is therefore `O(delta(r+delta))` from the identity on compacta, and applying it to L108's zero-slack normal certificate creates a PSD chart slack `H_delta=O(delta(r+delta))`.  In the analytic difference `E(T_A,B_A,0)−E(T_N,B_N,H_delta)`, every remainder monomial contains a transverse variable or `H_delta`; after L109's weighted degree-three jet the remainder is `C delta(r+delta)^3+o(delta(r²+delta²))`.  L109 cancels the quadratic and L100 leaves `−delta(delta²+2r²)I/4`, which dominates uniformly.  L95--L100 cover the compact complement of the sharp center.  Arbitrary-copy flag uniformity and the L86 lift remain. `proof/repeated_p3_terminal_tube.md`; exact jet `experiments/repeated_p3_slack_transfer.py`. |
| L111 | A multiplicity-`k` direct sum of one nonnormal `2 x 2` scalar-support copy block has no new equality-preserving tangent moduli: every relation tangent is a unitary-orbit tangent plus the same three-real-parameter two-dimensional tangent on every copy | **PROVED SCALAR-SUPPORT MULTIPLICITY RIGIDITY (2026-07-22)** | Linearize `Z²=alpha I` and `ZZ*+Z*Z=beta I`.  At the square-zero block, writing `X=[[A,B],[C,D]]` gives `C=cI,D=−A,B+B*=bI`; the `A` and skew part of `B` are a commutator, while `(b,c)` are the repeated two-dimensional parameters.  At an invertible nonnormal block, Pauli decomposition of the homogeneous equations leaves `3k²` real dimensions, exactly the dimension of the unitary commutator orbit `(4k²−k²)`.  The full tangent adds only the complex `dot alpha` and real `dot beta`, so has dimension `3k²+3`.  Thus L93 has no hidden large-multiplicity terminal tangent beyond unitary motion and L110's two-copy family.  Normal-stratum collisions and the full flag/L86 lift remain. `proof/repeated_p3_scalar_support_rigidity.md`; `experiments/repeated_p3_scalar_support_rigidity.py`. |
| L112 | At a normal full-common-top scalar-support block `Z0=s diag(Ip,−Iq)`, every equality-preserving tangent is, modulo a copy unitary, a rectangular direct sum of L110's trace-zero two-copy Schur edges and unmatched normal coordinates | **PROVED NORMAL-COLLISION SVD REDUCTION (2026-07-22)** | For `X=[[A,B],[C,D]]`, the linearized scalar relations force only `A=aIp,D=−aIq`; `B,C` are arbitrary.  The skew orbit generated by `K=[[0,L],[−L*,0]]` changes them by `−2s(L,L*)`.  Taking `L=C*/(2s)` leaves the single invariant rectangular edge `R=B−C*`.  An SVD of `R` gives independent blocks `[[s+ta,t sigma_j],[0,−s−ta]]`, exactly L110, plus normal one-dimensional leftovers.  Exact tangent ranks are `4pq+2` full, `4pq` homogeneous, and `2pq` orbit for all checked `p,q≤4`.  Together L111--L112 close every full-common-top multiplicity collision; only proper common-top flag ranks and L86 remain. `proof/repeated_p3_normal_collision.md`; `experiments/repeated_p3_normal_collision.py`. |
| L113 | For each fixed copy multiplicity `m`, the entire L87 flat copy core has a complete-`2` neighbourhood of `I_m tensor C3`, uniformly across all L93 common-top and metric-flag rank changes | **PROVED FIXED-MULTIPLICITY FLAT-FLAG TUBE (2026-07-22)** | The descending flag's exact leakage forms have kernel equal to the next flag space; its stable common-top space reduces `Z` and splits into the one-/two-dimensional strata closed by L110--L112.  Those rigidity lemmas identify the full tangent kernel with unitary/blockwise motion, so a complementary local slice has quadratic flag coercivity `−c s²||R||²`.  For a stable block projection, the sign unitary `P−(I−P)` sends the cross coupling `R` to `−R`; hence every active diagonal endpoint and the whole-domain inclusion slack are even in `R`.  With a normalized complementary support gap, `h_A−h_bd=O(s²||R||²)`, and L105's exact analytic chart has remainder `O(s³||R||²)`, absorbed by the flag form.  When the gap collapses the common-top block is enlarged, one of finitely many lower flag strata.  Compactness gives a finite cover for fixed `m`.  The remaining task is to glue this flat-core tube through L86's non-flat/losing variables. `proof/repeated_p3_flat_flag_tube.md`; dependencies L93/L110--L112. |
| L114 | For every fixed multiplicity `m`, there is a full matrix neighbourhood of `I_m tensor C3` on which the numerical range is a complete `2`-spectral set | **PROVED REPEATED-`C3` LOCAL NEIGHBOURHOOD THEOREM (2026-07-22)** | L61 gives strict first descent off the zero-Jensen face.  On that face, L86's endpoint is the sum of the matrix-Jensen defect, `−8H1²`, common-strong curvature, and three negative winner--loser Gram forms; L87 identifies every residual kernel with reducing flat blocks.  L113 supplies exact certificates on those blocks.  At the remaining zero-margin strata, the blocks are direct sums of L71 disk critical manifolds; L72 gives ambient stationarity, so on a complementary strong slice L86 is the first nonzero term and is coercive after split-off flat kernels.  L105--L110 give the exact zero-slack chart and a remainder `C(s||y||²+||y||³)`; L82 handles losing Slater gaps, and collapsing gaps only move between finitely many winner/flag rank strata.  Compactness of the normalized slice modulo copy unitaries gives a finite cover.  The radius may depend on `m`; this is not the general conjecture or a theorem for larger Crabb block size. `proof/repeated_p3_local_neighbourhood.md`. |
| L115 | At every single `p x p` Crabb block, the L65 equality kernel projects to only one complex direction normal to the local manifold of arbitrary-center circular numerical ranges; it is the elliptic mode represented by `C_p*` | **PROVED DISK-TANGENT INTERSECTION REDUCTION (2026-07-22)** | Lewis--Overton's disk-manifold tangent condition becomes the vanishing of support Fourier modes `2,...,p`.  Intersecting these exact equations with L65 gives `dim((ker e_p intersect T_disk)/O_p)=2p−4`, whereas L66's full quotient has dimension `2p−2`; hence the soft normal image is two-real-dimensional.  Mode one and all residual modes at least three are disk-tangent; mode two loses exactly one complex quotient coordinate.  The representative `C_p*` gives `W(C_p+cC_p*)={z+c conjugate(z):|z|<=1}`, so the survivor is precisely the elliptic deformation.  Full-form ranks for `p=3,...,7` and all mode-factor identities regenerate independently.  This is a reduction, not yet a larger-`p` neighbourhood theorem. `proof/crabb_disk_tangent_intersection.md`; `experiments/crabb_disk_tangent_intersection.py`. |
| L116 | For `T_c=phi(C_p+cC_p*)` and `L=p−1`, the optimal similarity square satisfies `t_*(T_c)>=k(c^(2L))/c^L<4`; equality holds for `p=3,4` and is the precise candidate in every size | **EXACT LOWER BOUND; ALL-SIZE UPPER COMPLETED BY L117 (2026-07-22)** | The degree-`L` Chebyshev--Blaschke product alternates with amplitude `sqrt(k(c^(2L)))` on the elliptic Lobatto spectrum.  DCT alternation gives `B_L(T_c)=+-sqrt(k(c^(2L))) D J D^-1`, proving the exact lower bound.  Jacobi's product makes it strictly below four.  Explicit rank-one Stein metrics prove equality at `p=3,4`, and SDPs recover the formula through `p=10`.  L117 now proves the missing all-size metric rather than importing the unproved parity identities from Li's thesis. `proof/crabb_elliptic_axis.md`; `experiments/crabb_elliptic_axis.py`. |
| L117 | For every `p>=3` and `0<c<1`, `t_*(phi(C_p+cC_p*))=k(c^(2p−2))/c^(p−1)<4`; an explicit diagonal metric has a rank-one Stein defect and attains this value | **PROVED EXACT ALL-SIZE ELLIPTIC CRABB AXIS (2026-07-22)** | At elliptic Lobatto nodes `x_j=sqrt(k)cd(2Kj/L)`, the Szegő Gramian `G_ij=beta_i beta_j/(1−x_i x_j)` obeys `G−XGX=beta beta*`.  Jacobi addition turns it into a DCT-I Toeplitz-plus-Hankel kernel.  Fourier aliasing gives diagonal weights `w_m=S_m/S_0`, `S_m=sum_n sech((m+2Ln)(−log c))`; Poisson summation identifies `w_m=dn(Ktilde m/L|ktilde²)` and yields `w_mw_(L−m)=k(c^(2L))`.  Hence `P=diag(w_m/c^m)` has `P−T*PT>=0`, while the termwise inequality `S_(m+1)>cS_m` proves `I<=P<=k(c^(2L))c^(−L)I`.  L116 supplies the matching lower bound.  A separate 260-decimal DCT/Jacobi checker audits sizes `3,...,30` without an SDP.  This closes the gap stated on Li thesis p.46, but only on the fixed-weight one-parameter axis; L115's transverse tube remains. `proof/crabb_elliptic_axis.md`; `experiments/crabb_elliptic_axis_theorem.py`. |
| L118 | Near every single `C_p`, the locally optimized rank-one Stein Gramian defines a real-analytic feasible envelope `Gamma_p`; its matrix Hessian is exactly L65's form and its restriction to `C_p+cC_p*` is exactly L117's optimum | **PROVED ANALYTIC ENVELOPE + TRANSVERSE REDUCTION (2026-07-22)** | For `P(T,q)=sum_n(T*)^nqq*T^n`, fixing `q_0=1` gives `kappa(P(C_p,q))−4=8 sum_(j<L)|q_j|²+(8/3)|q_L|²+O(||q−e_0||³)`.  The IFT therefore selects a unique analytic defect vector `q_*(A)`.  Equality in L62's Stein Schur complement identifies the Hessian of its condition-number envelope with L65's exact second variation.  L117's rank-one metric is globally optimal and tends to `P_0`, so it lies on this same branch.  Analytic strong-variable maximization leaves precisely L66's `2p−2` quotient, split by L115 into `2p−4` disk-tangent variables plus the elliptic complex normal.  A sufficient pure-axis tube gate is `||Proj_strong grad Gamma(C+cC*)||=o(c^(p−1))`.  Sparse exact series prove the dangerous weighted gradient vanishes in all real directions for `p=3,4,5` and in selected strong directions through `p=6`; the all-size path-length cancellation and disk-flat mixed face remain open. `proof/crabb_rank_one_envelope.md`; `experiments/general_crabb_weighted_series.py`; `experiments/crabb_transverse_weighted_gradient.py`. |
| L119 | On every L117 elliptic Crabb axis, the derivative of the analytic Stein upper envelope equals the derivative of the matching Chebyshev--Blaschke norm lower bound in every matrix direction; the gradient is one endpoint functional-calculus coefficient | **PROVED TOUCHING/ENDPOINT REDUCTION (2026-07-23)** | For fixed axis `c`, set `U_c(T)=kappa(P(T,q_c))` and `R_c(T)=||B_c(T)||²`.  Von Neumann and Stein feasibility give `R_c<=t_*<=U_c`, while L116--L117 give equality at `T_c`; hence the differentiable nonnegative gap `U_c−R_c` has zero derivative there.  Since `B_c(T_c)=sqrt(k(c^(2L)))D J D^-1` has a simple top singular pair `e_0,e_L`, `D Gamma[Y]=2sqrt(k(c^(2L)))c^(−L/2) Re <e_0,D_A[B_c(T(A))][Y]e_L>`.  Jacobi multiplication also gives the exact descent `B_c o phi_c=phi_(c^L) o (2c^(L/2)T_L(z/(2sqrt(c))))`, and the polynomial sends `A_c` to nested `2 x 2` reversal blocks with outer numerical range `E_(c^L)`.  Thus the all-size gate is now a scalar endpoint/Lobatto shape-derivative cancellation, not a metric optimization. `proof/crabb_touching_gradient.md`; `experiments/crabb_touching_gradient.py`. |
| L120 | The L118 envelope gradient on every arbitrary-size elliptic Crabb axis descends exactly to the outer `2 x 2` block of the degree-`p−1` Chebyshev polynomial; it is `O(c^(L+floor(L/2)))`, so the pure elliptic strong tube is strict | **PROVED ALL-SIZE DESCENT GRADIENT + PURE ELLIPTIC TUBE (2026-07-23)** | For `P=P_(L,c)` and `Q=Pi P(A_c)Pi=[[0,2],[2c^L,0]]`, polynomial-fibre conditional expectation gives `Pi h(A_c)Pi=(E h)(Q)`.  The arbitrary-direction differential quadrature `v*Pi DP[Y]Pi v=(1/L)sum_(zeta_j^L=xi)P'(z_j)u_j*Yu_j` shows that first Riemann shape derivatives commute with this fibre trace.  Therefore `D Gamma_p[Y]=(tau'(r)/2) Re((DP[Y])_(L0)−r(DP[Y])_(0L))`, `r=c^L`, `tau(r)=k(r²)/r`.  Dickson recurrence paths make the bracket `O(c^floor(L/2))`; since `tau'(c^L)=O(c^L)`, the full gradient is `o(c^L)`.  L118 then shows strong-variable optimization adds `o(c^(2L))` against the exact `−16c^(2L)` axis gap.  Exact symbolic audits cover arbitrary directions through `p=10` and match three independent sparse coefficients.  Disk-flat mixed variables remain. `proof/crabb_descent_gradient.md`; `experiments/crabb_descent_gradient.py`. |
| L121 | For every L115 disk-flat direction `Y`, L120's endpoint functional gains one full elliptic power: `F_c(Y)=O(c^(floor(L/2)+1))`, hence `D Gamma_p(C_p+cC_p*)[Y]=O(c^(L+floor(L/2)+1))` | **PROVED EXACT FLAT ENDPOINT SELECTION (2026-07-23)** | Root-of-unity filtering gives the full endpoint coefficient formula by circle grade.  The coefficient at the unrestricted first power is exactly `Y_(L0)` when `L` is odd and `sqrt(2)(Y_(L−1,0)+Y_(L,1))` when `L` is even.  L65 equality kills these bottom modes for `p>=4`; L115's circular-tangent constraint kills the exceptional `p=3` combination.  With `m=floor(L/2)+1>L/2`, a coercive disk-flat quartic would absorb the linear mixed term by Young with remainder exponent `4(L+m)/3>2L`.  This is only the linear selection rule; the quartic disk anchor and higher mixed terms remain open.  Exact symbolic audits cover all entries for `p=3,...,16`. `proof/crabb_flat_endpoint_selection.md`; `experiments/crabb_flat_endpoint_selection.py`. |
| L122 | The full L115 disk-flat quotient has an exact Hermitian-Toeplitz disk chart, and an explicit rank-one Stein metric has quartic deficit `32(||z||^4−|z^TJz|²)`; this quartic is nonnegative but noncoercive | **PROVED EXACT DISK CHART + QUARTIC; COERCIVE TARGET DISPROVED (2026-07-23)** | For positive Hermitian `H`, extend by zero and set `K=H+R*HR`, `X(H)=2K^−1/2HRK^−1/2`.  The factorization `K−(conj(w)HR+wR*H)=(I−wR*)H(I−conj(w)R)` and its polynomial null vector prove `W(X(H))=closed unit disk`.  Toeplitz `H=I/2+sZ(z)` gives all `2p−4` disk-flat quotient coordinates.  In coefficient coordinates, the Stein Gramian for `q=He_0` has endpoint generalized eigenvalues `1/2` and `2−16s^4Q_L(z)+O(s^5)`, where `Q_L=||z||^4−|z^TJz|²>=0`.  Equality is the phase-palindromic cone `z=omega J conjugate(z)`.  In `p=4`, exact optimization of the defect vector leaves quartic `−32(|z_1|²−|z_2|²)²`, so no bound `−a||d||^4` is possible.  Exact symbolic regeneration covers `p=3,...,6`, rational directions through `p=10`, and finite noninfinitesimal null spectra through `p=8`.  L123 subsequently proves that the null cone is an exact all-size `t_*=4` equality family; its joint elliptic normal remains open. `proof/crabb_disk_toeplitz_quartic.md`; `experiments/crabb_disk_toeplitz_quartic.py`. |
| L123 | Every positive phase-palindromic point of L122's Toeplitz disk chart has scalar Crouzeix norm exactly `2` and optimal similarity square exactly `t_*=4` in every size | **PROVED EXACT ALL-SIZE DISK EQUALITY FAMILY (2026-07-23)** | Normalize `H_(jj)=1/2`, set `K=H+R*HR`, `A=2K^−1HR`, `q=He_0`, and `r=J conjugate(q)`.  After a diagonal phase gauge, `A` has the explicit companion columns `Ae_0=0`, `Ae_1=2e_0`, `Ae_j=e_(j−1)+2z_(j−1)(e_0−e_L)`.  The exact metric `M=K−qq*+2rr*` obeys `M−A*MA=qq*` and has generalized spectrum `{1/2,1^(p−2),2}`, proving the complete upper square four.  If `det(xi I−A)=xi g(xi)`, then `B=g/g^sharp` is a finite Blaschke product and `B(A)=4e_0r*`; its `K`-norm is exactly two, proving the matching scalar lower bound.  Observability is exact because `det[q*;q*A;...;q*A^L]=2^(L−1)det H`, so all roots of `g` are strictly in the disk.  The union over phase is stratified and singular at the Crabb point; its joint elliptic/non-palindromic normal remains open. `proof/crabb_palindromic_equality.md`; `experiments/crabb_palindromic_equality.py`. |
| L124 | L122's quartic is exactly the squared normal distance to L123's stratified equality cone times its radial amplitude: `Q(z)=4d(z,E)^2(||z||²−d(z,E)²)` | **PROVED EXACT BEST-PHASE NORMAL FORM (2026-07-23)** | With reversal conjugation `Cz=J conjugate(z)`, choose `eta=(z^TJz)/|z^TJz|` and split `u=(z+eta Cz)/2`, `v=(z−eta Cz)/2`.  Then `eta C` fixes `u` and negates `v`, the summands are real-orthogonal, `||u||²=(||z||²+|z^TJz|)/2`, and `||v||²=(||z||²−|z^TJz|)/2`.  Projection onto the fixed space of each antiunitary involution proves `dist(z,E)=||v||`, hence `Q=4||u||²||v||²` and `2||z||²d²<=Q<=4||z||²d²`.  This gives quadratic normal coercivity away from the apex but proves why no uniform Morse--Bott coefficient exists at `z=0`. `proof/crabb_palindromic_normal_form.md`; `experiments/crabb_palindromic_normal_form.py`. |
| L125 | If `Psi_c(z)=sum_n b_n(c)z^(2n+1)` is the normalized disk-to-ellipse map, then `b_n(c)=c^n(C_n+O(c²))`; equivalently the direct map has coefficients `(-1)^nc^n+O(c^(n+2))` | **PROVED EXACT NEWTON EDGE (2026-07-23)** | Differentiating the classical elliptic formula gives `(Psi')²(k−z²)(1−kz²)=alpha²(4c−Psi²)`.  Theta identities make `k/c`, `alpha`, and the linear coefficient analytic in `c²`.  The blow-up `Psi=zH(c,cz²)` yields a regular even-`c` coefficient recursion.  At `c=0` it reduces to `(H+2yH')²(1−4y)=H²`, whose normalized solution is the Catalan generating function.  Series reversion and `C=1+xC²` give the direct bottom edge `z/(1+cz²)`.  This proves A84's scalar grade selection, not its optimized Stein coefficient. `proof/ellipse_inverse_newton_edge.md`; `experiments/ellipse_inverse_newton_edge.py`. |
| L126 | In size `2k+1`, the degree-`k` Dickson polynomial sends the entire central phase-palindromic equality/ellipse pencil to an exact reducing physical size-three family at parameter `c^k` | **PROVED EXACT ALL-SIZE FAMILY DESCENT (2026-07-23)** | For `S=C+cJCJ`, `E=2(e_0−e_(2k))(e_(k+1)^*−ce_(k−1)^*)`, and `V=span{e_0,e_k,e_(2k)}`, `P_k(S+aE)=P_k(S)+aDP_k(S)[E]` exactly: every multiple-`E` path is too long.  Both complete matrices reduce `V`, with blocks `[[0,2,0],[c^k,0,1],[0,2c^k,0]]` and `[[-2c^k,0,2],[0,0,0],[2c^k,0,−2]]`.  The Toeplitz coordinate Gramian also reduces `V` and restricts to the exact size-three Gramian.  Hence the Chebyshev--Blaschke image has an orthogonal physical `3×3` equality summand for every amplitude.  Full rank-one metrics numerically reduce the same space, restrict exactly to the size-three optimum, and have all inner levels strictly between its endpoints; proving that metric splitting remains open. `proof/crabb_central_dickson_descent.md`; `experiments/crabb_central_dickson_descent.py`; `experiments/crabb_central_metric_descent.py`. |
| L127 | Primal and dual Stein maps transfer exactly through a finite Blaschke product: `P_T(q)=L_(B,T)(P_(B(T))(q))`, and `R(Z)−TR(Z)T*=Z−B(T)ZB(T)*` | **PROVED EXACT POSITIVE PRIMAL/DUAL TRANSFERS (2026-07-23)** | The model-kernel identity `sum f_j(z)conj(f_j(w))=(1−B(z)conj(B(w)))/(1−zconj(w))` gives `L(X)−T*L(X)T=X−B(T)*XB(T)` and its adjoint-orientation companion for `R(Z)=sum f_j(T)Zf_j(T)*`.  Taking the unique primal Stein Gramian yields the rank-one composition formula; the dual transfer preserves L21's Hermitian Stein difference and trace ratio exactly.  These maps preserve positivity, but not automatically the original metric condition number.  A89 reduces that remaining central issue to one cross-fiber cancellation. `proof/blaschke_stein_composition.md`; `experiments/blaschke_stein_composition.py`. |
| L128 | After removing L126's outer size-three block, the complete central descended pencil is one two-layer operator algebra `[[H,I],[rI,H]]+2a[[-rI,H],[H,−I]]` with `H²=rI`; its coordinate Gramian is `[[I,2aI],[2aI,I]]` | **PROVED EXACT ALL-SIZE INNER NORMAL FORM (2026-07-23)** | In layer coordinates `L_j=e_j`, `R_j=e_(k+j)`, L126's full columns give `H=J diag(c^(k−1),...,c)` and the displayed block pencil directly; `H²=c^kI`.  The central Toeplitz Gramian couples only equal residues and gives the second formula.  The gauge `diag(c^(j/2))` sends `H` to `sqrt(r)J`, exposing only two algebraic `2×2` fiber types, although that gauge is not physical-unitary and therefore does not itself control condition numbers.  The metric-lift gate is now dimension-free operator-valued, not an arbitrary `(2k−2)`-dimensional induction. `proof/crabb_central_inner_fibers.md`; `experiments/crabb_central_dickson_descent.py`. |
| L129 | In a uniform neighbourhood of the central Crabb apex, every descended inner fiber has similarity square below three while the exact size-three outer block has similarity square above three; hence `t_*(B_(k,c)(T_k(a,c)))=t_*(T_1(a,c^k))` | **PROVED UNIFORM INNER INACTIVITY (2026-07-23)** | L128 has `||H||=c`, `r=c^k<=c²`, so its inner disk operator tends uniformly to `N=[[0,I],[0,0]]` and its Gramian tends to `I`.  The fixed metric `diag(I/2,I)` has Stein slack `I/2` at `N` and condition square two; it remains feasible with condition below three uniformly in `k`.  The outer size-three value tends to four.  Orthogonal direct-sum reduction then proves the identity for the descended operator.  This identifies the sole active block but does not control the condition cost of L127's lift back to `T`. `proof/crabb_central_inner_inactivity.md`. |
| L130 | For every fixed `k`, the complete central family has exact local metric descent `t_*(T_k(a,c))=t_*(T_1(a,c^k))`; its full rank-one metric is constructed from the outside critical factor of the degree-`k` Blaschke product | **PROVED EXACT CENTRAL METRIC LIFT (2026-07-23)** | For real `B=N/D`, factor `N'D−ND'=kappa Q Q#` with `Q` zero-free on the disk and set `F=Q/D in K_B`.  Residues at the outside critical points prove the basis-free trace identity `(1/k)sum_(B(t)=u)f(t)/F(t)=<f,F>/||F||²`.  Independently, the central pencil satisfies `Pi P_m(S(a,c))Pi=0` for every Dickson grade `1<=m<k`; polynomial division transfers the fiber trace to a scalar outer compression.  If `F=sum gamma_jf_j`, `J=F(T)^−*O`, and `q=Jv3`, then `q_j=f_j(T)^*q=(gamma_j/||F||²)Ov3+w_j` and `sum gamma_jw_j=0` exactly.  Thus the full Stein metric reduces the outer space and restricts to a scalar copy of the optimal size-three metric.  At the apex all inner levels equal two between endpoints one and four, so continuity and L129 prove the equality locally.  This closes the central `−64a²c^(2k)` event, not the noncentral-window localization or mixed-grade polarization. `proof/crabb_central_dual_lift.md`; three regenerating checkers. |
| L131 | The degree-`L` Faber--Dickson transform of every phase-palindromic equality tangent has zero endpoint derivative from the matrix perturbation; its explicit coefficient derivative is the coefficient vector at the top endpoint and its grade-weighted reflection at the bottom | **PROVED EXACT ALL-SIZE ENDPOINT REFLECTION (2026-07-23)** | For `S0=C+cJCJ`, `E_j=2(e0−eL)e_(j+1)*`, and the physical-adjoint tangent `H_j=E_j+cJE_(L−j)J`, differentiated Dickson recurrence gives `e0*DP_L(S0)[H_j]=eL*DP_L(S0)[H_j]=0`.  More explicitly, the two endpoint paths at every intermediate grade have the closed folded formulas (4a)--(4b) and coincide at grade `L`.  Hence for `g_a=xi^L+2a sum u_jxi^j`, the Faber transform has amplitude endpoint rows `4sum u_je_j*` and `4sum u_jc^je_(L−j)*`; the reflected-row energy is exactly `16sum|u_j|²c^(2j)`.  The scalar Joukowski identity gives the same result by Parseval and kills mixed grades.  This identifies A84's Hardy face algebraically, including complex directions, but still requires a metric-normal-form theorem showing that all non-endpoint Faber rows are inactive above the first Newton face. `proof/crabb_faber_reflection.md`; `experiments/crabb_faber_reflection.py`. |
| L132 | If the attacked grade `k` divides the Crabb length `L=qk`, degree-`k` Dickson descent gives an exact size-`q+1` first-offset outer family and a condition-preserving lift of its L118 rank-one metric | **PROVED EXACT DIVISIBLE-GRADE RANK-ONE LIFT (2026-07-23)** | The residue-zero space `V=span{e_0,e_k,...,e_(qk)}` reduces `P_k(S_(L,k)(a,c))`, and its block is exactly `S_(q,1)(a,c^k)`; every cross block vanishes, the polynomial is affine in amplitude, `Pi_VP_m(S)Pi_V=0` for `m<k`, and the Toeplitz coordinate Gramian restricts to the size-`q+1` Gramian.  Other residue classes are unweighted nilpotent shifts at the apex and remain inactive.  L130's critical-factor trace and defect reconstruction work with outer dimension `q+1`, producing a full rank-one metric whose condition is exactly the outer L118 envelope and whose inner levels stay between its endpoints.  Thus `t_*(T_(q,1))<=t_*(T_(L,k))<=Gamma_(q+1)(T_(q,1))` locally.  All polynomial, Gramian, and metric identities allow a complex one-pair coefficient `(u,conjugate(u))`; an independent `u=i` checker audits this extension.  Equality of the two `t_*` values is asserted only for `q=2`; global optimality of the rank-one branch is not known in general. `proof/crabb_divisible_dickson_descent.md`; real/complex exact and floating metric checkers. |
| L133 | In every size `L+1>=3`, the real phase-one first-offset equality/ellipse path has optimized rank-one amplitude Hessian `-64c²+O(c³)` | **PROVED UNIVERSAL OFFSET-ONE FACE (2026-07-23)** | In coefficient coordinates the disk defect seed is `2 sum_(j in {1,L−1})e_j`; the unique elliptic stationarity correction through order one is `−8ce_3` (omitted/folded on short chains).  The exact second-amplitude Stein forcing stabilizes for `L>=7` at diagonal `(48,−96,−32,16,0,...,0,16,−32,64)`; short lengths two through six are evaluated separately.  Stein inversion gives endpoint metric coefficients `(48,128)`, endpoint eigenvector couplings start at `c²`, and the generalized ratio coefficient is `128−4(48)=−64`.  L118's unit defect Hessian makes the explicit jet agree with the analytic optimizer through this coefficient.  Combined with L132, this transfers the real phase-one upper face to every divisor grade.  Complex-phase polarization, nondivisor localization, and a uniform analytic remainder remain open. `proof/crabb_offset_one_face.md`; `experiments/crabb_offset_one_face.py`. |
| L134 | The universal offset-one face is phase-isotropic: for an arbitrary one-pair coefficient `(u,conjugate(u))`, its amplitude Hessian is `-64|u|²c²+O(c³)`; L132 transfers the upper face to every divisor grade | **PROVED COMPLEX ONE-GRADE POLARIZATION (2026-07-23)** | For `u=i`, write the companion tangent as `iF`; the exact defect jet is `i(-2e_1+2e_(L−1)+8ce_3)`.  A rational real/imaginary-pair Stein calculation gives the same stable forcing and endpoint pair `(48,128)`, including all short collisions.  The defect-gradient residual is `O(c²)`, hence the optimized coefficient is `−64`.  The coefficient is a real quadratic form in `u=x+iy`; entrywise conjugation sends `y` to `−y` and preserves the condition, killing the mixed `xy` term, while L133 and the imaginary calculation give equal diagonal coefficients.  L132's Dickson/Gramian reduction and critical-factor metric reconstruction are complex-linear in the outer defect, so the rank-one upper face `−64|u|²c^(2k)` holds for every `k|L`.  Cross-grade polarization, nondivisor localization, and uniform remainder control remain open. `proof/crabb_offset_one_complex_face.md`; exact complex offset/descent checkers. |
| L135 | In L117's exact DCT-I eigenbasis of the elliptic Crabb axis, the grade-`j` companion row is a scaled DST-I mode: `(v_j^*R)_n=-2sqrt(2/L) eps_n c^((j+1)/2) sin(j theta_n)sin(theta_n)` | **PROVED EXACT SPECTRAL SINE FACTORIZATION (2026-07-23)** | With `K_0=diag(eps_m²)`, `D=diag(c^(m/2))`, and DCT-I matrix `U`, the right eigenvector matrix is `R=K_0^(−1/2)DU`.  Endpoint factors cancel rowwise, so `e_m^*R=sqrt(2/L)c^(m/2)(eps_n cos(m theta_n))_n`.  Subtracting rows `j+1` and `j−1` in `v_j^*=e_(j+1)^*−ce_(j−1)^*` gives the displayed identity by cosine subtraction.  Standard DST-I orthogonality then separates all grades exactly in this spectral leg, with no terminal alias.  This is the coordinate bridge for A95, not yet the optimized-Hessian theorem: L118's defect Schur kernel must still be proved DST-I diagonal after the common sine factors are removed. `proof/crabb_spectral_sine_factorization.md`; high-precision regression through size 20. |
| L136 | If `d` divides both the Crabb length `L=dq` and a one-pair grade `k=ds`, degree-`d` Dickson descent reduces the complete polynomial/Gramian pencil to the size-`q+1` grade-`s` family at parameter `c^d`, and the L132 critical-factor lift gives the same rank-one sandwich | **PROVED EXACT GCD DESCENT (2026-07-23)** | Multiplication by `P_d` preserves residues modulo `d`; on residue zero, dividing indices by `d` gives exactly `S_(q,s)(a,c^d)` and `K_(q,s)(a)`.  Every subcritical Dickson compression vanishes, and two amplitude insertions cannot return within a degree-`d` word, proving exact affinity.  The other residue blocks are strict unweighted shifts at the apex.  L132's critical-factor trace identity and defect reconstruction therefore apply verbatim, giving `t_*(T_(q,s)(a,c^d)) <= t_*(T_(L,k)(a,c)) <= Gamma_(q+1)(T_(q,s)(a,c^d))`.  Taking `d=gcd(L,k)` reduces all remaining one-grade work to coprime `(q,s)`.  This is a sandwich, not general equality of similarity optima. `proof/crabb_gcd_dickson_descent.md`; exact grid through `d=5,q=8`. |
| L137 | L117's elliptic-axis rank-one defect has only even coefficient coordinates, with Newton edge `d_(2r)=4(−1)^r c^r+O(c^(r+2))` for `1<=r<L/2` | **PROVED EXACT FOURIER EDGE (2026-07-23)** | In spectral nodes the defect is `beta_n=eps_n k'/dn(v_n)`.  DLMF 22.11.6 gives `k' nd(v)=pi/(2K)+(2pi/K)sum_(s>=1)(−1)^s q^s/(1+q^(2s)) cos(s pi v/K)`, with `q=c²`.  DCT-I sampling has only modes `2s`; conjugation by L135's `R=K_0^(−1/2)DU` contributes `c^(−r)`, so the mode ratio is `4(−1)^r c^r/(1+c^(4r))` plus strictly higher terminal aliases.  Thus the associated spatial series is `(1−3cS²)/(1+cS²)`.  This proves the axis-defect half of A98's completed-square constants, not the LDL Hessian factor or Faber coupling. `proof/crabb_axis_defect_fourier.md`; exact finite regression. |
| L138 | Every rank-one Stein metric on an L117 elliptic Crabb axis has exact homogeneous DCT normal form `K_0^(−1/2)M(q)K_0^(−1/2)=D^(-1)A_hWA_h^*D^(-1)`, and its pure-defect condition Hessian is the explicit endpoint Schur form (4)–(5) | **PROVED EXACT SPECTRAL CONGRUENCE + FINITE HESSIAN REDUCTION (2026-07-23)** | With `y=R^*q`, `h=y/beta`, and `A_h=U diag(h)U^*`, the spectral Stein Cauchy kernel factors entrywise and conjugation by `R=K_0^(-1/2)DU` proves the metric identity directly.  For `h=1+as`, this is `P(a)=(I+aB)P_0(I+aB)^*`.  The two DCT endpoint rows have identical squared entries, so every homogeneous defect direction has zero first condition variation; standard Hermitian perturbation leaves two explicit endpoint Schur sums in the Toeplitz-plus-Hankel matrix `U diag(s)U^*`.  Direct Stein solves and condition finite differences pass nine cases.  L139 subsequently derives the LDL edge from this formula; the Faber coupling is still open. `proof/crabb_homogeneous_defect_normal_form.md`; independent checker. |
| L139 | The all-size pure-defect condition Hessian is exactly parity separated and its LDL Newton edge is the finite-path outer factor `(1+cS²)/(1−cS²)`, with interior coefficient `2` and terminal coefficient `4/3` | **PROVED EXACT MODAL REDUCTION + ALL-SIZE NEWTON EDGE (2026-07-23)** | L138's endpoint Schur sums use only the two DCT endpoint rows, so raw cosine modes decouple exactly.  Their weights begin `4c^(−j)` in the interior and `(8/3)c^(−L)` at the endpoint.  Jacobi's `dn/k'` series has only even modes and transfers coordinate `i` to mode `j=i+2r` with coefficient `2c^r`, half of its Fourier coefficient four.  The unique leading shared mode gives Hessian entries `8c^r` or terminal `16c^r/3`; lower LDL products have strictly higher valuation, leaving factors `2c^r` and `4c^r/3`.  The original exact Stein-series audit independently regenerates all constants and parity zeros at length eight.  This proves the pure-defect factor, not the linear/constant Faber completed square. `proof/crabb_defect_hessian_edge.md`; `experiments/crabb_defect_hessian_factor.py`. |
| L140 | L131's first negative Faber frequency `2au_kc^k zeta^(−k)` has a unique degree-`L` finite-Blaschke numerator tangent, obtained by adding `2a conjugate(u_k)c^kP_(L−k)` | **PROVED EXACT HARDY-REFLECTION CENTER (2026-07-23)** | If `B=N/N#` and `delta N=alpha w^(L−k)`, then on the circle `delta B/B=alpha w^(−k)−conjugate(alpha)w^k`.  Fourier orthogonality therefore forces `alpha=2au_kc^k`; no other numerator coefficient can absorb the negative mode.  For the real phase-one family this is exactly A99's correction `lambda=2`, explaining analytically why the dual loss parabola is centered there.  This proves the center, not the observed curvature `4+|lambda−2|²/4` or primal complementary slackness. `proof/crabb_faber_blaschke_dual.md`. |
| L141 | In A99's associated Hardy model, a reflected-coefficient mismatch `epsilon=(lambda−2)ac^k` lowers the squared Crabb norm by exactly `4|epsilon|²+O(|epsilon|⁴)` | **PROVED EXACT TWO-COLUMN SINGULAR PERTURBATION (2026-07-23)** | For `B_epsilon(w)=(w^L+epsilon*w^(L−k))/(1+conjugate(epsilon)w^k)`, nilpotence gives the exact identity `B_epsilon(C)=(1−|epsilon|²)C^L+epsilon*C^(L−k)`.  Only columns `L−k` and `L` couple to the simple top singular value.  Their explicit `2×2` Gram block has largest eigenvalue `4−4|epsilon|²+O(|epsilon|⁴)`; every other eigenvalue is at most `2|epsilon|²`.  Thus the `4|lambda−2|²a²c^(2k)` part of A99's loss parabola is now exact.  The corrected base loss `64a²c^(2k)` and primal complementary slackness remain open. `proof/crabb_faber_blaschke_dual.md`. |
| L142 | On every central collision `L=2k`, A99's corrected root Blaschke product has exact mixed norm coefficient `[a²c^(2k)]||B(T)||²=−64` | **PROVED EXACT DICKSON COMPOSITION + SIZE-THREE SYMBOL (2026-07-23)** | `P_(2k)+2a(1+c^k)P_k=Q_(a,r)(P_k)` with `r=c^k` and `Q=y²+2a(1+r)y−2r`; hence the corrected degree-`2k` inner is exactly the degree-two root inner composed with `B_(k,c)`.  L126 reduces the latter to its physical size-three outer block, while L129 makes all inner fibers inactive.  Using L125's complete scalar Newton data relevant to `a²r²`, exact generalized singular perturbation has no terms through total degree three and gives coefficient `−64` at `a²r²`.  This proves A99's base curvature centrally; noncentral localization and primal complementary slackness remain. `proof/crabb_faber_blaschke_dual.md`; `experiments/crabb_central_faber_blaschke.py`. |
| L143 | For every noncentral grade `1<=k<L/2`, A99's corrected root Blaschke product has `[a²c^j]||B(T)||²=0` below `j=2k` and coefficient `−64` at `j=2k` | **PROVED FORMAL WEIERSTRASS/ENDPOINT INDUCTION (2026-07-23)** | Prepare `G(Psi_c(w))=N(w)O(w)` coefficientwise, evaluate `N/N#` on L123's exact pencil, and lift the simple top generalized singular pair.  The first mixed operator face is the sparse translation-invariant formula (29).  The singular-vector coupling starts strictly above grade `k`, so its Schur square misses `2k`.  The only tail fold occurs at degree `d=L−k`: it contributes `+8c^d` through the quadratic numerator and `−8c^d` through the coordinate metric, cancelling even at the resonance `L=3k`.  The surviving top Gram coefficient is `−32c^(2k)`; division by the terminal coordinate weight `1/2` gives `−64`.  This proves the corrected base curvature for all one-grade lengths; mixed-grade polarization, uniform remainder, and primal complementary slackness remain. `proof/crabb_faber_blaschke_dual.md`; `experiments/crabb_faber_blaschke_formal.py`. |
| L144 | The corrected multi-grade Faber--Blaschke dual Hessian has diagonal principal face `−64 sum_k |u_k|²c^(2k)` with no cross term between distinct grades | **PROVED FORMAL COMPLEX POLARIZATION (2026-07-23)** | Polarizing L143's preparation puts distinct first faces on distinct shift/Fourier grades; their top Gram pairing is zero, and their singular-vector Schur product starts strictly above `c^(k+ell)`.  A terminal fold can hit the mixed face only at `L=2k+ell` or `L=k+2ell`; in both cases the quadratic numerator contributes `+8` and the coordinate metric `−8`.  Complex cross monomials have nonzero circle grade and are annihilated by the endpoint functional.  Thus the diagonal entries are exactly L142--L143's `−64` and every mixed principal coefficient vanishes.  Exact combined-minus-separate series vanish through twice the larger grade in six cases covering both resonances and central collisions.  Uniform remainder and primal complementary slackness remain. `proof/crabb_faber_blaschke_dual.md`; `experiments/crabb_faber_blaschke_mixed.py`. |
| L145 | The corrected one-grade Blaschke product canonically determines a rank-one Stein defect whose condition Hessian agrees with its dual norm through `c^(2k)` | **PROVED MODEL-SPACE COMPLEMENTARITY (2026-07-23)** | For `B=N/D` and top right singular vector `x`, choose `q` orthogonal to `D(T)^(−1)span{x,Tx,...,T^(L−1)x}`.  L127's model-kernel identity then makes `P_T(q)−B(T)^*P_T(q)B(T)` annihilate `x`, giving exact Rayleigh complementary slackness.  The orbit complement is analytic and one-dimensional at the axes.  Differentiating it and using L143's singular-vector valuation shows `kappa(P_T(q))−||B(T)||²` has no `a²c^j` term for `j<=2k`.  Exact rational regeneration covers central/noncentral grades through four and gives matching coefficient `−64` in every case. `proof/crabb_faber_blaschke_dual.md`; `experiments/crabb_faber_blaschke_complementarity.py`. |
| L146 | The full optimized rank-one Stein Hessian has the same all-grade principal face as L144's dual: `−64 sum_k|u_k|²c^(2k)` | **PROVED BY PSD ZERO-DIAGONAL POLARIZATION (2026-07-23)** | For each fixed `c`, the primal-envelope Hessian minus the corrected-inner Hessian is PSD because the former is an upper bound and the latter a lower bound, touching on the axis.  L125's filtration makes entry `(k,ell)` divisible by `c^(k+ell)`, so diagonal congruence by `diag(c^(−k))` has a PSD associated limit.  L145 makes every diagonal entry of that limit zero; every `2×2` principal minor then forces every cross entry zero.  Hence the primal and dual principal matrices coincide.  This proves A98's completed-square minimum and all mixed coefficients; a uniform analytic remainder is still needed for the full local tube. `proof/crabb_faber_blaschke_dual.md`. |
| L147 | The negative primal face has a uniform convergent remainder on the entire local phase-palindromic equality stratum: `Gamma_eq(u,c)<=−8c^(2L)−32 sum_k|u_k|²c^(2k)` | **PROVED BY L149--L150 REFLECTED HARDY/MODEL LIFT (2026-07-23)** | The first draft's all-amplitude path assertion was correctly downgraded.  L149 subsequently constructs the convergent marked Hardy algebra and proves that both the zero- and one-reflection sectors vanish, using `q*(zI−A)^(−1)e_L=1/(zB(z))` to turn the top norm derivative into the real mean of a logarithmic inner tangent.  L150 uses L145's analytic orbit-complement metric; its nonnegative gap above the dual norm vanishes on the equality divisor and is therefore automatically quadratic in reflected variables.  L145--L146 identify the compact face, and convergence gives the uniform half-face estimate. `proof/crabb_reflected_hardy_lift.md`; audit trail and checks in `proof/crabb_uniform_weighted_remainder.md`. |
| L148 | At every nearby phase-palindromic disk-equality anchor, the ordinary first ellipse-pullback derivative of both the sharp prepared Blaschke norm and L118's touching Stein envelope is zero | **PROVED GRADE-ONE COMPANION ENDPOINT STATIONARITY (2026-07-23)** | In coefficient coordinates the first pulled-back operator tangent is `E=A^dagger_K−A^3`.  Variation of the finite inner function at fixed `A` is stationary because the disk theorem bounds it by two and L123 attains two.  For the operator variation, differentiating `B=g/g#`, using `B(A)=4e_0r*`, and running the companion endpoint recurrence gives `q*Dg(A)[E]d(A)^(−1)e_L=2r*Dd(A)[E]d(A)^(−1)e_L`; hence the top singular derivative vanishes.  The touching sandwich transfers it to `Gamma_eq`.  This is the explicit grade-one base case of L149's subsequently proved all-grade Hardy resolvent identity.  Symbolic checks cover generic coefficients through `L=4`; 18 further exact rational cases reach `L=10`. `proof/crabb_equality_normal_stationarity.md`; exact checker. |
| L149 | The complete sharp prepared Blaschke deficit on the equality/ellipse family belongs to the square of the convergent reflected ideal `(c^L,c^ku_k,c^k conjugate(u_k))` | **PROVED ALL-GRADE HARDY RESOLVENT TRANSFER (2026-07-23)** | L131 gives exact negative Hardy coordinates and monic Weierstrass division gives their convergent marked lift at arbitrary equality amplitude.  On the zero-reflection divisor L123 is exact.  For one reflected leg, the companion adjugate identity `q*(zI−A)^(−1)e_L=g#/(zg)=1/(zB)` turns the total prepared norm derivative into the real circle mean of `dot(Theta)/Theta`, `Theta=zB`.  A fixed-degree inner tangent has purely imaginary logarithmic boundary derivative, so the real mean is zero for every grade, phase, and central fold.  Thus only two reflected legs can contribute.  Exact finite-amplitude records through grade three/dimension nine independently verify the support. `proof/crabb_reflected_hardy_lift.md`; `experiments/crabb_finite_amplitude_dual_filtration.py`. |
| L150 | L145's canonical model-complement defect gives an arbitrary-amplitude primal upper certificate with the same compact reflected face as L149 and a uniformly dominated remainder | **PROVED NONNEGATIVE MODEL-GAP LIFT (2026-07-23)** | The orbit complement `q perpendicular D(T)^(−1)span{x,...,T^(L−1)x}` is analytic because L123's observability rank persists.  Its Stein metric satisfies exact complementary slackness, so `Delta=kappa(P_T(q))−||B(T)||²>=0`; on the zero-reflection divisor it is L123's exact equality metric, hence `Delta=0`.  In the real reflected variables analyticity and nonnegativity force `Delta` into the ideal square.  L145 makes each compact one-grade diagonal zero, and PSD zero-diagonal polarization removes every cross term, including the L117 axis.  Therefore the model upper face is `−16|c^L|²−64sum|c^ku_k|²`; convergence absorbs all higher terms uniformly. `proof/crabb_reflected_hardy_lift.md`. |
| L153-old | Over every nearby Toeplitz disk point, L145's orbit-complement condition equals the characteristic Blaschke norm and the corrected dual norm has zero first reflected derivative | **DISPROVED EXACTLY (2026-07-23)** | The two assertions fail independently.  At `(z1,z2)=(1/20,1/30)`, the model kernel annihilates the top line but the left singular line has a nonzero rational generalized-eigenvector residual, so the condition is strictly larger than the dual norm.  At the same point, the exact corrected first-reflection derivative is a nonzero rational about `−8.48e−6`.  Both gaps were hidden by small-amplitude binary64 tolerances. `proof/crabb_disk_flat_marked_merger.md`; `proof/crabb_disk_one_reflection.md`. |
| L154 | The first corrected reflected coefficient over the Toeplitz disk chart obeys `|partial_c R(z,0)|<=C_L Q(z)` locally, and analogously each marked grade-one sector is `O(|c|^kQ)` | **PROVED BY ENDPOINT FIRST JET + CONE DIVISION (2026-07-23)** | L156 proves that the endpoint-resolvent residual has zero full disk-coordinate first jet on the equality cone.  L149 expresses every one-reflection marked coefficient as a fixed finite functional of that residual, and L155 converts value-plus-gradient vanishing into the uniform `O(Q)` estimate through the singular apex.  The explicit mark contributes `|c|^k`. `proof/crabb_disk_one_reflection.md`; `proof/crabb_quartic_cone_division.md`; exact checker. |
| L155 | If a real-analytic germ `F` and its full gradient vanish on the phase-palindromic equality cone, then `|F(z)|<=C_L Q(z)` locally | **PROVED ANALYTIC CONE DIVISION; APEX HYPOTHESIS REMOVED (2026-07-23)** | After an orthogonal real change, the equality cone is the rank-one cone of a generic `2×(L−1)` matrix.  Degreewise first-order vanishing puts each Taylor term in the second symbolic power of the maximal-minor ideal; its symbolic and ordinary powers agree, so no term below degree four exists.  L124 then gives `z=u+v`, `Q=4||u||²||v||²`, and normal Taylor expansion gives `|F|=O(Q)` uniformly through the apex.  A separate subquartic apex recurrence is unnecessary. `proof/crabb_quartic_cone_division.md`. |
| L156 | The endpoint-resolvent residual `q_z*(xi I-A_z)^(-1)e_L-g_z#/(xi g_z)` has zero value and full disk-coordinate gradient at every phase-palindromic Toeplitz point | **PROVED ALL-SIZE ENDPOINT RECURRENCE (2026-07-23)** | In the phase-one gauge, an arbitrary Hermitian Toeplitz tangent satisfies the inverse-free three-boundary-piece formula `K dotA=e0 alpha(h)+2v_h alpha(u)−eL alpha(h^flat)`.  Explicit right/left companion endpoint vectors telescope its bilinear pairing to `xi(P_h g−2g#P_h^flat)`.  A rank-one adjugate argument at the roots of `g`, followed by polynomial continuation, gives `D_hg=2P_h^flat` and `D_hg#=2P_h`; direct resolvent differentiation then proves the two first jets coincide.  The four inverse-free recurrences and the stronger ambient Gram-gradient identity are audited exactly at generic complex anchors through length ten. `proof/crabb_disk_one_reflection.md`; `experiments/crabb_disk_one_reflection_jets.py`. |
| L157 | On the unreflected Toeplitz disk chart, L145's normalized orbit-complement metric differs from the characteristic Blaschke square by at most `C_LQ(z)^2` | **PROVED ENDPOINT-TO-ORBIT TRANSFER + TEMPLE SQUARING (2026-07-23)** | If `T=q_can*adj(xi I−A)e_L` and `D=g#`, L156 and L155 give coefficientwise `T−D=O(Q)`.  Laurent moment extraction shows every failure of `q_can` to annihilate `D(A)^(−1)span{e_L,...,A^(L−1)e_L}` is a finite linear functional of `T−D`; observability therefore gives `d_model−q_can=O(Q)`.  With `d_0=1/2`, the model Gramian has lower Rayleigh residual `(d_model−q_can)/2`; the uniform `{1/2,1,...,1,2}` equality spectral gap squares it to `lambda_min=1/2−O(Q²)`.  Exact complementarity fixes `lambda_max=||B(A)||_K²/2`, hence `0<=Delta(z,0)<=C_LQ²`.  Full real coordinate defect jets are audited exactly through length ten. `proof/crabb_disk_flat_marked_merger.md`; `experiments/crabb_disk_one_reflection_jets.py`. |
| L158 | In the independent reflected-Rees chart, L145's model certificate obeys `U−4<=−a_LQ(z)−b_L(|r|²+sum|w_k|²)` near the Crabb point | **PROVED POSITIVE-GAP MIXED MERGER (2026-07-23)** | L152 bounds the zero-reflection dual value by `4−aQ`; L154 makes its full one-reflection row `O(Q)`, and L144 makes the reflected Hessian uniformly negative.  For the nonnegative model gap, L157 gives `Delta(z,0)=O(Q²)`.  A one-dimensional Taylor test for a nonnegative function gives `||D_eta Delta(z,0)||²<=C Delta(z,0)`, hence `O(Q²)` and therefore `D_eta Delta=O(Q)`.  L145--L146 make the compact gap Hessian zero at the apex, so continuity and Taylor expansion give `Delta<=CQ²+CQ||eta||+epsilon||eta||²`.  Shrinking absorbs all three terms into the two negative faces. `proof/crabb_disk_flat_marked_merger.md`. |
| L159-old | The prepared reflected coordinates and raw Toeplitz grades satisfy `|c|^(2L)+sum_k|w_k|² asymp |c|^(2L)+sum_j|z_j|²|c|^(2(L−j))` uniformly as `(z,c)->0` | **DISPROVED EXACTLY BY NONLINEAR GRADE ALIASING (2026-07-23)** | For `L=6` and the sole raw coefficient `z_1=t`, exact elimination gives the prepared characteristic coefficient `a_3=32t^7/((6t²−1)(16t⁴−14t²+1))`.  With `c=t^5`, the raw norm is `asymp t^52`, while the prepared norm contains `|c³a_3|² asymp1024t^44`; their ratio diverges.  Ordinary invertibility of the unweighted coefficient map cannot be promoted through the singular weighted pullback.  This does not hurt L158's sign because the alias creates extra negative prepared descent.  A one-sided lower estimate may still be useful but is not proved. `proof/crabb_disk_flat_marked_merger.md`; `experiments/crabb_disk_model_complement.py`. |
| L160 | After anchoring on the full circular-range manifold, the offset-one reflected variable remains strictly descending after optimizing every true coercive circular normal | **PROVED FIRST CIRCULAR-NORMAL SCHUR FACE (2026-07-23)** | Optimizing all of L118's strong space over the Toeplitz chart is the wrong target: some strong directions recenter onto the larger disk manifold and cancel the Toeplitz quartic, as a direct Riemann/Stein probe confirms.  The correct L115 normal-quotient count leaves `2p−4` coercive directions after splitting off the complex elliptic soft normal; it is not a Euclidean Hessian-range intersection count.  Fourier selection makes the first reflected variable `w_1` couple only to the bottom entry `y=Y_(L0)`.  Exact weighted Riemann/defect recurrences give cross coefficient `−8(5L−1)/L`; L65 supplies the square `−(L²+36L−13)|y|²/(6L)`.  Completion leaves `−R_L|w_1|²`, with `R_L=32(L−1)(2L²−L+3)/(L(L²+36L−13))>0`.  Higher reflected grades and the uniform full-disk tubular lift remain. `proof/crabb_circular_normal_face.md`; `experiments/crabb_circular_normal_face.py`. |
| L161 | The first character-allowed circular-normal cross for the grade-two compact coordinate `w_2=ac²` vanishes in every size `L+1>=5` | **PROVED GRADE-TWO LEADING NORMAL CANCELLATION (2026-07-23)** | Circle character leaves only normal mode `L`, represented by `(E_(L−1,0)+E_(L,1))/sqrt(2)`.  Give `a,c` weight one and this normal weight three.  Exact physical-disk, Riemann, three-defect-jet, and Stein endpoint recurrences give the flat coefficient `−64a²c⁴` and zero linear normal coefficient at weight six.  For `L>=7` both endpoint derivatives vanish by support separation; `L=4,5` vanish directly, while the sole `L=6` collision gives `d_−=−2c(a²+2ac−2c²)` and `d_+=4d_−`, cancelling in `lambda_+/lambda_-`.  This proves the leading selection, not the uniform Rees-division estimate through the axes or any higher grade. `proof/crabb_grade_two_normal_selection.md`; `experiments/crabb_grade_two_normal_selection.py`. |
| L162 | At every positive phase-palindromic Crabb equality anchor, L123's explicit rank-one Stein certificate is stationary under every ambient complex matrix perturbation after the first Schwarz/Riemann correction | **PROVED ALL-SIZE AMBIENT FIRST-ORDER STATIONARITY (2026-07-23)** | Fix the anchor metric `K` and defect `q`, and continue the Stein solution under `A+sE`.  L123's Blaschke lower bound touches the certificate condition at four, so the certificate derivative is `8 Re(q* DB(A)[E]e_L)`.  L156's endpoint resolvents turn this functional into the exact boundary integral with density `1/|g#|²`.  The endpoint transfer function is `(L+2 sum(L−k)conj(z_k)t^k)/(1+2 sum conj(z_k)t^k)`, and its real boundary kernel is exactly `(f*Kf)/|g#|²`; hence the Schwarz correction has the same real derivative for every `E`.  This proves a critical-ridge theorem, not a negative normal Hessian or a full neighborhood bound. `proof/crabb_equality_ambient_stationarity.md`; exact rational audits through `p=6` and every real/imaginary matrix unit numerically through `p=8`. |
| L163 | Every reflected grade `k>=2` has zero leading compact cross with its only character-eligible coercive circular normal | **PROVED BY L171--L172 (2026-07-23)** | Put `d=k+1` and `m=L+2−k`.  L149's marking and L162 kill the zero-reflection sector; L166 transfers the sole one-reflection row to the prepared dual.  L171 proves that the L143 reflected tangent and `D(C^L)[E_d]` have disjoint top-coupling indices, killing every ordinary quadratic singular-Hessian term.  L172 writes the remaining L156 endpoint functional as a Cauchy residue.  Its reflected Blaschke powers `L−k,L+k` multiplied by the absolute residual power `1−k` give only `L−2k+1,L+1>=1`, never the residue power `−1`.  Grade one remains the separate nonzero terminal/feedthrough face. `proof/crabb_all_grade_normal_bridge.md`; `proof/crabb_dual_endpoint_residue.md`. |
| L164 | The locally optimized rank-one Stein Gramian of every real persymmetric operator near a Crabb block obeys `JP^(-1)J=alpha P` | **PROVED RECIPROCAL-REVERSAL FIXED-POINT THEOREM (2026-07-23)** | Rank-one Stein duality gives `P^(-1)-TP^(-1)T^T=rr^T`; persymmetry makes `JP^(-1)J` another rank-one Stein Gramian for the same operator with the same condition number.  Projective normalization gives an analytic involution whose fixed-defect linearization has no nonzero Crabb tangent, so its local fixed branch is unique and equals L118's critical branch.  This proves the identity, `alpha=(det P)^(-2/p)`, reversed reciprocal eigenpairing, and a half-order optimizer recurrence: defect jet `j` is solved from self-duality at order `j`, rather than endpoint stationarity at order `2j`.  Exact rational duality and four reflected jets regenerate cleanly and match the direct optimizer.  This removes one independent endpoint series from L163 but does not force the remaining condition derivative to vanish. `proof/crabb_reciprocal_reversal.md`; `experiments/crabb_reciprocal_reversal.py`. |
| L165 | The normal derivative of L118's optimized envelope is one adjoint-Stein trace with no differentiated defect jets | **PROVED ADJOINT HALF-ORDER + ENDPOINT-COMMUTATOR REDUCTION (2026-07-23)** | Put `G=v_+v_+*/lambda_+−v_-v_-*/lambda_-` and solve `Z−TZT*=G`.  Defect stationarity plus scale invariance gives `Zq=0`, while Stein-map adjointness gives `D log kappa[E]=2 Re tr(ZT*PE)=2 Re<PTZ,E>`.  Writing `M=PTZ`, the two Stein equations give `T^*M−MT^*=v_+v_+^*−v_-v_-^*`; L164 makes `M` persymmetric.  The target is a `d+1`-term convolution, split by companion form into endpoint and characteristic pieces.  Exact checks detect the nonzero grade-one exception, termwise-zero grade two, and nontrivial equal/opposite grades three and four.  L171--L172 close its all-grade total by an independent sparse-dual/residue calculation. `proof/crabb_adjoint_normal_recurrence.md`; exact checker. |
| L166 | The leading compact one-reflection/circular-normal coefficient of L118's optimized envelope equals the corresponding coefficient of the sharp prepared Blaschke lower certificate | **PROVED POSITIVITY ROW TRANSFER (2026-07-23)** | At every fixed nonzero weighted scale, the Stein-minus-Blaschke gap is nonnegative and zero on the exact elliptic axis, so its amplitude/strong Hessian is PSD.  L142--L145 make the pure reflected gap diagonal `o(epsilon^(2d))`, while the weight-`d` strong insertion gives a strong diagonal `O(epsilon^(2d))`.  PSD Cauchy--Schwarz makes the mixed gap `o(epsilon^(2d))`; analyticity removes its weight-`2d` coefficient.  This works in every size, after complex polarization, and at central folds.  It transfers only the component linear in the reflected amplitude; the common dual endpoint functional was the final gate and is closed by L171--L172. `proof/crabb_dual_normal_transfer.md`; `proof/crabb_all_grade_normal_bridge.md`; `proof/crabb_dual_endpoint_residue.md`. |
| L167 | The leading characteristic derivative of the grade-`k` eligible normalized circular normal at a Crabb block is `-k z^(k-1)` | **PROVED ALL-SIZE ADJUGATE/TRACE FORMULA (2026-07-23)** | For `C=C_(L+1)`, `adj(zI-C)=sum_(r=0)^L z^(L-r)C^r`.  The leading inverse-Riemann correction is a zero-constant polynomial in `C` and has zero trace against every `C^r`.  Only the offset-`L+1-k` subdiagonal of `N_(L+2-k)` closes a trace; it has `k` entries, and each endpoint `sqrt(2)` cancels its Riesz `1/sqrt(2)` weight.  Hence `D det(zI-C)[E_d]=-kz^(k-1)`.  The leading slice retains a zero factor exactly for `k>=2`, explaining a real grade-one distinction on L166's associated face.  A115 shows the zero factor need not persist at later mixed weights, so this is not itself L163. `proof/crabb_adjoint_normal_recurrence.md`; exact checker. |
| L168 | The first inverse-Riemann circular normal minus its optimized symmetrized defect response is an explicit logarithmic-inner endpoint tangent | **PROVED ALL-SIZE LEADING ENDPOINT TRANSFER (2026-07-23)** | For grade `k>=2` and mode `m=L+2−k`, weighted-path counting gives `D log(e0*(zI-C)^(-1)eL)[E]=kz^(-m)+beta_(L,k)z^m`, with the stated closed three-case `beta`.  The symmetrized defect endpoint factor satisfies `beta+k=delta gamma_(L,k)`, so the residual is `k(z^(-m)−z^m)`, purely imaginary on the circle.  L172 evaluates the downstream differentiated L156 functional on its remaining discrepancy. `proof/crabb_leading_endpoint_transfer.md`; exact checker. |
| L169 | Every finite rank-one Stein metric has a canonical scalar inner colligation transfer, and L164 determines both of its defect legs on the optimized Crabb branch | **PROVED GENERAL UNITARY-COLLIGATION IDENTITY (2026-07-23)** | Balancing `P-T*PT=qq*` and `P^(-1)-TP^(-1)T*=rr*` gives a contraction `S` with one-dimensional right/left defects; completing its defect columns gives a unitary `U=[[S,b],[a*,d]]`.  A one-line block determinant proves `theta(z)=d+zq*(I-zT)^(-1)r=omega det(zI-T*)/det(I-zT)`, so `theta` is inner and `Re(dot(theta)/theta)=0` on the circle for every analytic variation.  L164 supplies `r=sqrt(alpha)Jq`; the feedthrough `d` exactly carries A115's moving constant characteristic term.  L172 closes the associated direct endpoint functional needed downstream. `proof/crabb_colligation_transfer.md`; `experiments/rank_one_colligation_transfer.py`. |
| L170 | The moving characteristic feedthrough has a canonical analytic degree-reducing Schur transform whose first Crabb tangent vanishes exactly in grade one | **PROVED ALL-ORDER SCHUR CONTINUATION + LEADING DISCRIMINATOR (2026-07-23)** | For L169's inner `theta`, `d=theta(0)` and `B=(theta-d)/(z(1-conj(d)theta))` define a real-analytic finite Blaschke product of degree `p-1`; at a disk-equality anchor this is exactly `theta/z`, hence L149's prepared factor.  Linearizing at `theta=z^p`, L167 gives `dot B=0` for grade one and `dot B=-kz^(k-2)+kz^(2p-k)` for every `k>=2`, so `dot B/B=-k(z^(-m)-z^m)`.  The exact Schur step absorbs A115's later feedthrough without tracking roots.  L172 closes the differentiated L156 functional needed for L163. `proof/crabb_characteristic_schur_step.md`; extended L168 checker. |
| L171 | Reflection count and sparse dual support reduce L163 to one direct endpoint/cofactor functional | **PROVED REDUCTION; FINAL KERNEL RESOLVED BY L172 (2026-07-23)** | L162 kills the zero-reflection component.  L166 transfers the sole one-reflection row to the dual.  Exact path counting gives `D(C^L)[E_d]` on offset `k−2`, whereas L143's reflected tangent couples the top endpoint through `L−k`; their disjointness kills both ordinary quadratic singular-Hessian terms.  L168--L170 identify the remaining endpoint response as an inner tangent plus an optimized-defect relative mode.  L172 evaluates the complete L156 functional on that mode by Cauchy-residue selection. `proof/crabb_all_grade_normal_bridge.md`; `experiments/crabb_dual_normal_associated_face.py`; extended L168 checker. |
| L172 | L171's remaining direct endpoint/cofactor functional annihilates the higher-grade optimized-defect mode | **PROVED ALL-SIZE CAUCHY-RESIDUE SELECTION (2026-07-23)** | Cauchy functional calculus and L156 identify the direct row with `[z^(−1)] dot(B)_(w_k) dot(R)_s`.  L140 gives reflected Blaschke powers `L−k` and `L+k`.  L168--L170 give relative residual power `m=L+2−k`; multiplication by `F_0=2z^(−L−1)` makes the absolute residual power `1−k`.  The only products are therefore `L−2k+1` and `L+1`, both at least one for `k<=L/2`; neither can contribute a Cauchy residue.  This includes the central fold and complex polarizations.  Combined with L171, it proves L163.  An independent full bilinear-jet dual calculation gives exact zero in grades two and three and preserves three nonzero grade-one controls. `proof/crabb_dual_endpoint_residue.md`; both dual checkers. |
| L173 | The homogeneous Toeplitz-disk deficit strictly absorbs the complete true circular-normal Schur gain at every single Crabb apex | **PROVED ALL-SIZE PLÜCKER--SCHUR FACE; GAUGE-AUDITED (2026-07-24)** | The quadratic normal gradient is not zero from `L=6` onward; it equals an explicit weighted anti-diagonal of `h wedge J conj(h)`.  Takagi coordinates give `Q=4||p||²`, mutually orthogonal mode/polarization rows, and `||m_(L,k)||²=128(4k−1)² binom(L−k,3)/L⁴`.  After the required coefficient-to-physical Crabb gauge map, L65's singular path kernel gives `b_(L,k)=L^(−4)[(4k−1)²r(r−1)(r−2)/24+(2/3)k(k−1)(k−2)(r+1/4)²]`.  The first term is exactly `||m||²/512`; the positive null-lift term makes `b` strictly larger, so completing all orthogonal squares gains strictly less than `32Q`.  Exact response checks reach `L=7`; independent gauge-corrected all-mode response/curvature checks reach `L=12`.  The nonlinear transported tube remains downstream. `proof/crabb_circular_normal_plucker_schur.md`; two new checkers. |
| D2 | "Harmonic mean of difference quotients ≤ h′(ζ₀)" for conformal h onto D, convex domain, midpoint-normalized nodes | DISPROVED (2026-07-21) | Koebe 1+δ² (univalent); z+z²/4 exact 1/(1−δ²/16) (convex!); 55+/60 Herglotz-random convex maps violate (≤1.26). Equality ∀ Möbius (proved). Focus-pinning (criticality) essential — 4th "no soft proof" instance |
